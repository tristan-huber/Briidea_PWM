{
  "atomType": "Molecule",
  "x": 0,
  "y": 0,
  "uniqueID": "id-135502",
  "name": "Briidea_PWM",
  "topLevel": true,
  "allAtoms": [
    {
      "atomType": "Output",
      "x": 1,
      "y": 0.5,
      "uniqueID": "id-135503",
      "ioValues": [
        {
          "name": "number or geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        }
      ]
    },
    {
      "atomType": "Rectangle",
      "x": 0.2,
      "y": 0.4,
      "uniqueID": "id-135505",
      "ioValues": [
        {
          "name": "x length",
          "ioValue": 7.9
        },
        {
          "name": "y length",
          "ioValue": 6.4
        }
      ]
    },
    {
      "atomType": "Add-BOM-Tag",
      "x": 0.7,
      "y": 0.5,
      "uniqueID": "id-135509",
      "name": "Add BOM Tag",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "Item Name",
          "ioValue": "Bridea_PWM",
          "currentEquation": "Bridea_PWM"
        },
        {
          "name": "Number Needed",
          "ioValue": 1
        },
        {
          "name": "Cost (USD)",
          "ioValue": 70
        },
        {
          "name": "Source Link",
          "ioValue": "https://www.amazon.com/dp/B0DP1XZTR2?ref_=ppx_hzsearch_conn_dt_b_fed_asin_title_6&th=1",
          "currentEquation": "https://www.amazon.com/dp/B0DP1XZTR2?ref_=ppx_hzsearch_conn_dt_b_fed_asin_title_6&th=1"
        }
      ],
      "BOMitem": {
        "BOMitemName": "Bridea_PWM",
        "numberNeeded": 1,
        "costUSD": 70,
        "source": "https://www.amazon.com/dp/B0DP1XZTR2?ref_=ppx_hzsearch_conn_dt_b_fed_asin_title_6&th=1"
      }
    },
    {
      "atomType": "Extrude",
      "x": 0.3,
      "y": 0.6,
      "uniqueID": "id-135522",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "height",
          "ioValue": 0.125
        }
      ]
    },
    {
      "atomType": "GitHubMolecule",
      "x": 0.8,
      "y": 0.7,
      "uniqueID": "id-135531",
      "name": "phys.SetMass",
      "ioValues": [
        {
          "name": "assembly",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "mass",
          "ioValue": 1
        }
      ],
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 1,
          "y": 0.5,
          "uniqueID": "id-135527",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.2,
          "uniqueID": "id-135528",
          "name": "assembly",
          "type": "geometry",
          "options": []
        },
        {
          "atomType": "Code",
          "x": 0.4,
          "y": 0.7,
          "uniqueID": "id-135529",
          "ioValues": [
            {
              "name": "assembly",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "mass",
              "ioValue": 1
            }
          ],
          "interpreterVersion": 1,
          "code": "// Set mass\nfunction run(assembly: Assembly, mass: number) {\n  const initial = assembly.metadata ? assembly.metadata : {}\n  initial.mass = mass;\n  assembly.metadata = initial;\n  return assembly?.skipDisjointPostprocess();\n}",
          "compiledCode": "\"use strict\";\n// Set mass\nfunction run(assembly, mass) {\n    const initial = assembly.metadata ? assembly.metadata : {};\n    initial.mass = mass;\n    assembly.metadata = initial;\n    return assembly?.skipDisjointPostprocess();\n}\n"
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.2,
          "uniqueID": "id-135530",
          "name": "mass",
          "options": []
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "assembly",
          "ap1ID": "id-135528",
          "ap2ID": "id-135529"
        },
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-135529",
          "ap2ID": "id-135527"
        },
        {
          "ap2Name": "mass",
          "ap1ID": "id-135530",
          "ap2ID": "id-135529"
        }
      ],
      "parentRepo": {
        "lastFoundGit": "2026-09-15T18:23:38.997Z",
        "repoName": "phys.SetMass",
        "dateCreated": "2026-08-12T16:42:56Z",
        "likes": 0,
        "readMe": "https://raw.githubusercontent.com/tristan-huber/phys.SetMass/master/README.md?sanitize=true",
        "yyyy": 2026,
        "topMoleculeID": "id-43071",
        "githubMoleculesUsed": [],
        "html_url": "https://github.com/tristan-huber/phys.SetMass",
        "topics": [
          "abundance-tool"
        ],
        "pullRequests": [],
        "parentRepo": null,
        "forks": 0,
        "dateModified": "739840",
        "ranking": 0.6,
        "searchField": "phys.setmass tristan-huber adds a mass to the metadata of an assembly. in general this superseed masses of the leafs of an assembly. consumed by other physics methods in utils. arbitrary units.",
        "owner": "tristan-huber",
        "description": "Adds a mass to the metadata of an assembly. In general this superseed masses of the leafs of an assembly. Consumed by other physics methods in utils. Arbitrary units.",
        "contentURL": "https://raw.githubusercontent.com/tristan-huber/phys.SetMass/master/project.abundance?sanitize=true",
        "svgURL": "https://raw.githubusercontent.com/tristan-huber/phys.SetMass/master/project.svg?sanitize=true",
        "isLocal": false
      },
      "unitsKey": "Unitless",
      "fileTypeVersion": 1,
      "lastReloadedFromGithubAt": 1789510501728
    },
    {
      "atomType": "Circle",
      "x": 0.1,
      "y": 0.2,
      "uniqueID": "id-135543",
      "ioValues": [
        {
          "name": "diameter",
          "ioValue": 0.125
        }
      ]
    },
    {
      "atomType": "Constant",
      "x": 0.1,
      "y": 0.5,
      "uniqueID": "id-135546",
      "name": "width",
      "value": 7.9
    },
    {
      "atomType": "Constant",
      "x": 0.1,
      "y": 0.7,
      "uniqueID": "id-135548",
      "name": "length",
      "value": 6.4
    },
    {
      "atomType": "Move",
      "x": 0.2,
      "y": 0.1,
      "uniqueID": "id-135552",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": 3.8000000000000003,
          "currentEquation": "width / 2 - 0.15"
        },
        {
          "name": "yDist",
          "ioValue": 2.2,
          "currentEquation": "length / 2 - 1"
        },
        {
          "name": "zDist",
          "ioValue": 0
        }
      ]
    },
    {
      "atomType": "Move",
      "x": 0.2,
      "y": 0.3,
      "uniqueID": "id-135558",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": -3.8000000000000003,
          "currentEquation": "-width / 2 + 0.15"
        },
        {
          "name": "yDist",
          "ioValue": 2.2,
          "currentEquation": "length / 2 - 1"
        },
        {
          "name": "zDist",
          "ioValue": 0
        }
      ]
    },
    {
      "atomType": "Assembly",
      "x": 0.3,
      "y": 0.2,
      "uniqueID": "id-135564",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": null
        },
        {
          "name": "Shape 2",
          "ioValue": null
        }
      ]
    },
    {
      "atomType": "GitHubMolecule",
      "x": 0.4,
      "y": 0.2,
      "uniqueID": "id-135573",
      "name": "util.Mirror",
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 1,
          "y": 0.5,
          "uniqueID": "id-135570",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Code",
          "x": 0.4,
          "y": 0.2,
          "uniqueID": "id-135571",
          "ioValues": [
            {
              "name": "assembly",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "interpreterVersion": 1,
          "code": "function run(assembly: Assembly) {\n  return assembly.onLeafs((leaf) => {\n    if (leaf.geometry instanceof replicad.Drawing) {\n      leaf.geometry = leaf.geometry.mirror([1, 0], [0, 0], \"plane\")\n    } else {\n      leaf.geometry = leaf.geometry.mirror(\"XZ\")\n      if (leaf.geometry instanceof replicad.Face) {\n        leaf.geometry = leaf.geometry.flipOrientation()\n      }\n    }\n    return leaf\n  })\n}",
          "compiledCode": "\"use strict\";\nfunction run(assembly) {\n    return assembly.onLeafs((leaf) => {\n        if (leaf.geometry instanceof replicad.Drawing) {\n            leaf.geometry = leaf.geometry.mirror([1, 0], [0, 0], \"plane\");\n        }\n        else {\n            leaf.geometry = leaf.geometry.mirror(\"XZ\");\n            if (leaf.geometry instanceof replicad.Face) {\n                leaf.geometry = leaf.geometry.flipOrientation();\n            }\n        }\n        return leaf;\n    });\n}\n"
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.2,
          "uniqueID": "id-135572",
          "name": "assembly",
          "options": []
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-135571",
          "ap2ID": "id-135570"
        },
        {
          "ap2Name": "assembly",
          "ap1ID": "id-135572",
          "ap2ID": "id-135571"
        }
      ],
      "parentRepo": {
        "lastFoundGit": "2026-09-15T18:23:38.997Z",
        "privateRepo": false,
        "repoName": "util.Mirror",
        "dateCreated": "2026-07-09T18:32:34Z",
        "likes": 0,
        "readMe": "https://raw.githubusercontent.com/tristan-huber/util.Mirror/master/README.md?sanitize=true",
        "yyyy": 2026,
        "topMoleculeID": "id-21902",
        "githubMoleculesUsed": [],
        "html_url": "https://github.com/tristan-huber/util.Mirror",
        "topics": [
          "abundance-tool"
        ],
        "pullRequests": [],
        "parentRepo": null,
        "forks": 0,
        "dateModified": "739814",
        "ranking": 8.4,
        "searchField": "util.mirror tristan-huber mirror an assembly across the xz plane. abundance-tool",
        "owner": "tristan-huber",
        "description": "Mirror an assembly across the XZ plane.",
        "contentURL": "https://raw.githubusercontent.com/tristan-huber/util.Mirror/master/project.abundance?sanitize=true",
        "svgURL": "https://raw.githubusercontent.com/tristan-huber/util.Mirror/master/project.svg?sanitize=true",
        "isLocal": false
      },
      "unitsKey": "Unitless",
      "fileTypeVersion": 1,
      "lastReloadedFromGithubAt": 1789510730408
    },
    {
      "atomType": "Assembly",
      "x": 0.5,
      "y": 0.2,
      "uniqueID": "id-135582",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": null
        },
        {
          "name": "Shape 2",
          "ioValue": null
        }
      ]
    },
    {
      "atomType": "Move",
      "x": 0.6,
      "y": 0.3,
      "uniqueID": "id-135587",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": 0
        },
        {
          "name": "yDist",
          "ioValue": 0
        },
        {
          "name": "zDist",
          "ioValue": -0.5
        }
      ]
    },
    {
      "atomType": "Extrude",
      "x": 0.6,
      "y": 0.3,
      "uniqueID": "id-135593",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "height",
          "ioValue": 1
        }
      ]
    },
    {
      "atomType": "Color",
      "x": 0.7,
      "y": 0.3,
      "uniqueID": "id-135597",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        }
      ],
      "selectedColor": "Keep Out"
    },
    {
      "atomType": "Rectangle",
      "x": 0.2,
      "y": 0.8,
      "uniqueID": "id-135600",
      "ioValues": [
        {
          "name": "x length",
          "ioValue": 6.9,
          "currentEquation": "width - 1"
        },
        {
          "name": "y length",
          "ioValue": 6.4
        }
      ]
    },
    {
      "atomType": "Extrude",
      "x": 0.3,
      "y": 0.8,
      "uniqueID": "id-135604",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "height",
          "ioValue": 4.2
        }
      ]
    },
    {
      "atomType": "Assembly",
      "x": 0.7,
      "y": 0.8,
      "uniqueID": "id-135608",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": null
        },
        {
          "name": "Shape 2",
          "ioValue": null
        }
      ]
    },
    {
      "atomType": "Fusion",
      "x": 0.5,
      "y": 0.8,
      "uniqueID": "id-135612",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": {
            "geometry": "extrude-rectangle-6.9-6.4-{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]}-4.2",
            "dimension": "3D",
            "plane": {
              "origin": [
                0,
                0,
                0
              ],
              "xDir": [
                1,
                0,
                0
              ],
              "normal": [
                0,
                0,
                1
              ]
            },
            "color": "#aad7f2",
            "tags": [],
            "bom": []
          }
        },
        {
          "name": "Shape 2",
          "ioValue": {
            "geometry": "extrude-rectangle-7.9-6.4-{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]}-0.125",
            "dimension": "3D",
            "plane": {
              "origin": [
                0,
                0,
                0
              ],
              "xDir": [
                1,
                0,
                0
              ],
              "normal": [
                0,
                0,
                1
              ]
            },
            "color": "#aad7f2",
            "tags": [],
            "bom": []
          }
        }
      ],
      "unionType": "Fusion"
    },
    {
      "atomType": "GitHubMolecule",
      "x": 0.4,
      "y": 0.4,
      "uniqueID": "id-135788",
      "name": "Sphere",
      "ioValues": [
        {
          "name": "Radius",
          "ioValue": 10
        }
      ],
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 1,
          "y": 0.5,
          "uniqueID": "id-135784",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Code",
          "x": 0.5,
          "y": 0.4,
          "uniqueID": "id-135785",
          "ioValues": [
            {
              "name": "radius",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "interpreterVersion": 0,
          "code": "//Inputs:[radius];\n\n//let shape = replicad.makeSphere(radius)\n\nconst newPlane = new replicad.Plane().pivot(0, 'Y');\n\n return { geometry: [replicad.makeSphere(radius)], tags: [],\n      color: '#A3CE5B',\n      bom:[],\n      plane: newPlane}\n\n\n    /**\n    To Use the Code Atom, enter your inputs to the input list a.e Inputs:[shape, height]\n    If your input is connected to another atom with a replicad geometry you can access its geometry by looking up its ID in your library. a.e library[Input1].geometry[0] \n    Use any replicad available methods to modify your geometry. Learn more about all of the available methods at \n    https://replicad.xyz/docs/introapp/UserGuide.html \n    Return a replicad object that includes geometry, color, tags and plane. \n\n\n    Example Code Atom:\n\n      Inputs:[shape, x];\n\n      let finalShape = library[shape].geometry[0].clone.translate[x,0,0]\n\n      return {geometry: finalShape, color: library[shape].color, plane: library[shape].plane, tags: library[shape].tags }\n\n      - See more examples at _______ \n\n\n    */\n"
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.2,
          "uniqueID": "id-135786",
          "name": "Radius",
          "type": "number",
          "options": []
        },
        {
          "atomType": "Constant",
          "x": 0.2,
          "y": 0.9,
          "uniqueID": "id-135787",
          "value": 10
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-135785",
          "ap2ID": "id-135784"
        },
        {
          "ap2Name": "radius",
          "ap1ID": "id-135786",
          "ap2ID": "id-135785"
        }
      ],
      "parentRepo": {
        "lastFoundGit": "2026-09-20T02:23:39.099Z",
        "privateRepo": false,
        "repoName": "Sphere",
        "dateCreated": "2025-06-20T21:36:53Z",
        "likes": 0,
        "pngURL": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/project.png?sanitize=true",
        "readme": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/README.md?sanitize=true",
        "yyyy": 2025,
        "topMoleculeID": "",
        "userRanking": 0,
        "userSetAsThumbnail": false,
        "githubMoleculesUsed": [],
        "html_url": "https://github.com/BarbourSmith/Sphere",
        "topics": [],
        "pullRequests": [],
        "parentRepo": "alzatin/Sphere",
        "forks": 0,
        "dateModified": "2025-10-24T17:13:15Z",
        "ranking": 1,
        "searchField": "sphere barboursmith creates a sphere ",
        "owner": "BarbourSmith",
        "description": "Creates a sphere",
        "contentURL": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/project.abundance?sanitize=true",
        "svgURL": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/project.svg?sanitize=true",
        "isLocal": false
      },
      "unitsKey": "Inches",
      "fileTypeVersion": 1,
      "lastReloadedFromGithubAt": 1790207515283
    },
    {
      "atomType": "Export",
      "x": 0.6,
      "y": 0.6,
      "uniqueID": "id-135798",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "File Type",
          "ioValue": "STL",
          "currentEquation": "STL"
        },
        {
          "name": "Resolution (dpi)",
          "ioValue": 96
        },
        {
          "name": "STL Tolerance",
          "ioValue": 0.002
        },
        {
          "name": "Part Name",
          "ioValue": "sphere_3",
          "currentEquation": "sphere_3"
        }
      ],
      "type": "STL",
      "resolution": 96,
      "importIndex": 0,
      "partName": "sphere_3"
    },
    {
      "atomType": "GitHubMolecule",
      "x": 0.4,
      "y": 0.5,
      "uniqueID": "id-135808",
      "name": "Sphere",
      "ioValues": [
        {
          "name": "Radius",
          "ioValue": 0.1
        }
      ],
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 1,
          "y": 0.5,
          "uniqueID": "id-135804",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Code",
          "x": 0.5,
          "y": 0.5,
          "uniqueID": "id-135805",
          "ioValues": [
            {
              "name": "radius",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "interpreterVersion": 0,
          "code": "//Inputs:[radius];\n\n//let shape = replicad.makeSphere(radius)\n\nconst newPlane = new replicad.Plane().pivot(0, 'Y');\n\n return { geometry: [replicad.makeSphere(radius)], tags: [],\n      color: '#A3CE5B',\n      bom:[],\n      plane: newPlane}\n\n\n    /**\n    To Use the Code Atom, enter your inputs to the input list a.e Inputs:[shape, height]\n    If your input is connected to another atom with a replicad geometry you can access its geometry by looking up its ID in your library. a.e library[Input1].geometry[0] \n    Use any replicad available methods to modify your geometry. Learn more about all of the available methods at \n    https://replicad.xyz/docs/introapp/UserGuide.html \n    Return a replicad object that includes geometry, color, tags and plane. \n\n\n    Example Code Atom:\n\n      Inputs:[shape, x];\n\n      let finalShape = library[shape].geometry[0].clone.translate[x,0,0]\n\n      return {geometry: finalShape, color: library[shape].color, plane: library[shape].plane, tags: library[shape].tags }\n\n      - See more examples at _______ \n\n\n    */\n"
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.2,
          "uniqueID": "id-135806",
          "name": "Radius",
          "type": "number",
          "options": []
        },
        {
          "atomType": "Constant",
          "x": 0.2,
          "y": 0.9,
          "uniqueID": "id-135807",
          "value": 10
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-135805",
          "ap2ID": "id-135804"
        },
        {
          "ap2Name": "radius",
          "ap1ID": "id-135806",
          "ap2ID": "id-135805"
        }
      ],
      "parentRepo": {
        "lastFoundGit": "2026-09-20T02:23:39.099Z",
        "privateRepo": false,
        "repoName": "Sphere",
        "dateCreated": "2024-12-16T03:57:03Z",
        "likes": 0,
        "pngURL": "https://raw.githubusercontent.com/alzatin/Sphere/master/project.png?sanitize=true",
        "readme": "https://raw.githubusercontent.com/alzatin/Sphere/master/README.md?sanitize=true",
        "yyyy": 2024,
        "topMoleculeID": "",
        "userRanking": 0.2,
        "userSetAsThumbnail": false,
        "githubMoleculesUsed": [],
        "html_url": "https://github.com/alzatin/Sphere",
        "topics": [],
        "pullRequests": [],
        "forks": 1,
        "dateModified": "2025-06-20T21:44:34Z",
        "ranking": 1,
        "searchField": "sphere alzatin creates a sphere ",
        "owner": "alzatin",
        "description": "Creates a sphere",
        "contentURL": "https://raw.githubusercontent.com/alzatin/Sphere/master/project.abundance?sanitize=true",
        "svgURL": "https://raw.githubusercontent.com/alzatin/Sphere/master/project.svg?sanitize=true",
        "isLocal": false
      },
      "unitsKey": "Inches",
      "fileTypeVersion": 1,
      "lastReloadedFromGithubAt": 1790207640363
    },
    {
      "atomType": "GitHubMolecule",
      "x": 0.5,
      "y": 0.4,
      "uniqueID": "id-135935",
      "name": "Cross-Section",
      "ioValues": [
        {
          "name": "Offset",
          "ioValue": 0
        },
        {
          "name": "Angle X",
          "ioValue": 90
        },
        {
          "name": "Angle Z",
          "ioValue": 0
        },
        {
          "name": "Angle Y",
          "ioValue": 0
        }
      ],
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 1,
          "y": 0.5,
          "uniqueID": "id-135924",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.2,
          "uniqueID": "id-135925",
          "name": "Geometry",
          "type": "number",
          "options": []
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.2,
          "uniqueID": "id-135926",
          "name": "Offset",
          "type": "number",
          "options": []
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.3,
          "uniqueID": "id-135927",
          "name": "Angle X",
          "type": "number",
          "options": []
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.4,
          "uniqueID": "id-135928",
          "name": "Angle Z",
          "type": "number",
          "options": []
        },
        {
          "atomType": "Input",
          "x": 0,
          "y": 0.5,
          "uniqueID": "id-135929",
          "name": "Angle Y",
          "type": "number",
          "options": []
        },
        {
          "atomType": "Rectangle",
          "x": 0.2,
          "y": 0.4,
          "uniqueID": "id-135930",
          "ioValues": [
            {
              "name": "x length",
              "ioValue": 40000
            },
            {
              "name": "y length",
              "ioValue": 40000
            }
          ]
        },
        {
          "atomType": "Extrude",
          "x": 0.3,
          "y": 0.5,
          "uniqueID": "id-135931",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "height",
              "ioValue": 4000
            }
          ]
        },
        {
          "atomType": "Move",
          "x": 0.3,
          "y": 0.7,
          "uniqueID": "id-135932",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "xDist",
              "ioValue": 0
            },
            {
              "name": "yDist",
              "ioValue": 0
            },
            {
              "name": "zDist",
              "ioValue": 0
            }
          ]
        },
        {
          "atomType": "Rotate",
          "x": 0.4,
          "y": 0.8,
          "uniqueID": "id-135933",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "x-axis degrees",
              "ioValue": 90
            },
            {
              "name": "y-axis degrees",
              "ioValue": 0
            },
            {
              "name": "z-axis degrees",
              "ioValue": 0
            }
          ]
        },
        {
          "atomType": "Difference",
          "x": 0.5,
          "y": 0.2,
          "uniqueID": "id-135934",
          "ioValues": [
            {
              "name": "retain",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "remove",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "retain",
          "ap1ID": "id-135925",
          "ap2ID": "id-135934"
        },
        {
          "ap2Name": "zDist",
          "ap1ID": "id-135926",
          "ap2ID": "id-135932"
        },
        {
          "ap2Name": "x-axis degrees",
          "ap1ID": "id-135927",
          "ap2ID": "id-135933"
        },
        {
          "ap2Name": "z-axis degrees",
          "ap1ID": "id-135928",
          "ap2ID": "id-135933"
        },
        {
          "ap2Name": "y-axis degrees",
          "ap1ID": "id-135929",
          "ap2ID": "id-135933"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-135930",
          "ap2ID": "id-135931"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-135931",
          "ap2ID": "id-135932"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-135932",
          "ap2ID": "id-135933"
        },
        {
          "ap2Name": "remove",
          "ap1ID": "id-135933",
          "ap2ID": "id-135934"
        },
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-135934",
          "ap2ID": "id-135924"
        }
      ],
      "parentRepo": {
        "lastFoundGit": "2026-09-20T02:23:39.099Z",
        "privateRepo": false,
        "repoName": "Cross-Section",
        "dateCreated": "2025-02-04T20:50:02Z",
        "likes": 0,
        "pngURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.png?sanitize=true",
        "readme": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/README.md?sanitize=true",
        "yyyy": 2025,
        "topMoleculeID": "",
        "userRanking": 0,
        "userSetAsThumbnail": false,
        "githubMoleculesUsed": [],
        "html_url": "https://github.com/BarbourSmith/Cross-Section",
        "topics": [],
        "pullRequests": [],
        "forks": 0,
        "dateModified": "2026-01-23T18:20:41Z",
        "ranking": 3,
        "searchField": "cross-section barboursmith takes a cross section of the input ",
        "owner": "BarbourSmith",
        "description": "Takes a cross section of the input",
        "contentURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.abundance?sanitize=true",
        "svgURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.svg?sanitize=true",
        "isLocal": false
      },
      "unitsKey": "MM",
      "fileTypeVersion": 1,
      "lastReloadedFromGithubAt": 1790207732336
    },
    {
      "atomType": "Molecule",
      "x": 0.8,
      "y": 0.4,
      "uniqueID": "id-135975",
      "name": "Molecule",
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 1,
          "y": 0.5,
          "uniqueID": "id-135977",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Circle",
          "x": 0.2,
          "y": 0.9,
          "uniqueID": "id-135979",
          "ioValues": [
            {
              "name": "diameter",
              "ioValue": 10
            }
          ]
        },
        {
          "atomType": "Circle",
          "x": 0.1,
          "y": 0.3,
          "uniqueID": "id-135982",
          "ioValues": [
            {
              "name": "diameter",
              "ioValue": 2
            }
          ]
        },
        {
          "atomType": "Move",
          "x": 0.3,
          "y": 0.4,
          "uniqueID": "id-135985",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "xDist",
              "ioValue": 0
            },
            {
              "name": "yDist",
              "ioValue": 0
            },
            {
              "name": "zDist",
              "ioValue": 5
            }
          ]
        },
        {
          "atomType": "Loft",
          "x": 0.3,
          "y": 0.5,
          "uniqueID": "id-135991",
          "ioValues": [
            {
              "name": "Shape 1",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "Shape 2",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "Shape 3",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "Shape 1",
              "ioValue": {
                "geometry": "rotate-move-rectangle-10-10-0-0-0-0-0-45",
                "dimension": "2D",
                "plane": {
                  "origin": [
                    0,
                    0,
                    5
                  ],
                  "xDir": [
                    1,
                    0,
                    0
                  ],
                  "normal": [
                    0,
                    0,
                    1
                  ]
                },
                "color": "#aad7f2",
                "tags": [],
                "bom": []
              }
            },
            {
              "name": "Shape 2",
              "ioValue": {
                "geometry": "rectangle-10-10",
                "dimension": "2D",
                "plane": {
                  "origin": [
                    0,
                    0,
                    0
                  ],
                  "xDir": [
                    1,
                    0,
                    0
                  ],
                  "normal": [
                    0,
                    0,
                    1
                  ]
                },
                "color": "#aad7f2",
                "tags": [],
                "bom": []
              }
            },
            {
              "name": "Shape 3",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "closedSelection": 0
        },
        {
          "atomType": "Extrude",
          "x": 0.3,
          "y": 0.1,
          "uniqueID": "id-135996",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "height",
              "ioValue": 10
            }
          ]
        },
        {
          "atomType": "Move",
          "x": 0.5,
          "y": 0.5,
          "uniqueID": "id-136000",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "xDist",
              "ioValue": -5
            },
            {
              "name": "yDist",
              "ioValue": -2
            },
            {
              "name": "zDist",
              "ioValue": 2.2
            }
          ]
        },
        {
          "atomType": "Rotate",
          "x": 0.4,
          "y": 0.2,
          "uniqueID": "id-136006",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "x-axis degrees",
              "ioValue": 0
            },
            {
              "name": "y-axis degrees",
              "ioValue": 90
            },
            {
              "name": "z-axis degrees",
              "ioValue": 30
            }
          ]
        },
        {
          "atomType": "Difference",
          "x": 0.6,
          "y": 0.9,
          "uniqueID": "id-136012",
          "ioValues": [
            {
              "name": "retain",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "remove",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Export",
          "x": 0.9,
          "y": 0.6,
          "uniqueID": "id-136016",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "File Type",
              "ioValue": "STL",
              "currentEquation": "STL"
            },
            {
              "name": "Resolution (dpi)",
              "ioValue": 96
            },
            {
              "name": "STL Tolerance",
              "ioValue": 0.002
            },
            {
              "name": "Part Name",
              "ioValue": "test",
              "currentEquation": "test"
            }
          ],
          "type": "STL",
          "resolution": 96,
          "importIndex": 0,
          "partName": "test"
        },
        {
          "atomType": "Code",
          "x": 0.8,
          "y": 0.1,
          "uniqueID": "id-136199",
          "ioValues": [
            {
              "name": "surface",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "tolerance",
              "ioValue": 0.01
            },
            {
              "name": "asSingular",
              "ioValue": true
            }
          ],
          "interpreterVersion": 1,
          "code": "// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(\n  surface: Assembly,\n  tolerance: number = 0.01,\n  asSingular: boolean = false): Assembly {\n  const toUnroll: { srcLeaf: Assembly, mesh: Mesh }[] = [];\n  surface.onLeafs((leaf) => {\n    const sface = leaf.getSelectedFaces().filter((f) => f != undefined);\n    if (sface.length > 0) {\n      toUnroll.push(...sface.map((f) => { return { srcLeaf: leaf, mesh: faceToMesh(f, tolerance) }; }))\n    } else if (leaf.geometry instanceof replicad.Face) {\n      toUnroll.push({ srcLeaf: leaf, mesh: faceToMesh(leaf.geometry, tolerance) })\n    }\n    return leaf;\n  });\n  if (toUnroll.length == 0) {\n    throw new Error(\"No selected faces, and not an assembly of face geoms\")\n  }\n\n  if (asSingular) {\n    // combine meshes first, then unroll\n    const foo = toUnroll.reduce((accumulator: Mesh, curr) => {\n      return combineMeshs(accumulator, curr.mesh)\n    }, { positions: [], tris: [] })\n    const unrolled = unrollArap(foo)\n    const distortion = measureDistortion(foo, unrolled)\n    console.log(distortion.triStretch.length)\n    console.log(foo.tris.length)\n    console.log(`pointref range. input mesh: ${foo.positions.length} result mesh: ${unrolled.positions.length}`)\n    return new Assembly({\n      geometry: unrolledWithHoles(unrolled)\n    })\n\n  } else {\n    let ybuffer = 0;\n    const result = toUnroll.map((leaf) => {\n      const unrollResult = unrollArap(leaf.mesh)\n      const drawing = unrolledWithHoles(unrollResult).translate(0, ybuffer)\n      ybuffer += drawing.boundingBox.height * 1.1\n      return new Assembly({\n        ...leaf.srcLeaf,\n        geometry: drawing\n      })\n    })\n    return new Assembly({ geometry: result.flat() }).skipDisjointPostprocess();\n  }\n}\n\nfunction unrolledWithHoles(unrolResult): replicad.Drawing {\n  let res = xysToClosedDrawing(unrolResult.outline)\n  unrolResult.innerWires.forEach((inner) => {\n    res = res.cut(xysToClosedDrawing(inner))\n  })\n  return res;\n}\n\nfunction combineMeshs(m1: Mesh, m2: Mesh): Mesh {\n  console.log(`m1, position count: ${m1.positions.length} tris count: ${m1.tris.length}`)\n  console.log(`m2, position count: ${m2.positions.length} tris count: ${m2.tris.length}`)\n  const floatEq = (a: number, b: number) => {\n    return Math.abs(a - b) < 0.000001\n  }\n\n  // find match points TODO this is deeply suboptimal. Esp if combineMeshs is called more than once.\n  const indexTuples: { m1: number, m2: number }[] = []\n  m1.positions.forEach((point, index) => {\n    m2.positions.forEach((p2, i2) => {\n      if (floatEq(point.x, p2.x) && floatEq(point.y, p2.y) && floatEq(point.z, p2.z)) {\n        console.log('found a match')\n        indexTuples.push({ m1: index, m2: i2 })\n      }\n    })\n  })\n  console.log(indexTuples)\n\n  const shiftTris = m2.tris.map((tri) => {\n    return tri.map((pointIndex) => pointIndex += m1.positions.length) as Tri3\n  })\n  // replace equivalent points from meshes\n  const dedupTris = shiftTris.map((tri) => {\n    return tri.map((pointIndex) => {\n      const unshift = pointIndex - m1.positions.length;\n      const i = indexTuples.map((f) => f.m2).indexOf(unshift)\n      if (i >= 0) {\n        return indexTuples[i].m1\n      }\n      return pointIndex\n    }) as Tri3\n  })\n  const res = {\n    positions: m1.positions.concat(m2.positions),\n    tris: m1.tris.concat(dedupTris)\n  }\n  console.log(`res, position count: ${res.positions.length} tris count: ${res.tris.length}`)\n  return res;\n}\n\nfunction faceToMesh(face: replicad.Face, tolerance: number): Mesh {\n  const replMesh = face.mesh({ tolerance: tolerance, angularTolerance: 0.1 });\n  // translate\n  const points: Point3D[] = []\n  const v = replMesh.vertices\n  for (let i = 0; i < v.length; i += 3) {\n    points.push({ x: v[i], y: v[i + 1], z: v[i + 2] })\n  }\n  const tris: Tri3[] = []\n  const t = replMesh.triangles\n  for (let i = 0; i < t.length; i += 3) {\n    tris.push([t[i], t[i + 1], t[i + 2]])\n  }\n  return { positions: points, tris: tris }\n}\n\nfunction xysToClosedDrawing(points: Point2D[]) {\n  const pen = replicad.draw([points[0].x, points[0].y]);\n  for (let i = 1; i < points.length; i++) {\n    pen.lineTo([points[i].x, points[i].y]);\n  }\n  return pen.close();\n}\n\n\n\nexport type Point2D = { x: number; y: number }\nexport type Point3D = { x: number; y: number; z: number }\n\nexport type Tri3 = [number, number, number]\n\nexport type Mesh = {\n  positions: Point3D[]\n  tris: Tri3[]\n}\n\nexport type FlatMesh = {\n  positions: Point2D[]\n  outline: Point2D[]\n  innerWires: Point2D[][]\n}\n\nexport type Unroller = (mesh: Mesh) => FlatMesh\n\nconst COT_MIN = 0.05\nconst COT_MAX = 20\n\nconst ITERATIONS = 4000\n\nconst SETTLED = 1e-6\n\ntype Adj = { j: number; w: number; t: number; dx: number; dy: number }\n\nexport function unrollArap(mesh: Mesh): FlatMesh {\n  const tris = buildTris(mesh)\n  const nv = mesh.positions.length\n  const flat = placeByUnfolding(mesh, tris)\n\n  if (tris.length > 0 && nv > 1) {\n    const { adj, triW } = buildAdjacency(tris, nv)\n    solve(flat, tris, adj, triW)\n  }\n\n  const positions = canonicalise(flat)\n  const edges = boundaryLoop(mesh).map((wire) => wire.map((i) => positions[i]))\n  return { positions, outline: edges[0], innerWires: edges.slice(1) }\n}\n\nfunction buildAdjacency(\n  tris: Tri[],\n  nv: number,\n): { adj: Adj[][]; triW: Float64Array } {\n  const adj: Adj[][] = []\n  const triW = new Float64Array(tris.length * 3)\n  for (let i = 0; i < nv; i++) adj.push([])\n\n  tris.forEach((tri, t) => {\n    const { ids, xs } = tri\n    const twoArea = 2 * tri.area\n    for (let e = 0; e < 3; e++) {\n      const p = e\n      const q = (e + 1) % 3\n      const o = (e + 2) % 3\n      const ax = xs[p].x - xs[o].x, ay = xs[p].y - xs[o].y\n      const bx = xs[q].x - xs[o].x, by = xs[q].y - xs[o].y\n\n      const cot = twoArea > 0 ? (ax * bx + ay * by) / twoArea : COT_MIN\n      const w = Math.min(COT_MAX, Math.max(COT_MIN, cot))\n      triW[t * 3 + e] = w\n      const dx = xs[p].x - xs[q].x\n      const dy = xs[p].y - xs[q].y\n      adj[ids[p]].push({ j: ids[q], w, t, dx, dy })\n      adj[ids[q]].push({ j: ids[p], w, t, dx: -dx, dy: -dy })\n    }\n  })\n  return { adj, triW }\n}\n\nfunction solve(flat: Point2D[], tris: Tri[], adj: Adj[][], triW: Float64Array) {\n  const nv = flat.length\n  const cos = new Float64Array(tris.length)\n  const sin = new Float64Array(tris.length)\n  const tol = SETTLED * intrinsicScale(tris)\n  const system = factorSystem(adj, nv)\n\n  const bestX = new Float64Array(nv)\n  const bestY = new Float64Array(nv)\n  let bestE = energyOf(flat, tris)\n  snapshot(flat, bestX, bestY)\n\n  const rx = new Float64Array(Math.max(nv - 1, 0))\n  const ry = new Float64Array(Math.max(nv - 1, 0))\n\n  for (let it = 0; it < ITERATIONS; it++) {\n    localStep(flat, tris, triW, cos, sin)\n\n    let maxDelta = 0\n    if (system) {\n      rx.fill(0)\n      ry.fill(0)\n      for (let i = 1; i < nv; i++) {\n        let bx = 0\n        let by = 0\n        for (const e of adj[i]) {\n          bx += e.w * (cos[e.t] * e.dx - sin[e.t] * e.dy)\n          by += e.w * (sin[e.t] * e.dx + cos[e.t] * e.dy)\n          if (e.j === 0) {\n            bx += e.w * flat[0].x\n            by += e.w * flat[0].y\n          }\n        }\n        rx[i - 1] = bx\n        ry[i - 1] = by\n      }\n      solveBanded(system, rx, ry)\n      for (let i = 1; i < nv; i++) {\n        const d = Math.hypot(rx[i - 1] - flat[i].x, ry[i - 1] - flat[i].y)\n        if (d > maxDelta) maxDelta = d\n        flat[i].x = rx[i - 1]\n        flat[i].y = ry[i - 1]\n      }\n    } else {\n      maxDelta = gaussSeidel(flat, adj, cos, sin, nv)\n    }\n\n    const e = energyOf(flat, tris)\n    if (e < bestE) {\n      bestE = e\n      snapshot(flat, bestX, bestY)\n    }\n    if (maxDelta < tol) break\n  }\n\n  restore(flat, bestX, bestY)\n}\n\nfunction snapshot(flat: Point2D[], x: Float64Array, y: Float64Array) {\n  for (let i = 0; i < flat.length; i++) {\n    if (flat[i] == undefined) {\n      continue;\n    }\n    x[i] = flat[i].x\n    y[i] = flat[i].y\n  }\n}\n\nfunction restore(flat: Point2D[], x: Float64Array, y: Float64Array) {\n  for (let i = 0; i < flat.length; i++) {\n    flat[i] = { x: x[i], y: y[i] }\n  }\n}\n\nfunction localStep(\n  flat: Point2D[],\n  tris: Tri[],\n  triW: Float64Array,\n  cos: Float64Array,\n  sin: Float64Array,\n) {\n  tris.forEach((tri, t) => {\n    const { ids, xs } = tri\n    let num = 0\n    let den = 0\n    for (let e = 0; e < 3; e++) {\n      const p = e\n      const q = (e + 1) % 3\n      const w = triW[t * 3 + e]\n      const dxc = xs[q].x - xs[p].x\n      const dyc = xs[q].y - xs[p].y\n      const dqx = flat[ids[q]].x - flat[ids[p]].x\n      const dqy = flat[ids[q]].y - flat[ids[p]].y\n      num += w * (dqy * dxc - dqx * dyc)\n      den += w * (dqx * dxc + dqy * dyc)\n    }\n    const th = Math.atan2(num, den)\n    cos[t] = Math.cos(th)\n    sin[t] = Math.sin(th)\n  })\n}\n\nfunction gaussSeidel(\n  flat: Point2D[],\n  adj: Adj[][],\n  cos: Float64Array,\n  sin: Float64Array,\n  nv: number,\n): number {\n  let maxDelta = 0\n  for (let sweep = 0; sweep < 3; sweep++) {\n    for (let i = 1; i < nv; i++) {\n      let diag = 0\n      let sx = 0\n      let sy = 0\n      for (const e of adj[i]) {\n        diag += e.w\n        sx += e.w * (flat[e.j].x + cos[e.t] * e.dx - sin[e.t] * e.dy)\n        sy += e.w * (flat[e.j].y + sin[e.t] * e.dx + cos[e.t] * e.dy)\n      }\n      if (diag <= 0) continue\n      const nx = sx / diag\n      const ny = sy / diag\n      const d = Math.hypot(nx - flat[i].x, ny - flat[i].y)\n      if (d > maxDelta) maxDelta = d\n      flat[i] = { x: nx, y: ny }\n    }\n  }\n  return maxDelta\n}\n\ntype Banded = { n: number; b: number; a: Float64Array; envelope: Int32Array }\n\nfunction factorSystem(adj: Adj[][], nv: number): Banded | null {\n  const n = nv - 1\n  if (n <= 0) return null\n\n  let b = 0\n  for (let i = 1; i < nv; i++) {\n    for (const e of adj[i]) {\n      if (e.j === 0) continue\n      const d = Math.abs(i - e.j)\n      if (d > b) b = d\n    }\n  }\n  if (b >= n) return null\n\n  const a = new Float64Array(n * (b + 1))\n  for (let i = 1; i < nv; i++) {\n    const r = i - 1\n    for (const e of adj[i]) {\n      a[r * (b + 1)] += e.w\n      if (e.j === 0) continue\n      const c = e.j - 1\n      if (c < r) a[r * (b + 1) + (r - c)] -= e.w\n    }\n  }\n\n  const envelope = new Int32Array(n)\n  for (let r = 0; r < n; r++) {\n    const top = Math.min(b, r)\n    for (let k = top; k >= 1; k--) {\n      if (a[r * (b + 1) + k] !== 0) { envelope[r] = k; break }\n    }\n  }\n\n  for (let i = 0; i < n; i++) {\n    const wi = envelope[i]\n    for (let k = wi; k >= 1; k--) {\n      const j = i - k\n      if (j < 0) continue\n      let s = a[i * (b + 1) + k]\n      const top = Math.min(wi - k, envelope[j])\n      for (let m = 1; m <= top; m++) {\n        s -= a[i * (b + 1) + (k + m)] * a[j * (b + 1) + m]\n      }\n      a[i * (b + 1) + k] = s / a[j * (b + 1)]\n    }\n    let d = a[i * (b + 1)]\n    for (let k = 1; k <= wi; k++) {\n      d -= a[i * (b + 1) + k] * a[i * (b + 1) + k]\n    }\n    if (!(d > 0)) return null\n    a[i * (b + 1)] = Math.sqrt(d)\n  }\n  return { n, b, a, envelope }\n}\n\nfunction solveBanded(m: Banded, rx: Float64Array, ry: Float64Array) {\n  const { n, b, a, envelope } = m\n  const w = b + 1\n  for (let i = 0; i < n; i++) {\n    let sx = rx[i]\n    let sy = ry[i]\n    const wi = envelope[i]\n    for (let k = 1; k <= wi; k++) {\n      const v = a[i * w + k]\n      sx -= v * rx[i - k]\n      sy -= v * ry[i - k]\n    }\n    const d = a[i * w]\n    rx[i] = sx / d\n    ry[i] = sy / d\n  }\n  for (let i = n - 1; i >= 0; i--) {\n    const d = a[i * w]\n    const vx = rx[i] / d\n    const vy = ry[i] / d\n    rx[i] = vx\n    ry[i] = vy\n    const wi = envelope[i]\n    for (let k = 1; k <= wi; k++) {\n      const v = a[i * w + k]\n      rx[i - k] -= v * vx\n      ry[i - k] -= v * vy\n    }\n  }\n}\n\nfunction intrinsicScale(tris: Tri[]): number {\n  let area = 0\n  for (const tri of tris) area += tri.area\n  return area > 0 ? Math.sqrt(area) : 1\n}\n\nexport type Tri = {\n  ids: Tri3\n  xs: [Point2D, Point2D, Point2D]\n  area: number\n}\n\nexport function buildTris(mesh: Mesh): Tri[] {\n  const out: Tri[] = []\n  for (const [a, b, c] of mesh.tris) {\n    const A = mesh.positions[a]\n    const B = mesh.positions[b]\n    const C = mesh.positions[c]\n    const ux = B.x - A.x, uy = B.y - A.y, uz = B.z - A.z\n    const vx = C.x - A.x, vy = C.y - A.y, vz = C.z - A.z\n    const cx = uy * vz - uz * vy\n    const cy = uz * vx - ux * vz\n    const cz = ux * vy - uy * vx\n    const cross = Math.hypot(cx, cy, cz)\n    const la = Math.hypot(ux, uy, uz)\n    const lb = Math.hypot(vx, vy, vz)\n    if (la === 0 || lb === 0) continue\n    const cos = Math.max(-1, Math.min(1, (ux * vx + uy * vy + uz * vz) / (la * lb)))\n    out.push({\n      ids: [a, b, c],\n      xs: [\n        { x: 0, y: 0 },\n        { x: la, y: 0 },\n        { x: lb * cos, y: lb * Math.sqrt(Math.max(0, 1 - cos * cos)) },\n      ],\n      area: cross / 2,\n    })\n  }\n  return out\n}\n\nexport function energyOf(flat: Point2D[], tris: Tri[]): number {\n  let e = 0\n  let area = 0\n  for (const tri of tris) {\n    const { ids, xs } = tri\n    for (let k = 0; k < 3; k++) {\n      const p = k\n      const q = (k + 1) % 3\n      const lc = Math.hypot(xs[q].x - xs[p].x, xs[q].y - xs[p].y)\n      const lq = Math.hypot(\n        flat[ids[q]].x - flat[ids[p]].x,\n        flat[ids[q]].y - flat[ids[p]].y,\n      )\n      e += (lq - lc) * (lq - lc)\n    }\n    area += tri.area\n  }\n  return area > 0 ? e / area : 0\n}\n\nexport function placeByUnfolding(mesh: Mesh, tris: Tri[]): Point2D[] {\n  const nv = mesh.positions.length\n  const flat: Point2D[] = new Array(nv)\n  const placed: boolean[] = new Array(nv).fill(false)\n\n  const incident: number[][] = []\n  for (let i = 0; i < nv; i++) incident.push([])\n  tris.forEach((tri, t) => {\n    for (const id of tri.ids) incident[id].push(t)\n  })\n\n  const seed = tris[0]\n  seed.ids.forEach((id, k) => {\n    flat[id] = { x: seed.xs[k].x, y: seed.xs[k].y }\n    placed[id] = true\n  })\n\n  const queue: number[] = seed.ids.flatMap((id) => incident[id])\n  for (let head = 0; head < queue.length; head++) {\n    const tri = tris[queue[head]]\n    const open = tri.ids.filter((i) => !placed[i])\n    if (open.length !== 1) continue\n    const v = open[0]\n    const p = placeFrom(bestHinge(v, incident[v], tris, placed, tri), v, flat, placed)\n    if (!p) continue\n    flat[v] = p\n    placed[v] = true\n    for (const t of incident[v]) queue.push(t)\n  }\n\n  for (let i = 0; i < nv; i++) {\n    if (!placed[i]) console.warn(`unroll: vertex ${i} of ${nv} is not connected to the mesh`)\n  }\n  return flat\n}\n\nfunction bestHinge(\n  v: number,\n  candidates: number[],\n  tris: Tri[],\n  placed: boolean[],\n  fallback: Tri,\n): Tri {\n  let best = fallback\n  let bestScore = -Infinity\n  for (const t of candidates) {\n    const tri = tris[t]\n    const k = tri.ids.indexOf(v)\n    if (k < 0) continue\n    const i = (k + 1) % 3\n    const j = (k + 2) % 3\n    if (!placed[tri.ids[i]] || !placed[tri.ids[j]]) continue\n    const edge = sq(tri.xs[j].x - tri.xs[i].x, tri.xs[j].y - tri.xs[i].y)\n    const reach = sq(tri.xs[k].x - tri.xs[i].x, tri.xs[k].y - tri.xs[i].y)\n    const score = reach > 0 ? edge / reach : Infinity\n    if (score > bestScore) {\n      bestScore = score\n      best = tri\n    }\n  }\n  return best\n}\n\nfunction sq(dx: number, dy: number): number {\n  return dx * dx + dy * dy\n}\n\nfunction placeFrom(\n  tri: Tri,\n  v: number,\n  flat: Point2D[],\n  placed: boolean[],\n): Point2D | null {\n  const { ids, xs } = tri\n  const k = ids.indexOf(v)\n  const i = (k + 1) % 3\n  const j = (k + 2) % 3\n  if (!placed[ids[i]] || !placed[ids[j]]) return null\n\n  const dx = xs[j].x - xs[i].x\n  const dy = xs[j].y - xs[i].y\n  const den = dx * dx + dy * dy\n  if (den < 1e-12) return null\n  const ex = flat[ids[j]].x - flat[ids[i]].x\n  const ey = flat[ids[j]].y - flat[ids[i]].y\n  const sr = (ex * dx + ey * dy) / den\n  const si = (ey * dx - ex * dy) / den\n  const norm = Math.hypot(sr, si)\n  if (norm < 1e-12) return null\n  const cr = sr / norm\n  const ci = si / norm\n  const vx = xs[k].x - xs[i].x\n  const vy = xs[k].y - xs[i].y\n  return {\n    x: flat[ids[i]].x + cr * vx - ci * vy,\n    y: flat[ids[i]].y + ci * vx + cr * vy,\n  }\n}\n\nexport function canonicalise(flat: Point2D[]): Point2D[] {\n  if (flat.length === 0) return flat\n  let cx = 0\n  let cy = 0\n  for (const p of flat) {\n    cx += p.x\n    cy += p.y\n  }\n  cx /= flat.length\n  cy /= flat.length\n\n  let sxx = 0\n  let sxy = 0\n  let syy = 0\n  for (const p of flat) {\n    const dx = p.x - cx\n    const dy = p.y - cy\n    sxx += dx * dx\n    sxy += dx * dy\n    syy += dy * dy\n  }\n  const ang = 0.5 * Math.atan2(2 * sxy, sxx - syy)\n  const co = Math.cos(-ang)\n  const si = Math.sin(-ang)\n\n  let minX = Infinity\n  let minY = Infinity\n  const out = flat.map((p) => {\n    const dx = p.x - cx\n    const dy = p.y - cy\n    const q = { x: co * dx - si * dy, y: si * dx + co * dy }\n    if (q.x < minX) minX = q.x\n    if (q.y < minY) minY = q.y\n    return q\n  })\n  for (const q of out) {\n    q.x -= minX\n    q.y -= minY\n  }\n  return out\n}\n\nexport function boundaryEdges(mesh: Mesh): [number, number][] {\n  const used = new Map<string, number>()\n  const key = (a: number, b: number) => (a < b ? `${a}-${b}` : `${b}-${a}`)\n\n  for (const [a, b, c] of mesh.tris) {\n    for (const [u, v] of [[a, b], [b, c], [c, a]]) {\n      used.set(key(u, v), (used.get(key(u, v)) ?? 0) + 1)\n    }\n  }\n\n  const out: [number, number][] = []\n  for (const [a, b, c] of mesh.tris) {\n    for (const [u, v] of [[a, b], [b, c], [c, a]] as [number, number][]) {\n      if (used.get(key(u, v)) === 1) out.push([u, v])\n    }\n  }\n  return out\n}\n\nexport function chainEdges(edges: [number, number][]): number[][] {\n  const next = new Map<number, number>()\n  const hasPredecessor = new Set<number>()\n  for (const [u, v] of edges) {\n    next.set(u, v)\n    hasPredecessor.add(v)\n  }\n\n  const chains: number[][] = []\n  const visited = new Set<number>()\n\n  const walk = (seed: number) => {\n    const chain = [seed]\n    visited.add(seed)\n    let cur = seed\n    while (true) {\n      const to = next.get(cur)\n      if (to === undefined || to === seed) break\n      if (visited.has(to)) break\n      chain.push(to)\n      visited.add(to)\n      cur = to\n    }\n\n    if (next.get(cur) === seed && chain.length > 2) chain.push(seed)\n    if (chain.length > 1) chains.push(chain)\n  }\n\n  for (const [u] of next) if (!hasPredecessor.has(u)) walk(u)\n  for (const [u] of next) if (!visited.has(u)) walk(u)\n  return chains\n}\n\nexport function boundaryLoop(mesh: Mesh): number[][] {\n  const chains = chainEdges(boundaryEdges(mesh))\n  return chains;\n  console.log(\"chain count: \" + chains.length)\n  if (chains.length === 0) return []\n  const loop = chains[0]\n  return loop[0] === loop[loop.length - 1] ? loop.slice(0, -1) : loop\n}\n\n// Distortion measurements\nexport type Distortion = {\n  edgeRms: number\n  edgeMax: number\n  areaRatio: number\n  triStretch: number[]\n}\n\nexport function measureDistortion(mesh: Mesh, flat: FlatMesh): Distortion {\n  let squared = 0\n  let count = 0\n  let edgeMax = 0\n  let area3d = 0\n  let area2d = 0\n  const triStretch: number[] = []\n\n  for (const [a, b, c] of mesh.tris) {\n    const local = flattenTriangle(mesh, a, b, c)\n    for (const [u, v] of [[a, b], [b, c], [c, a]]) {\n      const length3d = distance3d(mesh, u, v)\n      if (length3d === 0) continue\n      const p = flat.positions[u]\n      const q = flat.positions[v]\n      const ratio = Math.hypot(q.x - p.x, q.y - p.y) / length3d - 1\n      squared += ratio * ratio\n      count += 1\n      edgeMax = Math.max(edgeMax, Math.abs(ratio))\n    }\n\n    area3d += local.area\n    area2d += Math.abs(signedArea(flat.positions[a], flat.positions[b], flat.positions[c]))\n    triStretch.push(stretchOf(local, flat, a, b, c))\n  }\n\n  return {\n    edgeRms: count > 0 ? Math.sqrt(squared / count) : 0,\n    edgeMax,\n    areaRatio: area3d > 0 ? area2d / area3d : 1,\n    triStretch,\n  }\n}\n\ntype LocalTriangle = { ux: number; vx: number; vy: number; area: number }\n\nfunction flattenTriangle(mesh: Mesh, a: number, b: number, c: number): LocalTriangle {\n  const A = mesh.positions[a]\n  const B = mesh.positions[b]\n  const C = mesh.positions[c]\n  const ex = B.x - A.x, ey = B.y - A.y, ez = B.z - A.z\n  const fx = C.x - A.x, fy = C.y - A.y, fz = C.z - A.z\n  const ux = Math.hypot(ex, ey, ez)\n  const dot = ux > 0 ? (ex * fx + ey * fy + ez * fz) / ux : 0\n  const nx = ey * fz - ez * fy\n  const ny = ez * fx - ex * fz\n  const nz = ex * fy - ey * fx\n  const cross = Math.hypot(nx, ny, nz)\n  return { ux, vx: dot, vy: ux > 0 ? cross / ux : 0, area: cross / 2 }\n}\n\nfunction stretchOf(\n  local: LocalTriangle,\n  flat: FlatMesh,\n  a: number,\n  b: number,\n  c: number,\n): number {\n  const det = local.ux * local.vy\n  if (det === 0) return 1\n\n  const A = flat.positions[a]\n  const B = flat.positions[b]\n  const C = flat.positions[c]\n  const qx1 = B.x - A.x, qy1 = B.y - A.y\n  const qx2 = C.x - A.x, qy2 = C.y - A.y\n\n  const i00 = local.vy / det\n  const i01 = -local.vx / det\n  const i11 = local.ux / det\n\n  const j00 = qx1 * i00\n  const j01 = qx1 * i01 + qx2 * i11\n  const j10 = qy1 * i00\n  const j11 = qy1 * i01 + qy2 * i11\n\n  const e = (j00 + j11) / 2\n  const f = (j00 - j11) / 2\n  const g = (j10 + j01) / 2\n  const h = (j10 - j01) / 2\n  const q = Math.hypot(e, h)\n  const r = Math.hypot(f, g)\n  const sigma1 = q + r\n  const sigma2 = Math.abs(q - r)\n  return Math.max(sigma1, sigma2 > 0 ? 1 / sigma2 : Infinity)\n}\n\nfunction signedArea(\n  a: { x: number; y: number },\n  b: { x: number; y: number },\n  c: { x: number; y: number },\n): number {\n  return ((b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x)) / 2\n}\n\nfunction distance3d(mesh: Mesh, u: number, v: number): number {\n  const p = mesh.positions[u]\n  const q = mesh.positions[v]\n  return Math.hypot(q.x - p.x, q.y - p.y, q.z - p.z)\n}\n\n",
          "compiledCode": "// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(surface, tolerance = 0.01, asSingular = false) {\n    const toUnroll = [];\n    surface.onLeafs((leaf) => {\n        const sface = leaf.getSelectedFaces().filter((f) => f != undefined);\n        if (sface.length > 0) {\n            toUnroll.push(...sface.map((f) => { return { srcLeaf: leaf, mesh: faceToMesh(f, tolerance) }; }));\n        }\n        else if (leaf.geometry instanceof replicad.Face) {\n            toUnroll.push({ srcLeaf: leaf, mesh: faceToMesh(leaf.geometry, tolerance) });\n        }\n        return leaf;\n    });\n    if (toUnroll.length == 0) {\n        throw new Error(\"No selected faces, and not an assembly of face geoms\");\n    }\n    if (asSingular) {\n        // combine meshes first, then unroll\n        const foo = toUnroll.reduce((accumulator, curr) => {\n            return combineMeshs(accumulator, curr.mesh);\n        }, { positions: [], tris: [] });\n        const unrolled = unrollArap(foo);\n        const distortion = measureDistortion(foo, unrolled);\n        console.log(distortion.triStretch.length);\n        console.log(foo.tris.length);\n        console.log(`pointref range. input mesh: ${foo.positions.length} result mesh: ${unrolled.positions.length}`);\n        return new Assembly({\n            geometry: unrolledWithHoles(unrolled)\n        });\n    }\n    else {\n        let ybuffer = 0;\n        const result = toUnroll.map((leaf) => {\n            const unrollResult = unrollArap(leaf.mesh);\n            const drawing = unrolledWithHoles(unrollResult).translate(0, ybuffer);\n            ybuffer += drawing.boundingBox.height * 1.1;\n            return new Assembly({\n                ...leaf.srcLeaf,\n                geometry: drawing\n            });\n        });\n        return new Assembly({ geometry: result.flat() }).skipDisjointPostprocess();\n    }\n}\nfunction unrolledWithHoles(unrolResult) {\n    let res = xysToClosedDrawing(unrolResult.outline);\n    unrolResult.innerWires.forEach((inner) => {\n        res = res.cut(xysToClosedDrawing(inner));\n    });\n    return res;\n}\nfunction combineMeshs(m1, m2) {\n    console.log(`m1, position count: ${m1.positions.length} tris count: ${m1.tris.length}`);\n    console.log(`m2, position count: ${m2.positions.length} tris count: ${m2.tris.length}`);\n    const floatEq = (a, b) => {\n        return Math.abs(a - b) < 0.000001;\n    };\n    // find match points TODO this is deeply suboptimal. Esp if combineMeshs is called more than once.\n    const indexTuples = [];\n    m1.positions.forEach((point, index) => {\n        m2.positions.forEach((p2, i2) => {\n            if (floatEq(point.x, p2.x) && floatEq(point.y, p2.y) && floatEq(point.z, p2.z)) {\n                console.log('found a match');\n                indexTuples.push({ m1: index, m2: i2 });\n            }\n        });\n    });\n    console.log(indexTuples);\n    const shiftTris = m2.tris.map((tri) => {\n        return tri.map((pointIndex) => pointIndex += m1.positions.length);\n    });\n    // replace equivalent points from meshes\n    const dedupTris = shiftTris.map((tri) => {\n        return tri.map((pointIndex) => {\n            const unshift = pointIndex - m1.positions.length;\n            const i = indexTuples.map((f) => f.m2).indexOf(unshift);\n            if (i >= 0) {\n                return indexTuples[i].m1;\n            }\n            return pointIndex;\n        });\n    });\n    const res = {\n        positions: m1.positions.concat(m2.positions),\n        tris: m1.tris.concat(dedupTris)\n    };\n    console.log(`res, position count: ${res.positions.length} tris count: ${res.tris.length}`);\n    return res;\n}\nfunction faceToMesh(face, tolerance) {\n    const replMesh = face.mesh({ tolerance: tolerance, angularTolerance: 0.1 });\n    // translate\n    const points = [];\n    const v = replMesh.vertices;\n    for (let i = 0; i < v.length; i += 3) {\n        points.push({ x: v[i], y: v[i + 1], z: v[i + 2] });\n    }\n    const tris = [];\n    const t = replMesh.triangles;\n    for (let i = 0; i < t.length; i += 3) {\n        tris.push([t[i], t[i + 1], t[i + 2]]);\n    }\n    return { positions: points, tris: tris };\n}\nfunction xysToClosedDrawing(points) {\n    const pen = replicad.draw([points[0].x, points[0].y]);\n    for (let i = 1; i < points.length; i++) {\n        pen.lineTo([points[i].x, points[i].y]);\n    }\n    return pen.close();\n}\nconst COT_MIN = 0.05;\nconst COT_MAX = 20;\nconst ITERATIONS = 4000;\nconst SETTLED = 1e-6;\nexport function unrollArap(mesh) {\n    const tris = buildTris(mesh);\n    const nv = mesh.positions.length;\n    const flat = placeByUnfolding(mesh, tris);\n    if (tris.length > 0 && nv > 1) {\n        const { adj, triW } = buildAdjacency(tris, nv);\n        solve(flat, tris, adj, triW);\n    }\n    const positions = canonicalise(flat);\n    const edges = boundaryLoop(mesh).map((wire) => wire.map((i) => positions[i]));\n    return { positions, outline: edges[0], innerWires: edges.slice(1) };\n}\nfunction buildAdjacency(tris, nv) {\n    const adj = [];\n    const triW = new Float64Array(tris.length * 3);\n    for (let i = 0; i < nv; i++)\n        adj.push([]);\n    tris.forEach((tri, t) => {\n        const { ids, xs } = tri;\n        const twoArea = 2 * tri.area;\n        for (let e = 0; e < 3; e++) {\n            const p = e;\n            const q = (e + 1) % 3;\n            const o = (e + 2) % 3;\n            const ax = xs[p].x - xs[o].x, ay = xs[p].y - xs[o].y;\n            const bx = xs[q].x - xs[o].x, by = xs[q].y - xs[o].y;\n            const cot = twoArea > 0 ? (ax * bx + ay * by) / twoArea : COT_MIN;\n            const w = Math.min(COT_MAX, Math.max(COT_MIN, cot));\n            triW[t * 3 + e] = w;\n            const dx = xs[p].x - xs[q].x;\n            const dy = xs[p].y - xs[q].y;\n            adj[ids[p]].push({ j: ids[q], w, t, dx, dy });\n            adj[ids[q]].push({ j: ids[p], w, t, dx: -dx, dy: -dy });\n        }\n    });\n    return { adj, triW };\n}\nfunction solve(flat, tris, adj, triW) {\n    const nv = flat.length;\n    const cos = new Float64Array(tris.length);\n    const sin = new Float64Array(tris.length);\n    const tol = SETTLED * intrinsicScale(tris);\n    const system = factorSystem(adj, nv);\n    const bestX = new Float64Array(nv);\n    const bestY = new Float64Array(nv);\n    let bestE = energyOf(flat, tris);\n    snapshot(flat, bestX, bestY);\n    const rx = new Float64Array(Math.max(nv - 1, 0));\n    const ry = new Float64Array(Math.max(nv - 1, 0));\n    for (let it = 0; it < ITERATIONS; it++) {\n        localStep(flat, tris, triW, cos, sin);\n        let maxDelta = 0;\n        if (system) {\n            rx.fill(0);\n            ry.fill(0);\n            for (let i = 1; i < nv; i++) {\n                let bx = 0;\n                let by = 0;\n                for (const e of adj[i]) {\n                    bx += e.w * (cos[e.t] * e.dx - sin[e.t] * e.dy);\n                    by += e.w * (sin[e.t] * e.dx + cos[e.t] * e.dy);\n                    if (e.j === 0) {\n                        bx += e.w * flat[0].x;\n                        by += e.w * flat[0].y;\n                    }\n                }\n                rx[i - 1] = bx;\n                ry[i - 1] = by;\n            }\n            solveBanded(system, rx, ry);\n            for (let i = 1; i < nv; i++) {\n                const d = Math.hypot(rx[i - 1] - flat[i].x, ry[i - 1] - flat[i].y);\n                if (d > maxDelta)\n                    maxDelta = d;\n                flat[i].x = rx[i - 1];\n                flat[i].y = ry[i - 1];\n            }\n        }\n        else {\n            maxDelta = gaussSeidel(flat, adj, cos, sin, nv);\n        }\n        const e = energyOf(flat, tris);\n        if (e < bestE) {\n            bestE = e;\n            snapshot(flat, bestX, bestY);\n        }\n        if (maxDelta < tol)\n            break;\n    }\n    restore(flat, bestX, bestY);\n}\nfunction snapshot(flat, x, y) {\n    for (let i = 0; i < flat.length; i++) {\n        if (flat[i] == undefined) {\n            continue;\n        }\n        x[i] = flat[i].x;\n        y[i] = flat[i].y;\n    }\n}\nfunction restore(flat, x, y) {\n    for (let i = 0; i < flat.length; i++) {\n        flat[i] = { x: x[i], y: y[i] };\n    }\n}\nfunction localStep(flat, tris, triW, cos, sin) {\n    tris.forEach((tri, t) => {\n        const { ids, xs } = tri;\n        let num = 0;\n        let den = 0;\n        for (let e = 0; e < 3; e++) {\n            const p = e;\n            const q = (e + 1) % 3;\n            const w = triW[t * 3 + e];\n            const dxc = xs[q].x - xs[p].x;\n            const dyc = xs[q].y - xs[p].y;\n            const dqx = flat[ids[q]].x - flat[ids[p]].x;\n            const dqy = flat[ids[q]].y - flat[ids[p]].y;\n            num += w * (dqy * dxc - dqx * dyc);\n            den += w * (dqx * dxc + dqy * dyc);\n        }\n        const th = Math.atan2(num, den);\n        cos[t] = Math.cos(th);\n        sin[t] = Math.sin(th);\n    });\n}\nfunction gaussSeidel(flat, adj, cos, sin, nv) {\n    let maxDelta = 0;\n    for (let sweep = 0; sweep < 3; sweep++) {\n        for (let i = 1; i < nv; i++) {\n            let diag = 0;\n            let sx = 0;\n            let sy = 0;\n            for (const e of adj[i]) {\n                diag += e.w;\n                sx += e.w * (flat[e.j].x + cos[e.t] * e.dx - sin[e.t] * e.dy);\n                sy += e.w * (flat[e.j].y + sin[e.t] * e.dx + cos[e.t] * e.dy);\n            }\n            if (diag <= 0)\n                continue;\n            const nx = sx / diag;\n            const ny = sy / diag;\n            const d = Math.hypot(nx - flat[i].x, ny - flat[i].y);\n            if (d > maxDelta)\n                maxDelta = d;\n            flat[i] = { x: nx, y: ny };\n        }\n    }\n    return maxDelta;\n}\nfunction factorSystem(adj, nv) {\n    const n = nv - 1;\n    if (n <= 0)\n        return null;\n    let b = 0;\n    for (let i = 1; i < nv; i++) {\n        for (const e of adj[i]) {\n            if (e.j === 0)\n                continue;\n            const d = Math.abs(i - e.j);\n            if (d > b)\n                b = d;\n        }\n    }\n    if (b >= n)\n        return null;\n    const a = new Float64Array(n * (b + 1));\n    for (let i = 1; i < nv; i++) {\n        const r = i - 1;\n        for (const e of adj[i]) {\n            a[r * (b + 1)] += e.w;\n            if (e.j === 0)\n                continue;\n            const c = e.j - 1;\n            if (c < r)\n                a[r * (b + 1) + (r - c)] -= e.w;\n        }\n    }\n    const envelope = new Int32Array(n);\n    for (let r = 0; r < n; r++) {\n        const top = Math.min(b, r);\n        for (let k = top; k >= 1; k--) {\n            if (a[r * (b + 1) + k] !== 0) {\n                envelope[r] = k;\n                break;\n            }\n        }\n    }\n    for (let i = 0; i < n; i++) {\n        const wi = envelope[i];\n        for (let k = wi; k >= 1; k--) {\n            const j = i - k;\n            if (j < 0)\n                continue;\n            let s = a[i * (b + 1) + k];\n            const top = Math.min(wi - k, envelope[j]);\n            for (let m = 1; m <= top; m++) {\n                s -= a[i * (b + 1) + (k + m)] * a[j * (b + 1) + m];\n            }\n            a[i * (b + 1) + k] = s / a[j * (b + 1)];\n        }\n        let d = a[i * (b + 1)];\n        for (let k = 1; k <= wi; k++) {\n            d -= a[i * (b + 1) + k] * a[i * (b + 1) + k];\n        }\n        if (!(d > 0))\n            return null;\n        a[i * (b + 1)] = Math.sqrt(d);\n    }\n    return { n, b, a, envelope };\n}\nfunction solveBanded(m, rx, ry) {\n    const { n, b, a, envelope } = m;\n    const w = b + 1;\n    for (let i = 0; i < n; i++) {\n        let sx = rx[i];\n        let sy = ry[i];\n        const wi = envelope[i];\n        for (let k = 1; k <= wi; k++) {\n            const v = a[i * w + k];\n            sx -= v * rx[i - k];\n            sy -= v * ry[i - k];\n        }\n        const d = a[i * w];\n        rx[i] = sx / d;\n        ry[i] = sy / d;\n    }\n    for (let i = n - 1; i >= 0; i--) {\n        const d = a[i * w];\n        const vx = rx[i] / d;\n        const vy = ry[i] / d;\n        rx[i] = vx;\n        ry[i] = vy;\n        const wi = envelope[i];\n        for (let k = 1; k <= wi; k++) {\n            const v = a[i * w + k];\n            rx[i - k] -= v * vx;\n            ry[i - k] -= v * vy;\n        }\n    }\n}\nfunction intrinsicScale(tris) {\n    let area = 0;\n    for (const tri of tris)\n        area += tri.area;\n    return area > 0 ? Math.sqrt(area) : 1;\n}\nexport function buildTris(mesh) {\n    const out = [];\n    for (const [a, b, c] of mesh.tris) {\n        const A = mesh.positions[a];\n        const B = mesh.positions[b];\n        const C = mesh.positions[c];\n        const ux = B.x - A.x, uy = B.y - A.y, uz = B.z - A.z;\n        const vx = C.x - A.x, vy = C.y - A.y, vz = C.z - A.z;\n        const cx = uy * vz - uz * vy;\n        const cy = uz * vx - ux * vz;\n        const cz = ux * vy - uy * vx;\n        const cross = Math.hypot(cx, cy, cz);\n        const la = Math.hypot(ux, uy, uz);\n        const lb = Math.hypot(vx, vy, vz);\n        if (la === 0 || lb === 0)\n            continue;\n        const cos = Math.max(-1, Math.min(1, (ux * vx + uy * vy + uz * vz) / (la * lb)));\n        out.push({\n            ids: [a, b, c],\n            xs: [\n                { x: 0, y: 0 },\n                { x: la, y: 0 },\n                { x: lb * cos, y: lb * Math.sqrt(Math.max(0, 1 - cos * cos)) },\n            ],\n            area: cross / 2,\n        });\n    }\n    return out;\n}\nexport function energyOf(flat, tris) {\n    let e = 0;\n    let area = 0;\n    for (const tri of tris) {\n        const { ids, xs } = tri;\n        for (let k = 0; k < 3; k++) {\n            const p = k;\n            const q = (k + 1) % 3;\n            const lc = Math.hypot(xs[q].x - xs[p].x, xs[q].y - xs[p].y);\n            const lq = Math.hypot(flat[ids[q]].x - flat[ids[p]].x, flat[ids[q]].y - flat[ids[p]].y);\n            e += (lq - lc) * (lq - lc);\n        }\n        area += tri.area;\n    }\n    return area > 0 ? e / area : 0;\n}\nexport function placeByUnfolding(mesh, tris) {\n    const nv = mesh.positions.length;\n    const flat = new Array(nv);\n    const placed = new Array(nv).fill(false);\n    const incident = [];\n    for (let i = 0; i < nv; i++)\n        incident.push([]);\n    tris.forEach((tri, t) => {\n        for (const id of tri.ids)\n            incident[id].push(t);\n    });\n    const seed = tris[0];\n    seed.ids.forEach((id, k) => {\n        flat[id] = { x: seed.xs[k].x, y: seed.xs[k].y };\n        placed[id] = true;\n    });\n    const queue = seed.ids.flatMap((id) => incident[id]);\n    for (let head = 0; head < queue.length; head++) {\n        const tri = tris[queue[head]];\n        const open = tri.ids.filter((i) => !placed[i]);\n        if (open.length !== 1)\n            continue;\n        const v = open[0];\n        const p = placeFrom(bestHinge(v, incident[v], tris, placed, tri), v, flat, placed);\n        if (!p)\n            continue;\n        flat[v] = p;\n        placed[v] = true;\n        for (const t of incident[v])\n            queue.push(t);\n    }\n    for (let i = 0; i < nv; i++) {\n        if (!placed[i])\n            console.warn(`unroll: vertex ${i} of ${nv} is not connected to the mesh`);\n    }\n    return flat;\n}\nfunction bestHinge(v, candidates, tris, placed, fallback) {\n    let best = fallback;\n    let bestScore = -Infinity;\n    for (const t of candidates) {\n        const tri = tris[t];\n        const k = tri.ids.indexOf(v);\n        if (k < 0)\n            continue;\n        const i = (k + 1) % 3;\n        const j = (k + 2) % 3;\n        if (!placed[tri.ids[i]] || !placed[tri.ids[j]])\n            continue;\n        const edge = sq(tri.xs[j].x - tri.xs[i].x, tri.xs[j].y - tri.xs[i].y);\n        const reach = sq(tri.xs[k].x - tri.xs[i].x, tri.xs[k].y - tri.xs[i].y);\n        const score = reach > 0 ? edge / reach : Infinity;\n        if (score > bestScore) {\n            bestScore = score;\n            best = tri;\n        }\n    }\n    return best;\n}\nfunction sq(dx, dy) {\n    return dx * dx + dy * dy;\n}\nfunction placeFrom(tri, v, flat, placed) {\n    const { ids, xs } = tri;\n    const k = ids.indexOf(v);\n    const i = (k + 1) % 3;\n    const j = (k + 2) % 3;\n    if (!placed[ids[i]] || !placed[ids[j]])\n        return null;\n    const dx = xs[j].x - xs[i].x;\n    const dy = xs[j].y - xs[i].y;\n    const den = dx * dx + dy * dy;\n    if (den < 1e-12)\n        return null;\n    const ex = flat[ids[j]].x - flat[ids[i]].x;\n    const ey = flat[ids[j]].y - flat[ids[i]].y;\n    const sr = (ex * dx + ey * dy) / den;\n    const si = (ey * dx - ex * dy) / den;\n    const norm = Math.hypot(sr, si);\n    if (norm < 1e-12)\n        return null;\n    const cr = sr / norm;\n    const ci = si / norm;\n    const vx = xs[k].x - xs[i].x;\n    const vy = xs[k].y - xs[i].y;\n    return {\n        x: flat[ids[i]].x + cr * vx - ci * vy,\n        y: flat[ids[i]].y + ci * vx + cr * vy,\n    };\n}\nexport function canonicalise(flat) {\n    if (flat.length === 0)\n        return flat;\n    let cx = 0;\n    let cy = 0;\n    for (const p of flat) {\n        cx += p.x;\n        cy += p.y;\n    }\n    cx /= flat.length;\n    cy /= flat.length;\n    let sxx = 0;\n    let sxy = 0;\n    let syy = 0;\n    for (const p of flat) {\n        const dx = p.x - cx;\n        const dy = p.y - cy;\n        sxx += dx * dx;\n        sxy += dx * dy;\n        syy += dy * dy;\n    }\n    const ang = 0.5 * Math.atan2(2 * sxy, sxx - syy);\n    const co = Math.cos(-ang);\n    const si = Math.sin(-ang);\n    let minX = Infinity;\n    let minY = Infinity;\n    const out = flat.map((p) => {\n        const dx = p.x - cx;\n        const dy = p.y - cy;\n        const q = { x: co * dx - si * dy, y: si * dx + co * dy };\n        if (q.x < minX)\n            minX = q.x;\n        if (q.y < minY)\n            minY = q.y;\n        return q;\n    });\n    for (const q of out) {\n        q.x -= minX;\n        q.y -= minY;\n    }\n    return out;\n}\nexport function boundaryEdges(mesh) {\n    const used = new Map();\n    const key = (a, b) => (a < b ? `${a}-${b}` : `${b}-${a}`);\n    for (const [a, b, c] of mesh.tris) {\n        for (const [u, v] of [[a, b], [b, c], [c, a]]) {\n            used.set(key(u, v), (used.get(key(u, v)) ?? 0) + 1);\n        }\n    }\n    const out = [];\n    for (const [a, b, c] of mesh.tris) {\n        for (const [u, v] of [[a, b], [b, c], [c, a]]) {\n            if (used.get(key(u, v)) === 1)\n                out.push([u, v]);\n        }\n    }\n    return out;\n}\nexport function chainEdges(edges) {\n    const next = new Map();\n    const hasPredecessor = new Set();\n    for (const [u, v] of edges) {\n        next.set(u, v);\n        hasPredecessor.add(v);\n    }\n    const chains = [];\n    const visited = new Set();\n    const walk = (seed) => {\n        const chain = [seed];\n        visited.add(seed);\n        let cur = seed;\n        while (true) {\n            const to = next.get(cur);\n            if (to === undefined || to === seed)\n                break;\n            if (visited.has(to))\n                break;\n            chain.push(to);\n            visited.add(to);\n            cur = to;\n        }\n        if (next.get(cur) === seed && chain.length > 2)\n            chain.push(seed);\n        if (chain.length > 1)\n            chains.push(chain);\n    };\n    for (const [u] of next)\n        if (!hasPredecessor.has(u))\n            walk(u);\n    for (const [u] of next)\n        if (!visited.has(u))\n            walk(u);\n    return chains;\n}\nexport function boundaryLoop(mesh) {\n    const chains = chainEdges(boundaryEdges(mesh));\n    return chains;\n    console.log(\"chain count: \" + chains.length);\n    if (chains.length === 0)\n        return [];\n    const loop = chains[0];\n    return loop[0] === loop[loop.length - 1] ? loop.slice(0, -1) : loop;\n}\nexport function measureDistortion(mesh, flat) {\n    let squared = 0;\n    let count = 0;\n    let edgeMax = 0;\n    let area3d = 0;\n    let area2d = 0;\n    const triStretch = [];\n    for (const [a, b, c] of mesh.tris) {\n        const local = flattenTriangle(mesh, a, b, c);\n        for (const [u, v] of [[a, b], [b, c], [c, a]]) {\n            const length3d = distance3d(mesh, u, v);\n            if (length3d === 0)\n                continue;\n            const p = flat.positions[u];\n            const q = flat.positions[v];\n            const ratio = Math.hypot(q.x - p.x, q.y - p.y) / length3d - 1;\n            squared += ratio * ratio;\n            count += 1;\n            edgeMax = Math.max(edgeMax, Math.abs(ratio));\n        }\n        area3d += local.area;\n        area2d += Math.abs(signedArea(flat.positions[a], flat.positions[b], flat.positions[c]));\n        triStretch.push(stretchOf(local, flat, a, b, c));\n    }\n    return {\n        edgeRms: count > 0 ? Math.sqrt(squared / count) : 0,\n        edgeMax,\n        areaRatio: area3d > 0 ? area2d / area3d : 1,\n        triStretch,\n    };\n}\nfunction flattenTriangle(mesh, a, b, c) {\n    const A = mesh.positions[a];\n    const B = mesh.positions[b];\n    const C = mesh.positions[c];\n    const ex = B.x - A.x, ey = B.y - A.y, ez = B.z - A.z;\n    const fx = C.x - A.x, fy = C.y - A.y, fz = C.z - A.z;\n    const ux = Math.hypot(ex, ey, ez);\n    const dot = ux > 0 ? (ex * fx + ey * fy + ez * fz) / ux : 0;\n    const nx = ey * fz - ez * fy;\n    const ny = ez * fx - ex * fz;\n    const nz = ex * fy - ey * fx;\n    const cross = Math.hypot(nx, ny, nz);\n    return { ux, vx: dot, vy: ux > 0 ? cross / ux : 0, area: cross / 2 };\n}\nfunction stretchOf(local, flat, a, b, c) {\n    const det = local.ux * local.vy;\n    if (det === 0)\n        return 1;\n    const A = flat.positions[a];\n    const B = flat.positions[b];\n    const C = flat.positions[c];\n    const qx1 = B.x - A.x, qy1 = B.y - A.y;\n    const qx2 = C.x - A.x, qy2 = C.y - A.y;\n    const i00 = local.vy / det;\n    const i01 = -local.vx / det;\n    const i11 = local.ux / det;\n    const j00 = qx1 * i00;\n    const j01 = qx1 * i01 + qx2 * i11;\n    const j10 = qy1 * i00;\n    const j11 = qy1 * i01 + qy2 * i11;\n    const e = (j00 + j11) / 2;\n    const f = (j00 - j11) / 2;\n    const g = (j10 + j01) / 2;\n    const h = (j10 - j01) / 2;\n    const q = Math.hypot(e, h);\n    const r = Math.hypot(f, g);\n    const sigma1 = q + r;\n    const sigma2 = Math.abs(q - r);\n    return Math.max(sigma1, sigma2 > 0 ? 1 / sigma2 : Infinity);\n}\nfunction signedArea(a, b, c) {\n    return ((b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x)) / 2;\n}\nfunction distance3d(mesh, u, v) {\n    const p = mesh.positions[u];\n    const q = mesh.positions[v];\n    return Math.hypot(q.x - p.x, q.y - p.y, q.z - p.z);\n}\n"
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.6,
          "y": 0.5,
          "uniqueID": "id-136213",
          "name": "ExtractFaces",
          "ioValues": [
            {
              "name": "Assembly",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-136210",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136211",
              "name": "Assembly",
              "type": "faceselect",
              "options": [],
              "selectedFaceData": {
                "0": [
                  0
                ]
              }
            },
            {
              "atomType": "Code",
              "x": 0.3,
              "y": 0.8,
              "uniqueID": "id-136212",
              "ioValues": [
                {
                  "name": "input",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ],
              "interpreterVersion": 1,
              "code": "function run(input: Assembly) {\n  const res =  input.onLeafs((leaf) => {\n    if (leaf.geometry instanceof replicad.Drawing) {\n      throw new Error(\"Drawings have no Faces. Input must be 3d\")\n    }\n    if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n      const faces = leaf.geometry.faces;\n      const faceIndexes = leaf.selection.faces\n      if (faceIndexes.length == 1) {\n        leaf.geometry = faces[faceIndexes[0]]\n        return leaf;\n      } else {\n        const res = faceIndexes.map((index) => {\n          return new Assembly({ ...leaf, geometry: faces[index] })\n        })\n        return new Assembly({\n          geometry: res\n        })\n      }\n    }\n    return null;\n  })\n  console.log(res)\n  return res;\n}",
              "compiledCode": "\"use strict\";\nfunction run(input) {\n    const res = input.onLeafs((leaf) => {\n        if (leaf.geometry instanceof replicad.Drawing) {\n            throw new Error(\"Drawings have no Faces. Input must be 3d\");\n        }\n        if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n            const faces = leaf.geometry.faces;\n            const faceIndexes = leaf.selection.faces;\n            if (faceIndexes.length == 1) {\n                leaf.geometry = faces[faceIndexes[0]];\n                return leaf;\n            }\n            else {\n                const res = faceIndexes.map((index) => {\n                    return new Assembly({ ...leaf, geometry: faces[index] });\n                });\n                return new Assembly({\n                    geometry: res\n                });\n            }\n        }\n        return null;\n    });\n    console.log(res);\n    return res;\n}\n"
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "input",
              "ap1ID": "id-136211",
              "ap2ID": "id-136212"
            },
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-136212",
              "ap2ID": "id-136210"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "ExtractFaces",
            "dateCreated": "2026-07-24T19:57:30Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/README.md?sanitize=true",
            "yyyy": 2026,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/tristan-huber/ExtractFaces",
            "topics": [
              "abundance-tool"
            ],
            "pullRequests": [],
            "forks": 0,
            "dateModified": "2026-07-24T20:38:04Z",
            "ranking": 3,
            "searchField": "extractfaces tristan-huber extract faces from an assembly by click selection. faces are identified by index within their leaf of the assembly. returns a new assembly with just faces abundance-tool",
            "owner": "tristan-huber",
            "description": "Extract faces from an assembly by click selection. Faces are identified by index within their leaf of the assembly. Returns a new assembly with just faces",
            "contentURL": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "Unitless",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790275752349
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.7,
          "y": 0.6,
          "uniqueID": "id-136226",
          "name": "util.asFaces",
          "ioValues": [
            {
              "name": "shape",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136223",
              "name": "shape",
              "type": "geometry",
              "options": []
            },
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-136224",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Code",
              "x": 0.4,
              "y": 0.6,
              "uniqueID": "id-136225",
              "ioValues": [
                {
                  "name": "shape",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ],
              "interpreterVersion": 1,
              "code": "\nfunction run(\n  shape: Assembly\n) {\n  let result = undefined;\n  return shape.onLeafs((leaf) => {\n    if (leaf.is3D()) {\n      const f = leaf.geometry.faces\n      if (f.length > 1) {\n        const subassembly = []\n        for (const face of f) {\n          subassembly.push(new Assembly({\n            ...leaf\n            geometry: face\n          }))\n        }\n        leaf.geometry = subassembly\n      } else {\n        leaf.geometry = f[0]\n      }\n    } else if (leaf.geometry instanceof replicad.Drawing) {\n      const solidifiedFaces = leaf.geometry.sketchOnPlane(leaf.plane).extrude(0.1).faces\n      leaf.geometry = solidifiedFaces[solidifiedFaces.length - 2] // Usually where the initial face is indexed after extrusion.      \n    } else if (leaf.geometry instanceof replicad.Face) {\n      leaf.geometry = leaf.geometry.clone();\n    } else {\n      return undefined\n    }\n    return leaf;\n  })?.skipDisjointPostprocess()\n}\n",
              "compiledCode": "\"use strict\";\nfunction run(shape) {\n    let result = undefined;\n    return shape.onLeafs((leaf) => {\n        if (leaf.is3D()) {\n            const f = leaf.geometry.faces;\n            if (f.length > 1) {\n                const subassembly = [];\n                for (const face of f) {\n                    subassembly.push(new Assembly({\n                        ...leaf,\n                        geometry: face\n                    }));\n                }\n                leaf.geometry = subassembly;\n            }\n            else {\n                leaf.geometry = f[0];\n            }\n        }\n        else if (leaf.geometry instanceof replicad.Drawing) {\n            const solidifiedFaces = leaf.geometry.sketchOnPlane(leaf.plane).extrude(0.1).faces;\n            leaf.geometry = solidifiedFaces[solidifiedFaces.length - 2]; // Usually where the initial face is indexed after extrusion.      \n        }\n        else if (leaf.geometry instanceof replicad.Face) {\n            leaf.geometry = leaf.geometry.clone();\n        }\n        else {\n            return undefined;\n        }\n        return leaf;\n    })?.skipDisjointPostprocess();\n}\n"
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "shape",
              "ap1ID": "id-136223",
              "ap2ID": "id-136225"
            },
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-136225",
              "ap2ID": "id-136224"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "util.asFaces",
            "dateCreated": "2026-07-20T21:58:22Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/tristan-huber/util.asFaces/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/tristan-huber/util.asFaces/master/README.md?sanitize=true",
            "yyyy": 2026,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/tristan-huber/util.asFaces",
            "topics": [
              "abundance-tool"
            ],
            "pullRequests": [],
            "forks": 0,
            "dateModified": "2026-09-03T21:29:53Z",
            "ranking": 1,
            "searchField": "util.asfaces tristan-huber from the input geometry get all faces then return them as an assembly of bare faces. extract faces as assembly. abundance-tool",
            "owner": "tristan-huber",
            "description": "from the input geometry get all faces then return them as an assembly of bare faces. Extract faces as assembly.",
            "contentURL": "https://raw.githubusercontent.com/tristan-huber/util.asFaces/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/tristan-huber/util.asFaces/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "Unitless",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790275807851
        },
        {
          "atomType": "Move",
          "x": 0.1,
          "y": 0.4,
          "uniqueID": "id-136236",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "xDist",
              "ioValue": 1.3
            },
            {
              "name": "yDist",
              "ioValue": 0
            },
            {
              "name": "zDist",
              "ioValue": 0
            }
          ]
        },
        {
          "atomType": "Difference",
          "x": 0.2,
          "y": 0.2,
          "uniqueID": "id-136242",
          "ioValues": [
            {
              "name": "retain",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "remove",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.6,
          "y": 0.2,
          "uniqueID": "id-136246",
          "name": "ExtractFaces",
          "ioValues": [
            {
              "name": "Assembly",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-136247",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136248",
              "name": "Assembly",
              "type": "faceselect",
              "options": [],
              "selectedFaceData": {
                "0": [
                  0,
                  3
                ]
              }
            },
            {
              "atomType": "Code",
              "x": 0.3,
              "y": 0.8,
              "uniqueID": "id-136249",
              "ioValues": [
                {
                  "name": "input",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ],
              "interpreterVersion": 1,
              "code": "function run(input: Assembly) {\n  const res =  input.onLeafs((leaf) => {\n    if (leaf.geometry instanceof replicad.Drawing) {\n      throw new Error(\"Drawings have no Faces. Input must be 3d\")\n    }\n    if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n      const faces = leaf.geometry.faces;\n      const faceIndexes = leaf.selection.faces\n      if (faceIndexes.length == 1) {\n        leaf.geometry = faces[faceIndexes[0]]\n        return leaf;\n      } else {\n        const res = faceIndexes.map((index) => {\n          return new Assembly({ ...leaf, geometry: faces[index] })\n        })\n        return new Assembly({\n          geometry: res\n        })\n      }\n    }\n    return null;\n  })\n  console.log(res)\n  return res;\n}",
              "compiledCode": "\"use strict\";\nfunction run(input) {\n    const res = input.onLeafs((leaf) => {\n        if (leaf.geometry instanceof replicad.Drawing) {\n            throw new Error(\"Drawings have no Faces. Input must be 3d\");\n        }\n        if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n            const faces = leaf.geometry.faces;\n            const faceIndexes = leaf.selection.faces;\n            if (faceIndexes.length == 1) {\n                leaf.geometry = faces[faceIndexes[0]];\n                return leaf;\n            }\n            else {\n                const res = faceIndexes.map((index) => {\n                    return new Assembly({ ...leaf, geometry: faces[index] });\n                });\n                return new Assembly({\n                    geometry: res\n                });\n            }\n        }\n        return null;\n    });\n    console.log(res);\n    return res;\n}\n"
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "input",
              "ap1ID": "id-136248",
              "ap2ID": "id-136249"
            },
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-136249",
              "ap2ID": "id-136247"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "ExtractFaces",
            "dateCreated": "2026-07-24T19:57:30Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/README.md?sanitize=true",
            "yyyy": 2026,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/tristan-huber/ExtractFaces",
            "topics": [
              "abundance-tool"
            ],
            "pullRequests": [],
            "forks": 0,
            "dateModified": "2026-07-24T20:38:04Z",
            "ranking": 3,
            "searchField": "extractfaces tristan-huber extract faces from an assembly by click selection. faces are identified by index within their leaf of the assembly. returns a new assembly with just faces abundance-tool",
            "owner": "tristan-huber",
            "description": "Extract faces from an assembly by click selection. Faces are identified by index within their leaf of the assembly. Returns a new assembly with just faces",
            "contentURL": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/tristan-huber/ExtractFaces/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "Unitless",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790275752349
        },
        {
          "atomType": "Code",
          "x": 0.8,
          "y": 0.2,
          "uniqueID": "id-136924",
          "ioValues": [
            {
              "name": "surface",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "tolerance",
              "ioValue": 0.01
            },
            {
              "name": "asSingular",
              "ioValue": true
            },
            {
              "name": "maxTensileStrain",
              "ioValue": 0.05,
              "currentEquation": "0.05"
            },
            {
              "name": "maxCompressiveStrain",
              "ioValue": 0.05,
              "currentEquation": "0.05"
            }
          ],
          "interpreterVersion": 1,
          "code": "// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(\n  surface: Assembly,\n  tolerance: number = 0.01,\n  asSingular: boolean = false,\n  maxTensileStrain: number = 0.05,\n  maxCompressiveStrain: number = 0.05,\n): Assembly {\n  const toUnroll: { srcLeaf: Assembly; mesh: Mesh }[] = [];\n  surface.onLeafs((leaf) => {\n    const sface = leaf.getSelectedFaces().filter((f) => f != undefined);\n    if (sface.length > 0) {\n      toUnroll.push(\n        ...sface.map((f) => {\n          return { srcLeaf: leaf, mesh: faceToMesh(f, tolerance) };\n        }),\n      );\n    } else if (leaf.geometry instanceof replicad.Face) {\n      toUnroll.push({\n        srcLeaf: leaf,\n        mesh: faceToMesh(leaf.geometry, tolerance),\n      });\n    }\n    return leaf;\n  });\n  if (toUnroll.length == 0) {\n    throw new Error(\"No selected faces, and not an assembly of face geoms\");\n  }\n\n  if (asSingular) {\n    // combine meshes first, then unroll\n    const foo = toUnroll.reduce(\n      (accumulator: Mesh, curr) => {\n        return combineMeshs(accumulator, curr.mesh);\n      },\n      { positions: [], tris: [] },\n    );\n    const unrolled = unrollArap(foo);\n    const distortion = measureDistortion(foo, unrolled);\n    console.log(distortion.triStrains.length);\n    console.log(foo.tris.length);\n    console.log(\n      `pointref range. input mesh: ${foo.positions.length} result mesh: ${unrolled.positions.length}`,\n    );\n    return new Assembly({\n      geometry: unrolledWithHoles(unrolled),\n      metadata: {\n        meshOverride: buildHeatmapMeshOverride(\n          foo,\n          unrolled,\n          distortion.triStrains,\n          maxTensileStrain,\n          maxCompressiveStrain,\n        ),\n      },\n    });\n  } else {\n    let ybuffer = 0;\n    const result = toUnroll.map((leaf) => {\n      const unrollResult = unrollArap(leaf.mesh);\n      const distortion = measureDistortion(leaf.mesh, unrollResult);\n      const drawing = unrolledWithHoles(unrollResult).translate(0, ybuffer);\n      const meshOverride = buildHeatmapMeshOverride(\n        leaf.mesh,\n        unrollResult,\n        distortion.triStrains,\n        maxTensileStrain,\n        maxCompressiveStrain,\n        ybuffer,\n      );\n      ybuffer += drawing.boundingBox.height * 1.1;\n      return new Assembly({\n        ...leaf.srcLeaf,\n        geometry: drawing,\n        metadata: {\n          ...leaf.srcLeaf.metadata,\n          meshOverride,\n        },\n      });\n    });\n    return new Assembly({ geometry: result.flat() }).skipDisjointPostprocess();\n  }\n}\n\nfunction unrolledWithHoles(unrolResult: FlatMesh): replicad.Drawing {\n  let res = xysToClosedDrawing(unrolResult.outline);\n  unrolResult.innerWires.forEach((inner: Point2D[]) => {\n    res = res.cut(xysToClosedDrawing(inner));\n  });\n  return res;\n}\n\nfunction buildHeatmapMeshOverride(\n  mesh: Mesh,\n  flat: FlatMesh,\n  triStrains: { e1: number; e2: number }[],\n  maxTensileStrain: number,\n  maxCompressiveStrain: number,\n  yOffset: number = 0,\n) {\n  const vertices: number[] = [];\n  const triangles: number[] = [];\n  const vertexColors: number[] = [];\n\n  mesh.tris.forEach((tri, triIndex) => {\n    const { e1, e2 } = triStrains[triIndex] ?? { e1: 0, e2: 0 };\n    const tensionRisk = e1 > 0 ? e1 / maxTensileStrain : 0;\n    const compressionRisk = e2 < 0 ? -e2 / maxCompressiveStrain : 0;\n    const risk = Math.max(tensionRisk, compressionRisk, 0);\n    const [r, g, b] = riskToRgb(risk);\n    const baseIndex = vertices.length / 3;\n\n    tri.forEach((vertexIndex) => {\n      const point = flat.positions[vertexIndex];\n      vertices.push(point.x, point.y + yOffset, 0);\n      vertexColors.push(r, g, b);\n    });\n\n    triangles.push(baseIndex, baseIndex + 1, baseIndex + 2);\n  });\n\n  return {\n    faces: {\n      vertices,\n      triangles,\n    },\n    vertexColors,\n  };\n}\n\n// 0 = no distortion (white), 1 = exactly at the material's strain limit (solid\n// red); clamped for anything past the limit since it's already unsafe.\nfunction riskToRgb(risk: number): [number, number, number] {\n  const t = Math.max(0, Math.min(1, risk));\n  return [1, 1 - t, 1 - t];\n}\n\nfunction combineMeshs(m1: Mesh, m2: Mesh): Mesh {\n  console.log(\n    `m1, position count: ${m1.positions.length} tris count: ${m1.tris.length}`,\n  );\n  console.log(\n    `m2, position count: ${m2.positions.length} tris count: ${m2.tris.length}`,\n  );\n  const floatEq = (a: number, b: number) => {\n    return Math.abs(a - b) < 0.000001;\n  };\n\n  // find match points TODO this is deeply suboptimal. Esp if combineMeshs is called more than once.\n  const indexTuples: { m1: number; m2: number }[] = [];\n  m1.positions.forEach((point, index) => {\n    m2.positions.forEach((p2, i2) => {\n      if (\n        floatEq(point.x, p2.x) &&\n        floatEq(point.y, p2.y) &&\n        floatEq(point.z, p2.z)\n      ) {\n        console.log(\"found a match\");\n        indexTuples.push({ m1: index, m2: i2 });\n      }\n    });\n  });\n  console.log(indexTuples);\n\n  const shiftTris = m2.tris.map((tri) => {\n    return tri.map((pointIndex) => (pointIndex += m1.positions.length)) as Tri3;\n  });\n  // replace equivalent points from meshes\n  const dedupTris = shiftTris.map((tri) => {\n    return tri.map((pointIndex) => {\n      const unshift = pointIndex - m1.positions.length;\n      const i = indexTuples.map((f) => f.m2).indexOf(unshift);\n      if (i >= 0) {\n        return indexTuples[i].m1;\n      }\n      return pointIndex;\n    }) as Tri3;\n  });\n  const res = {\n    positions: m1.positions.concat(m2.positions),\n    tris: m1.tris.concat(dedupTris),\n  };\n  console.log(\n    `res, position count: ${res.positions.length} tris count: ${res.tris.length}`,\n  );\n  return res;\n}\n\nfunction faceToMesh(face: replicad.Face, tolerance: number): Mesh {\n  const replMesh = face.mesh({ tolerance: tolerance, angularTolerance: 0.1 });\n  // translate\n  const points: Point3D[] = [];\n  const v = replMesh.vertices;\n  for (let i = 0; i < v.length; i += 3) {\n    points.push({ x: v[i], y: v[i + 1], z: v[i + 2] });\n  }\n  const tris: Tri3[] = [];\n  const t = replMesh.triangles;\n  for (let i = 0; i < t.length; i += 3) {\n    tris.push([t[i], t[i + 1], t[i + 2]]);\n  }\n  return { positions: points, tris: tris };\n}\n\nfunction xysToClosedDrawing(points: Point2D[]) {\n  const pen = replicad.draw([points[0].x, points[0].y]);\n  for (let i = 1; i < points.length; i++) {\n    pen.lineTo([points[i].x, points[i].y]);\n  }\n  return pen.close();\n}\n\nexport type Point2D = { x: number; y: number };\nexport type Point3D = { x: number; y: number; z: number };\n\nexport type Tri3 = [number, number, number];\n\nexport type Mesh = {\n  positions: Point3D[];\n  tris: Tri3[];\n};\n\nexport type FlatMesh = {\n  positions: Point2D[];\n  outline: Point2D[];\n  innerWires: Point2D[][];\n};\n\nexport type Unroller = (mesh: Mesh) => FlatMesh;\n\nconst COT_MIN = 0.05;\nconst COT_MAX = 20;\n\nconst ITERATIONS = 4000;\n\nconst SETTLED = 1e-6;\n\ntype Adj = { j: number; w: number; t: number; dx: number; dy: number };\n\nexport function unrollArap(mesh: Mesh): FlatMesh {\n  const tris = buildTris(mesh);\n  const nv = mesh.positions.length;\n  const flat = placeByUnfolding(mesh, tris);\n\n  if (tris.length > 0 && nv > 1) {\n    const { adj, triW } = buildAdjacency(tris, nv);\n    solve(flat, tris, adj, triW);\n  }\n\n  const positions = canonicalise(flat);\n  const edges = boundaryLoop(mesh).map((wire) => wire.map((i) => positions[i]));\n  return { positions, outline: edges[0], innerWires: edges.slice(1) };\n}\n\nfunction buildAdjacency(\n  tris: Tri[],\n  nv: number,\n): { adj: Adj[][]; triW: Float64Array } {\n  const adj: Adj[][] = [];\n  const triW = new Float64Array(tris.length * 3);\n  for (let i = 0; i < nv; i++) adj.push([]);\n\n  tris.forEach((tri, t) => {\n    const { ids, xs } = tri;\n    const twoArea = 2 * tri.area;\n    for (let e = 0; e < 3; e++) {\n      const p = e;\n      const q = (e + 1) % 3;\n      const o = (e + 2) % 3;\n      const ax = xs[p].x - xs[o].x,\n        ay = xs[p].y - xs[o].y;\n      const bx = xs[q].x - xs[o].x,\n        by = xs[q].y - xs[o].y;\n\n      const cot = twoArea > 0 ? (ax * bx + ay * by) / twoArea : COT_MIN;\n      const w = Math.min(COT_MAX, Math.max(COT_MIN, cot));\n      triW[t * 3 + e] = w;\n      const dx = xs[p].x - xs[q].x;\n      const dy = xs[p].y - xs[q].y;\n      adj[ids[p]].push({ j: ids[q], w, t, dx, dy });\n      adj[ids[q]].push({ j: ids[p], w, t, dx: -dx, dy: -dy });\n    }\n  });\n  return { adj, triW };\n}\n\nfunction solve(flat: Point2D[], tris: Tri[], adj: Adj[][], triW: Float64Array) {\n  const nv = flat.length;\n  const cos = new Float64Array(tris.length);\n  const sin = new Float64Array(tris.length);\n  const tol = SETTLED * intrinsicScale(tris);\n  const system = factorSystem(adj, nv);\n\n  const bestX = new Float64Array(nv);\n  const bestY = new Float64Array(nv);\n  let bestE = energyOf(flat, tris);\n  snapshot(flat, bestX, bestY);\n\n  const rx = new Float64Array(Math.max(nv - 1, 0));\n  const ry = new Float64Array(Math.max(nv - 1, 0));\n\n  for (let it = 0; it < ITERATIONS; it++) {\n    localStep(flat, tris, triW, cos, sin);\n\n    let maxDelta = 0;\n    if (system) {\n      rx.fill(0);\n      ry.fill(0);\n      for (let i = 1; i < nv; i++) {\n        let bx = 0;\n        let by = 0;\n        for (const e of adj[i]) {\n          bx += e.w * (cos[e.t] * e.dx - sin[e.t] * e.dy);\n          by += e.w * (sin[e.t] * e.dx + cos[e.t] * e.dy);\n          if (e.j === 0) {\n            bx += e.w * flat[0].x;\n            by += e.w * flat[0].y;\n          }\n        }\n        rx[i - 1] = bx;\n        ry[i - 1] = by;\n      }\n      solveBanded(system, rx, ry);\n      for (let i = 1; i < nv; i++) {\n        const d = Math.hypot(rx[i - 1] - flat[i].x, ry[i - 1] - flat[i].y);\n        if (d > maxDelta) maxDelta = d;\n        flat[i].x = rx[i - 1];\n        flat[i].y = ry[i - 1];\n      }\n    } else {\n      maxDelta = gaussSeidel(flat, adj, cos, sin, nv);\n    }\n\n    const e = energyOf(flat, tris);\n    if (e < bestE) {\n      bestE = e;\n      snapshot(flat, bestX, bestY);\n    }\n    if (maxDelta < tol) break;\n  }\n\n  restore(flat, bestX, bestY);\n}\n\nfunction snapshot(flat: Point2D[], x: Float64Array, y: Float64Array) {\n  for (let i = 0; i < flat.length; i++) {\n    if (flat[i] == undefined) {\n      continue;\n    }\n    x[i] = flat[i].x;\n    y[i] = flat[i].y;\n  }\n}\n\nfunction restore(flat: Point2D[], x: Float64Array, y: Float64Array) {\n  for (let i = 0; i < flat.length; i++) {\n    flat[i] = { x: x[i], y: y[i] };\n  }\n}\n\nfunction localStep(\n  flat: Point2D[],\n  tris: Tri[],\n  triW: Float64Array,\n  cos: Float64Array,\n  sin: Float64Array,\n) {\n  tris.forEach((tri, t) => {\n    const { ids, xs } = tri;\n    let num = 0;\n    let den = 0;\n    for (let e = 0; e < 3; e++) {\n      const p = e;\n      const q = (e + 1) % 3;\n      const w = triW[t * 3 + e];\n      const dxc = xs[q].x - xs[p].x;\n      const dyc = xs[q].y - xs[p].y;\n      const dqx = flat[ids[q]].x - flat[ids[p]].x;\n      const dqy = flat[ids[q]].y - flat[ids[p]].y;\n      num += w * (dqy * dxc - dqx * dyc);\n      den += w * (dqx * dxc + dqy * dyc);\n    }\n    const th = Math.atan2(num, den);\n    cos[t] = Math.cos(th);\n    sin[t] = Math.sin(th);\n  });\n}\n\nfunction gaussSeidel(\n  flat: Point2D[],\n  adj: Adj[][],\n  cos: Float64Array,\n  sin: Float64Array,\n  nv: number,\n): number {\n  let maxDelta = 0;\n  for (let sweep = 0; sweep < 3; sweep++) {\n    for (let i = 1; i < nv; i++) {\n      let diag = 0;\n      let sx = 0;\n      let sy = 0;\n      for (const e of adj[i]) {\n        diag += e.w;\n        sx += e.w * (flat[e.j].x + cos[e.t] * e.dx - sin[e.t] * e.dy);\n        sy += e.w * (flat[e.j].y + sin[e.t] * e.dx + cos[e.t] * e.dy);\n      }\n      if (diag <= 0) continue;\n      const nx = sx / diag;\n      const ny = sy / diag;\n      const d = Math.hypot(nx - flat[i].x, ny - flat[i].y);\n      if (d > maxDelta) maxDelta = d;\n      flat[i] = { x: nx, y: ny };\n    }\n  }\n  return maxDelta;\n}\n\ntype Banded = { n: number; b: number; a: Float64Array; envelope: Int32Array };\n\nfunction factorSystem(adj: Adj[][], nv: number): Banded | null {\n  const n = nv - 1;\n  if (n <= 0) return null;\n\n  let b = 0;\n  for (let i = 1; i < nv; i++) {\n    for (const e of adj[i]) {\n      if (e.j === 0) continue;\n      const d = Math.abs(i - e.j);\n      if (d > b) b = d;\n    }\n  }\n  if (b >= n) return null;\n\n  const a = new Float64Array(n * (b + 1));\n  for (let i = 1; i < nv; i++) {\n    const r = i - 1;\n    for (const e of adj[i]) {\n      a[r * (b + 1)] += e.w;\n      if (e.j === 0) continue;\n      const c = e.j - 1;\n      if (c < r) a[r * (b + 1) + (r - c)] -= e.w;\n    }\n  }\n\n  const envelope = new Int32Array(n);\n  for (let r = 0; r < n; r++) {\n    const top = Math.min(b, r);\n    for (let k = top; k >= 1; k--) {\n      if (a[r * (b + 1) + k] !== 0) {\n        envelope[r] = k;\n        break;\n      }\n    }\n  }\n\n  for (let i = 0; i < n; i++) {\n    const wi = envelope[i];\n    for (let k = wi; k >= 1; k--) {\n      const j = i - k;\n      if (j < 0) continue;\n      let s = a[i * (b + 1) + k];\n      const top = Math.min(wi - k, envelope[j]);\n      for (let m = 1; m <= top; m++) {\n        s -= a[i * (b + 1) + (k + m)] * a[j * (b + 1) + m];\n      }\n      a[i * (b + 1) + k] = s / a[j * (b + 1)];\n    }\n    let d = a[i * (b + 1)];\n    for (let k = 1; k <= wi; k++) {\n      d -= a[i * (b + 1) + k] * a[i * (b + 1) + k];\n    }\n    if (!(d > 0)) return null;\n    a[i * (b + 1)] = Math.sqrt(d);\n  }\n  return { n, b, a, envelope };\n}\n\nfunction solveBanded(m: Banded, rx: Float64Array, ry: Float64Array) {\n  const { n, b, a, envelope } = m;\n  const w = b + 1;\n  for (let i = 0; i < n; i++) {\n    let sx = rx[i];\n    let sy = ry[i];\n    const wi = envelope[i];\n    for (let k = 1; k <= wi; k++) {\n      const v = a[i * w + k];\n      sx -= v * rx[i - k];\n      sy -= v * ry[i - k];\n    }\n    const d = a[i * w];\n    rx[i] = sx / d;\n    ry[i] = sy / d;\n  }\n  for (let i = n - 1; i >= 0; i--) {\n    const d = a[i * w];\n    const vx = rx[i] / d;\n    const vy = ry[i] / d;\n    rx[i] = vx;\n    ry[i] = vy;\n    const wi = envelope[i];\n    for (let k = 1; k <= wi; k++) {\n      const v = a[i * w + k];\n      rx[i - k] -= v * vx;\n      ry[i - k] -= v * vy;\n    }\n  }\n}\n\nfunction intrinsicScale(tris: Tri[]): number {\n  let area = 0;\n  for (const tri of tris) area += tri.area;\n  return area > 0 ? Math.sqrt(area) : 1;\n}\n\nexport type Tri = {\n  ids: Tri3;\n  xs: [Point2D, Point2D, Point2D];\n  area: number;\n};\n\nexport function buildTris(mesh: Mesh): Tri[] {\n  const out: Tri[] = [];\n  for (const [a, b, c] of mesh.tris) {\n    const A = mesh.positions[a];\n    const B = mesh.positions[b];\n    const C = mesh.positions[c];\n    const ux = B.x - A.x,\n      uy = B.y - A.y,\n      uz = B.z - A.z;\n    const vx = C.x - A.x,\n      vy = C.y - A.y,\n      vz = C.z - A.z;\n    const cx = uy * vz - uz * vy;\n    const cy = uz * vx - ux * vz;\n    const cz = ux * vy - uy * vx;\n    const cross = Math.hypot(cx, cy, cz);\n    const la = Math.hypot(ux, uy, uz);\n    const lb = Math.hypot(vx, vy, vz);\n    if (la === 0 || lb === 0) continue;\n    const cos = Math.max(\n      -1,\n      Math.min(1, (ux * vx + uy * vy + uz * vz) / (la * lb)),\n    );\n    out.push({\n      ids: [a, b, c],\n      xs: [\n        { x: 0, y: 0 },\n        { x: la, y: 0 },\n        { x: lb * cos, y: lb * Math.sqrt(Math.max(0, 1 - cos * cos)) },\n      ],\n      area: cross / 2,\n    });\n  }\n  return out;\n}\n\nexport function energyOf(flat: Point2D[], tris: Tri[]): number {\n  let e = 0;\n  let area = 0;\n  for (const tri of tris) {\n    const { ids, xs } = tri;\n    for (let k = 0; k < 3; k++) {\n      const p = k;\n      const q = (k + 1) % 3;\n      const lc = Math.hypot(xs[q].x - xs[p].x, xs[q].y - xs[p].y);\n      const lq = Math.hypot(\n        flat[ids[q]].x - flat[ids[p]].x,\n        flat[ids[q]].y - flat[ids[p]].y,\n      );\n      e += (lq - lc) * (lq - lc);\n    }\n    area += tri.area;\n  }\n  return area > 0 ? e / area : 0;\n}\n\nexport function placeByUnfolding(mesh: Mesh, tris: Tri[]): Point2D[] {\n  const nv = mesh.positions.length;\n  const flat: Point2D[] = new Array(nv);\n  const placed: boolean[] = new Array(nv).fill(false);\n\n  const incident: number[][] = [];\n  for (let i = 0; i < nv; i++) incident.push([]);\n  tris.forEach((tri, t) => {\n    for (const id of tri.ids) incident[id].push(t);\n  });\n\n  const seed = tris[0];\n  seed.ids.forEach((id, k) => {\n    flat[id] = { x: seed.xs[k].x, y: seed.xs[k].y };\n    placed[id] = true;\n  });\n\n  const queue: number[] = seed.ids.flatMap((id) => incident[id]);\n  for (let head = 0; head < queue.length; head++) {\n    const tri = tris[queue[head]];\n    const open = tri.ids.filter((i) => !placed[i]);\n    if (open.length !== 1) continue;\n    const v = open[0];\n    const p = placeFrom(\n      bestHinge(v, incident[v], tris, placed, tri),\n      v,\n      flat,\n      placed,\n    );\n    if (!p) continue;\n    flat[v] = p;\n    placed[v] = true;\n    for (const t of incident[v]) queue.push(t);\n  }\n\n  for (let i = 0; i < nv; i++) {\n    if (!placed[i])\n      console.warn(`unroll: vertex ${i} of ${nv} is not connected to the mesh`);\n  }\n  return flat;\n}\n\nfunction bestHinge(\n  v: number,\n  candidates: number[],\n  tris: Tri[],\n  placed: boolean[],\n  fallback: Tri,\n): Tri {\n  let best = fallback;\n  let bestScore = -Infinity;\n  for (const t of candidates) {\n    const tri = tris[t];\n    const k = tri.ids.indexOf(v);\n    if (k < 0) continue;\n    const i = (k + 1) % 3;\n    const j = (k + 2) % 3;\n    if (!placed[tri.ids[i]] || !placed[tri.ids[j]]) continue;\n    const edge = sq(tri.xs[j].x - tri.xs[i].x, tri.xs[j].y - tri.xs[i].y);\n    const reach = sq(tri.xs[k].x - tri.xs[i].x, tri.xs[k].y - tri.xs[i].y);\n    const score = reach > 0 ? edge / reach : Infinity;\n    if (score > bestScore) {\n      bestScore = score;\n      best = tri;\n    }\n  }\n  return best;\n}\n\nfunction sq(dx: number, dy: number): number {\n  return dx * dx + dy * dy;\n}\n\nfunction placeFrom(\n  tri: Tri,\n  v: number,\n  flat: Point2D[],\n  placed: boolean[],\n): Point2D | null {\n  const { ids, xs } = tri;\n  const k = ids.indexOf(v);\n  const i = (k + 1) % 3;\n  const j = (k + 2) % 3;\n  if (!placed[ids[i]] || !placed[ids[j]]) return null;\n\n  const dx = xs[j].x - xs[i].x;\n  const dy = xs[j].y - xs[i].y;\n  const den = dx * dx + dy * dy;\n  if (den < 1e-12) return null;\n  const ex = flat[ids[j]].x - flat[ids[i]].x;\n  const ey = flat[ids[j]].y - flat[ids[i]].y;\n  const sr = (ex * dx + ey * dy) / den;\n  const si = (ey * dx - ex * dy) / den;\n  const norm = Math.hypot(sr, si);\n  if (norm < 1e-12) return null;\n  const cr = sr / norm;\n  const ci = si / norm;\n  const vx = xs[k].x - xs[i].x;\n  const vy = xs[k].y - xs[i].y;\n  return {\n    x: flat[ids[i]].x + cr * vx - ci * vy,\n    y: flat[ids[i]].y + ci * vx + cr * vy,\n  };\n}\n\nexport function canonicalise(flat: Point2D[]): Point2D[] {\n  if (flat.length === 0) return flat;\n  let cx = 0;\n  let cy = 0;\n  for (const p of flat) {\n    cx += p.x;\n    cy += p.y;\n  }\n  cx /= flat.length;\n  cy /= flat.length;\n\n  let sxx = 0;\n  let sxy = 0;\n  let syy = 0;\n  for (const p of flat) {\n    const dx = p.x - cx;\n    const dy = p.y - cy;\n    sxx += dx * dx;\n    sxy += dx * dy;\n    syy += dy * dy;\n  }\n  const ang = 0.5 * Math.atan2(2 * sxy, sxx - syy);\n  const co = Math.cos(-ang);\n  const si = Math.sin(-ang);\n\n  let minX = Infinity;\n  let minY = Infinity;\n  const out = flat.map((p) => {\n    const dx = p.x - cx;\n    const dy = p.y - cy;\n    const q = { x: co * dx - si * dy, y: si * dx + co * dy };\n    if (q.x < minX) minX = q.x;\n    if (q.y < minY) minY = q.y;\n    return q;\n  });\n  for (const q of out) {\n    q.x -= minX;\n    q.y -= minY;\n  }\n  return out;\n}\n\nexport function boundaryEdges(mesh: Mesh): [number, number][] {\n  const used = new Map<string, number>();\n  const key = (a: number, b: number) => (a < b ? `${a}-${b}` : `${b}-${a}`);\n\n  for (const [a, b, c] of mesh.tris) {\n    for (const [u, v] of [\n      [a, b],\n      [b, c],\n      [c, a],\n    ]) {\n      used.set(key(u, v), (used.get(key(u, v)) ?? 0) + 1);\n    }\n  }\n\n  const out: [number, number][] = [];\n  for (const [a, b, c] of mesh.tris) {\n    for (const [u, v] of [\n      [a, b],\n      [b, c],\n      [c, a],\n    ] as [number, number][]) {\n      if (used.get(key(u, v)) === 1) out.push([u, v]);\n    }\n  }\n  return out;\n}\n\nexport function chainEdges(edges: [number, number][]): number[][] {\n  const next = new Map<number, number>();\n  const hasPredecessor = new Set<number>();\n  for (const [u, v] of edges) {\n    next.set(u, v);\n    hasPredecessor.add(v);\n  }\n\n  const chains: number[][] = [];\n  const visited = new Set<number>();\n\n  const walk = (seed: number) => {\n    const chain = [seed];\n    visited.add(seed);\n    let cur = seed;\n    while (true) {\n      const to = next.get(cur);\n      if (to === undefined || to === seed) break;\n      if (visited.has(to)) break;\n      chain.push(to);\n      visited.add(to);\n      cur = to;\n    }\n\n    if (next.get(cur) === seed && chain.length > 2) chain.push(seed);\n    if (chain.length > 1) chains.push(chain);\n  };\n\n  for (const [u] of next) if (!hasPredecessor.has(u)) walk(u);\n  for (const [u] of next) if (!visited.has(u)) walk(u);\n  return chains;\n}\n\nexport function boundaryLoop(mesh: Mesh): number[][] {\n  const chains = chainEdges(boundaryEdges(mesh));\n  if (chains.length === 0) return [];\n  return chains.map((loop) => {\n    return loop[0] === loop[loop.length - 1] ? loop.slice(0, -1) : loop;\n  });\n}\n\n// Distortion measurements\nexport type Distortion = {\n  edgeRms: number;\n  edgeMax: number;\n  areaRatio: number;\n  triStrains: { e1: number; e2: number }[];\n};\n\nexport function measureDistortion(mesh: Mesh, flat: FlatMesh): Distortion {\n  let squared = 0;\n  let count = 0;\n  let edgeMax = 0;\n  let area3d = 0;\n  let area2d = 0;\n  const triStrains: { e1: number; e2: number }[] = [];\n\n  for (const [a, b, c] of mesh.tris) {\n    const local = flattenTriangle(mesh, a, b, c);\n    for (const [u, v] of [\n      [a, b],\n      [b, c],\n      [c, a],\n    ]) {\n      const length3d = distance3d(mesh, u, v);\n      if (length3d === 0) continue;\n      const p = flat.positions[u];\n      const q = flat.positions[v];\n      const ratio = Math.hypot(q.x - p.x, q.y - p.y) / length3d - 1;\n      squared += ratio * ratio;\n      count += 1;\n      edgeMax = Math.max(edgeMax, Math.abs(ratio));\n    }\n\n    area3d += local.area;\n    area2d += Math.abs(\n      signedArea(flat.positions[a], flat.positions[b], flat.positions[c]),\n    );\n    triStrains.push(principalStrains(local, flat, a, b, c));\n  }\n\n  return {\n    edgeRms: count > 0 ? Math.sqrt(squared / count) : 0,\n    edgeMax,\n    areaRatio: area3d > 0 ? area2d / area3d : 1,\n    triStrains,\n  };\n}\n\ntype LocalTriangle = { ux: number; vx: number; vy: number; area: number };\n\nfunction flattenTriangle(\n  mesh: Mesh,\n  a: number,\n  b: number,\n  c: number,\n): LocalTriangle {\n  const A = mesh.positions[a];\n  const B = mesh.positions[b];\n  const C = mesh.positions[c];\n  const ex = B.x - A.x,\n    ey = B.y - A.y,\n    ez = B.z - A.z;\n  const fx = C.x - A.x,\n    fy = C.y - A.y,\n    fz = C.z - A.z;\n  const ux = Math.hypot(ex, ey, ez);\n  const dot = ux > 0 ? (ex * fx + ey * fy + ez * fz) / ux : 0;\n  const nx = ey * fz - ez * fy;\n  const ny = ez * fx - ex * fz;\n  const nz = ex * fy - ey * fx;\n  const cross = Math.hypot(nx, ny, nz);\n  return { ux, vx: dot, vy: ux > 0 ? cross / ux : 0, area: cross / 2 };\n}\n\nfunction principalStrains(\n  local: LocalTriangle,\n  flat: FlatMesh,\n  a: number,\n  b: number,\n  c: number,\n): { e1: number; e2: number } {\n  const det = local.ux * local.vy;\n  if (det === 0) return { e1: 0, e2: 0 };\n\n  const A = flat.positions[a];\n  const B = flat.positions[b];\n  const C = flat.positions[c];\n  const qx1 = B.x - A.x,\n    qy1 = B.y - A.y;\n  const qx2 = C.x - A.x,\n    qy2 = C.y - A.y;\n\n  const i00 = local.vy / det;\n  const i01 = -local.vx / det;\n  const i11 = local.ux / det;\n\n  const j00 = qx1 * i00;\n  const j01 = qx1 * i01 + qx2 * i11;\n  const j10 = qy1 * i00;\n  const j11 = qy1 * i01 + qy2 * i11;\n\n  const e = (j00 + j11) / 2;\n  const f = (j00 - j11) / 2;\n  const g = (j10 + j01) / 2;\n  const h = (j10 - j01) / 2;\n  const q = Math.hypot(e, h);\n  const r = Math.hypot(f, g);\n  const sigma1 = q + r;\n  const sigma2 = Math.abs(q - r);\n  // sigma1 >= sigma2, so e1 (tension) is always >= e2 (compression)\n  return { e1: sigma1 - 1, e2: sigma2 - 1 };\n}\n\nfunction signedArea(\n  a: { x: number; y: number },\n  b: { x: number; y: number },\n  c: { x: number; y: number },\n): number {\n  return ((b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x)) / 2;\n}\n\nfunction distance3d(mesh: Mesh, u: number, v: number): number {\n  const p = mesh.positions[u];\n  const q = mesh.positions[v];\n  return Math.hypot(q.x - p.x, q.y - p.y, q.z - p.z);\n}\n",
          "compiledCode": "// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(surface, tolerance = 0.01, asSingular = false, maxTensileStrain = 0.05, maxCompressiveStrain = 0.05) {\n    const toUnroll = [];\n    surface.onLeafs((leaf) => {\n        const sface = leaf.getSelectedFaces().filter((f) => f != undefined);\n        if (sface.length > 0) {\n            toUnroll.push(...sface.map((f) => {\n                return { srcLeaf: leaf, mesh: faceToMesh(f, tolerance) };\n            }));\n        }\n        else if (leaf.geometry instanceof replicad.Face) {\n            toUnroll.push({\n                srcLeaf: leaf,\n                mesh: faceToMesh(leaf.geometry, tolerance),\n            });\n        }\n        return leaf;\n    });\n    if (toUnroll.length == 0) {\n        throw new Error(\"No selected faces, and not an assembly of face geoms\");\n    }\n    if (asSingular) {\n        // combine meshes first, then unroll\n        const foo = toUnroll.reduce((accumulator, curr) => {\n            return combineMeshs(accumulator, curr.mesh);\n        }, { positions: [], tris: [] });\n        const unrolled = unrollArap(foo);\n        const distortion = measureDistortion(foo, unrolled);\n        console.log(distortion.triStrains.length);\n        console.log(foo.tris.length);\n        console.log(`pointref range. input mesh: ${foo.positions.length} result mesh: ${unrolled.positions.length}`);\n        return new Assembly({\n            geometry: unrolledWithHoles(unrolled),\n            metadata: {\n                meshOverride: buildHeatmapMeshOverride(foo, unrolled, distortion.triStrains, maxTensileStrain, maxCompressiveStrain),\n            },\n        });\n    }\n    else {\n        let ybuffer = 0;\n        const result = toUnroll.map((leaf) => {\n            const unrollResult = unrollArap(leaf.mesh);\n            const distortion = measureDistortion(leaf.mesh, unrollResult);\n            const drawing = unrolledWithHoles(unrollResult).translate(0, ybuffer);\n            const meshOverride = buildHeatmapMeshOverride(leaf.mesh, unrollResult, distortion.triStrains, maxTensileStrain, maxCompressiveStrain, ybuffer);\n            ybuffer += drawing.boundingBox.height * 1.1;\n            return new Assembly({\n                ...leaf.srcLeaf,\n                geometry: drawing,\n                metadata: {\n                    ...leaf.srcLeaf.metadata,\n                    meshOverride,\n                },\n            });\n        });\n        return new Assembly({ geometry: result.flat() }).skipDisjointPostprocess();\n    }\n}\nfunction unrolledWithHoles(unrolResult) {\n    let res = xysToClosedDrawing(unrolResult.outline);\n    unrolResult.innerWires.forEach((inner) => {\n        res = res.cut(xysToClosedDrawing(inner));\n    });\n    return res;\n}\nfunction buildHeatmapMeshOverride(mesh, flat, triStrains, maxTensileStrain, maxCompressiveStrain, yOffset = 0) {\n    const vertices = [];\n    const triangles = [];\n    const vertexColors = [];\n    mesh.tris.forEach((tri, triIndex) => {\n        const { e1, e2 } = triStrains[triIndex] ?? { e1: 0, e2: 0 };\n        const tensionRisk = e1 > 0 ? e1 / maxTensileStrain : 0;\n        const compressionRisk = e2 < 0 ? -e2 / maxCompressiveStrain : 0;\n        const risk = Math.max(tensionRisk, compressionRisk, 0);\n        const [r, g, b] = riskToRgb(risk);\n        const baseIndex = vertices.length / 3;\n        tri.forEach((vertexIndex) => {\n            const point = flat.positions[vertexIndex];\n            vertices.push(point.x, point.y + yOffset, 0);\n            vertexColors.push(r, g, b);\n        });\n        triangles.push(baseIndex, baseIndex + 1, baseIndex + 2);\n    });\n    return {\n        faces: {\n            vertices,\n            triangles,\n        },\n        vertexColors,\n    };\n}\n// 0 = no distortion (white), 1 = exactly at the material's strain limit (solid\n// red); clamped for anything past the limit since it's already unsafe.\nfunction riskToRgb(risk) {\n    const t = Math.max(0, Math.min(1, risk));\n    return [1, 1 - t, 1 - t];\n}\nfunction combineMeshs(m1, m2) {\n    console.log(`m1, position count: ${m1.positions.length} tris count: ${m1.tris.length}`);\n    console.log(`m2, position count: ${m2.positions.length} tris count: ${m2.tris.length}`);\n    const floatEq = (a, b) => {\n        return Math.abs(a - b) < 0.000001;\n    };\n    // find match points TODO this is deeply suboptimal. Esp if combineMeshs is called more than once.\n    const indexTuples = [];\n    m1.positions.forEach((point, index) => {\n        m2.positions.forEach((p2, i2) => {\n            if (floatEq(point.x, p2.x) &&\n                floatEq(point.y, p2.y) &&\n                floatEq(point.z, p2.z)) {\n                console.log(\"found a match\");\n                indexTuples.push({ m1: index, m2: i2 });\n            }\n        });\n    });\n    console.log(indexTuples);\n    const shiftTris = m2.tris.map((tri) => {\n        return tri.map((pointIndex) => (pointIndex += m1.positions.length));\n    });\n    // replace equivalent points from meshes\n    const dedupTris = shiftTris.map((tri) => {\n        return tri.map((pointIndex) => {\n            const unshift = pointIndex - m1.positions.length;\n            const i = indexTuples.map((f) => f.m2).indexOf(unshift);\n            if (i >= 0) {\n                return indexTuples[i].m1;\n            }\n            return pointIndex;\n        });\n    });\n    const res = {\n        positions: m1.positions.concat(m2.positions),\n        tris: m1.tris.concat(dedupTris),\n    };\n    console.log(`res, position count: ${res.positions.length} tris count: ${res.tris.length}`);\n    return res;\n}\nfunction faceToMesh(face, tolerance) {\n    const replMesh = face.mesh({ tolerance: tolerance, angularTolerance: 0.1 });\n    // translate\n    const points = [];\n    const v = replMesh.vertices;\n    for (let i = 0; i < v.length; i += 3) {\n        points.push({ x: v[i], y: v[i + 1], z: v[i + 2] });\n    }\n    const tris = [];\n    const t = replMesh.triangles;\n    for (let i = 0; i < t.length; i += 3) {\n        tris.push([t[i], t[i + 1], t[i + 2]]);\n    }\n    return { positions: points, tris: tris };\n}\nfunction xysToClosedDrawing(points) {\n    const pen = replicad.draw([points[0].x, points[0].y]);\n    for (let i = 1; i < points.length; i++) {\n        pen.lineTo([points[i].x, points[i].y]);\n    }\n    return pen.close();\n}\nconst COT_MIN = 0.05;\nconst COT_MAX = 20;\nconst ITERATIONS = 4000;\nconst SETTLED = 1e-6;\nexport function unrollArap(mesh) {\n    const tris = buildTris(mesh);\n    const nv = mesh.positions.length;\n    const flat = placeByUnfolding(mesh, tris);\n    if (tris.length > 0 && nv > 1) {\n        const { adj, triW } = buildAdjacency(tris, nv);\n        solve(flat, tris, adj, triW);\n    }\n    const positions = canonicalise(flat);\n    const edges = boundaryLoop(mesh).map((wire) => wire.map((i) => positions[i]));\n    return { positions, outline: edges[0], innerWires: edges.slice(1) };\n}\nfunction buildAdjacency(tris, nv) {\n    const adj = [];\n    const triW = new Float64Array(tris.length * 3);\n    for (let i = 0; i < nv; i++)\n        adj.push([]);\n    tris.forEach((tri, t) => {\n        const { ids, xs } = tri;\n        const twoArea = 2 * tri.area;\n        for (let e = 0; e < 3; e++) {\n            const p = e;\n            const q = (e + 1) % 3;\n            const o = (e + 2) % 3;\n            const ax = xs[p].x - xs[o].x, ay = xs[p].y - xs[o].y;\n            const bx = xs[q].x - xs[o].x, by = xs[q].y - xs[o].y;\n            const cot = twoArea > 0 ? (ax * bx + ay * by) / twoArea : COT_MIN;\n            const w = Math.min(COT_MAX, Math.max(COT_MIN, cot));\n            triW[t * 3 + e] = w;\n            const dx = xs[p].x - xs[q].x;\n            const dy = xs[p].y - xs[q].y;\n            adj[ids[p]].push({ j: ids[q], w, t, dx, dy });\n            adj[ids[q]].push({ j: ids[p], w, t, dx: -dx, dy: -dy });\n        }\n    });\n    return { adj, triW };\n}\nfunction solve(flat, tris, adj, triW) {\n    const nv = flat.length;\n    const cos = new Float64Array(tris.length);\n    const sin = new Float64Array(tris.length);\n    const tol = SETTLED * intrinsicScale(tris);\n    const system = factorSystem(adj, nv);\n    const bestX = new Float64Array(nv);\n    const bestY = new Float64Array(nv);\n    let bestE = energyOf(flat, tris);\n    snapshot(flat, bestX, bestY);\n    const rx = new Float64Array(Math.max(nv - 1, 0));\n    const ry = new Float64Array(Math.max(nv - 1, 0));\n    for (let it = 0; it < ITERATIONS; it++) {\n        localStep(flat, tris, triW, cos, sin);\n        let maxDelta = 0;\n        if (system) {\n            rx.fill(0);\n            ry.fill(0);\n            for (let i = 1; i < nv; i++) {\n                let bx = 0;\n                let by = 0;\n                for (const e of adj[i]) {\n                    bx += e.w * (cos[e.t] * e.dx - sin[e.t] * e.dy);\n                    by += e.w * (sin[e.t] * e.dx + cos[e.t] * e.dy);\n                    if (e.j === 0) {\n                        bx += e.w * flat[0].x;\n                        by += e.w * flat[0].y;\n                    }\n                }\n                rx[i - 1] = bx;\n                ry[i - 1] = by;\n            }\n            solveBanded(system, rx, ry);\n            for (let i = 1; i < nv; i++) {\n                const d = Math.hypot(rx[i - 1] - flat[i].x, ry[i - 1] - flat[i].y);\n                if (d > maxDelta)\n                    maxDelta = d;\n                flat[i].x = rx[i - 1];\n                flat[i].y = ry[i - 1];\n            }\n        }\n        else {\n            maxDelta = gaussSeidel(flat, adj, cos, sin, nv);\n        }\n        const e = energyOf(flat, tris);\n        if (e < bestE) {\n            bestE = e;\n            snapshot(flat, bestX, bestY);\n        }\n        if (maxDelta < tol)\n            break;\n    }\n    restore(flat, bestX, bestY);\n}\nfunction snapshot(flat, x, y) {\n    for (let i = 0; i < flat.length; i++) {\n        if (flat[i] == undefined) {\n            continue;\n        }\n        x[i] = flat[i].x;\n        y[i] = flat[i].y;\n    }\n}\nfunction restore(flat, x, y) {\n    for (let i = 0; i < flat.length; i++) {\n        flat[i] = { x: x[i], y: y[i] };\n    }\n}\nfunction localStep(flat, tris, triW, cos, sin) {\n    tris.forEach((tri, t) => {\n        const { ids, xs } = tri;\n        let num = 0;\n        let den = 0;\n        for (let e = 0; e < 3; e++) {\n            const p = e;\n            const q = (e + 1) % 3;\n            const w = triW[t * 3 + e];\n            const dxc = xs[q].x - xs[p].x;\n            const dyc = xs[q].y - xs[p].y;\n            const dqx = flat[ids[q]].x - flat[ids[p]].x;\n            const dqy = flat[ids[q]].y - flat[ids[p]].y;\n            num += w * (dqy * dxc - dqx * dyc);\n            den += w * (dqx * dxc + dqy * dyc);\n        }\n        const th = Math.atan2(num, den);\n        cos[t] = Math.cos(th);\n        sin[t] = Math.sin(th);\n    });\n}\nfunction gaussSeidel(flat, adj, cos, sin, nv) {\n    let maxDelta = 0;\n    for (let sweep = 0; sweep < 3; sweep++) {\n        for (let i = 1; i < nv; i++) {\n            let diag = 0;\n            let sx = 0;\n            let sy = 0;\n            for (const e of adj[i]) {\n                diag += e.w;\n                sx += e.w * (flat[e.j].x + cos[e.t] * e.dx - sin[e.t] * e.dy);\n                sy += e.w * (flat[e.j].y + sin[e.t] * e.dx + cos[e.t] * e.dy);\n            }\n            if (diag <= 0)\n                continue;\n            const nx = sx / diag;\n            const ny = sy / diag;\n            const d = Math.hypot(nx - flat[i].x, ny - flat[i].y);\n            if (d > maxDelta)\n                maxDelta = d;\n            flat[i] = { x: nx, y: ny };\n        }\n    }\n    return maxDelta;\n}\nfunction factorSystem(adj, nv) {\n    const n = nv - 1;\n    if (n <= 0)\n        return null;\n    let b = 0;\n    for (let i = 1; i < nv; i++) {\n        for (const e of adj[i]) {\n            if (e.j === 0)\n                continue;\n            const d = Math.abs(i - e.j);\n            if (d > b)\n                b = d;\n        }\n    }\n    if (b >= n)\n        return null;\n    const a = new Float64Array(n * (b + 1));\n    for (let i = 1; i < nv; i++) {\n        const r = i - 1;\n        for (const e of adj[i]) {\n            a[r * (b + 1)] += e.w;\n            if (e.j === 0)\n                continue;\n            const c = e.j - 1;\n            if (c < r)\n                a[r * (b + 1) + (r - c)] -= e.w;\n        }\n    }\n    const envelope = new Int32Array(n);\n    for (let r = 0; r < n; r++) {\n        const top = Math.min(b, r);\n        for (let k = top; k >= 1; k--) {\n            if (a[r * (b + 1) + k] !== 0) {\n                envelope[r] = k;\n                break;\n            }\n        }\n    }\n    for (let i = 0; i < n; i++) {\n        const wi = envelope[i];\n        for (let k = wi; k >= 1; k--) {\n            const j = i - k;\n            if (j < 0)\n                continue;\n            let s = a[i * (b + 1) + k];\n            const top = Math.min(wi - k, envelope[j]);\n            for (let m = 1; m <= top; m++) {\n                s -= a[i * (b + 1) + (k + m)] * a[j * (b + 1) + m];\n            }\n            a[i * (b + 1) + k] = s / a[j * (b + 1)];\n        }\n        let d = a[i * (b + 1)];\n        for (let k = 1; k <= wi; k++) {\n            d -= a[i * (b + 1) + k] * a[i * (b + 1) + k];\n        }\n        if (!(d > 0))\n            return null;\n        a[i * (b + 1)] = Math.sqrt(d);\n    }\n    return { n, b, a, envelope };\n}\nfunction solveBanded(m, rx, ry) {\n    const { n, b, a, envelope } = m;\n    const w = b + 1;\n    for (let i = 0; i < n; i++) {\n        let sx = rx[i];\n        let sy = ry[i];\n        const wi = envelope[i];\n        for (let k = 1; k <= wi; k++) {\n            const v = a[i * w + k];\n            sx -= v * rx[i - k];\n            sy -= v * ry[i - k];\n        }\n        const d = a[i * w];\n        rx[i] = sx / d;\n        ry[i] = sy / d;\n    }\n    for (let i = n - 1; i >= 0; i--) {\n        const d = a[i * w];\n        const vx = rx[i] / d;\n        const vy = ry[i] / d;\n        rx[i] = vx;\n        ry[i] = vy;\n        const wi = envelope[i];\n        for (let k = 1; k <= wi; k++) {\n            const v = a[i * w + k];\n            rx[i - k] -= v * vx;\n            ry[i - k] -= v * vy;\n        }\n    }\n}\nfunction intrinsicScale(tris) {\n    let area = 0;\n    for (const tri of tris)\n        area += tri.area;\n    return area > 0 ? Math.sqrt(area) : 1;\n}\nexport function buildTris(mesh) {\n    const out = [];\n    for (const [a, b, c] of mesh.tris) {\n        const A = mesh.positions[a];\n        const B = mesh.positions[b];\n        const C = mesh.positions[c];\n        const ux = B.x - A.x, uy = B.y - A.y, uz = B.z - A.z;\n        const vx = C.x - A.x, vy = C.y - A.y, vz = C.z - A.z;\n        const cx = uy * vz - uz * vy;\n        const cy = uz * vx - ux * vz;\n        const cz = ux * vy - uy * vx;\n        const cross = Math.hypot(cx, cy, cz);\n        const la = Math.hypot(ux, uy, uz);\n        const lb = Math.hypot(vx, vy, vz);\n        if (la === 0 || lb === 0)\n            continue;\n        const cos = Math.max(-1, Math.min(1, (ux * vx + uy * vy + uz * vz) / (la * lb)));\n        out.push({\n            ids: [a, b, c],\n            xs: [\n                { x: 0, y: 0 },\n                { x: la, y: 0 },\n                { x: lb * cos, y: lb * Math.sqrt(Math.max(0, 1 - cos * cos)) },\n            ],\n            area: cross / 2,\n        });\n    }\n    return out;\n}\nexport function energyOf(flat, tris) {\n    let e = 0;\n    let area = 0;\n    for (const tri of tris) {\n        const { ids, xs } = tri;\n        for (let k = 0; k < 3; k++) {\n            const p = k;\n            const q = (k + 1) % 3;\n            const lc = Math.hypot(xs[q].x - xs[p].x, xs[q].y - xs[p].y);\n            const lq = Math.hypot(flat[ids[q]].x - flat[ids[p]].x, flat[ids[q]].y - flat[ids[p]].y);\n            e += (lq - lc) * (lq - lc);\n        }\n        area += tri.area;\n    }\n    return area > 0 ? e / area : 0;\n}\nexport function placeByUnfolding(mesh, tris) {\n    const nv = mesh.positions.length;\n    const flat = new Array(nv);\n    const placed = new Array(nv).fill(false);\n    const incident = [];\n    for (let i = 0; i < nv; i++)\n        incident.push([]);\n    tris.forEach((tri, t) => {\n        for (const id of tri.ids)\n            incident[id].push(t);\n    });\n    const seed = tris[0];\n    seed.ids.forEach((id, k) => {\n        flat[id] = { x: seed.xs[k].x, y: seed.xs[k].y };\n        placed[id] = true;\n    });\n    const queue = seed.ids.flatMap((id) => incident[id]);\n    for (let head = 0; head < queue.length; head++) {\n        const tri = tris[queue[head]];\n        const open = tri.ids.filter((i) => !placed[i]);\n        if (open.length !== 1)\n            continue;\n        const v = open[0];\n        const p = placeFrom(bestHinge(v, incident[v], tris, placed, tri), v, flat, placed);\n        if (!p)\n            continue;\n        flat[v] = p;\n        placed[v] = true;\n        for (const t of incident[v])\n            queue.push(t);\n    }\n    for (let i = 0; i < nv; i++) {\n        if (!placed[i])\n            console.warn(`unroll: vertex ${i} of ${nv} is not connected to the mesh`);\n    }\n    return flat;\n}\nfunction bestHinge(v, candidates, tris, placed, fallback) {\n    let best = fallback;\n    let bestScore = -Infinity;\n    for (const t of candidates) {\n        const tri = tris[t];\n        const k = tri.ids.indexOf(v);\n        if (k < 0)\n            continue;\n        const i = (k + 1) % 3;\n        const j = (k + 2) % 3;\n        if (!placed[tri.ids[i]] || !placed[tri.ids[j]])\n            continue;\n        const edge = sq(tri.xs[j].x - tri.xs[i].x, tri.xs[j].y - tri.xs[i].y);\n        const reach = sq(tri.xs[k].x - tri.xs[i].x, tri.xs[k].y - tri.xs[i].y);\n        const score = reach > 0 ? edge / reach : Infinity;\n        if (score > bestScore) {\n            bestScore = score;\n            best = tri;\n        }\n    }\n    return best;\n}\nfunction sq(dx, dy) {\n    return dx * dx + dy * dy;\n}\nfunction placeFrom(tri, v, flat, placed) {\n    const { ids, xs } = tri;\n    const k = ids.indexOf(v);\n    const i = (k + 1) % 3;\n    const j = (k + 2) % 3;\n    if (!placed[ids[i]] || !placed[ids[j]])\n        return null;\n    const dx = xs[j].x - xs[i].x;\n    const dy = xs[j].y - xs[i].y;\n    const den = dx * dx + dy * dy;\n    if (den < 1e-12)\n        return null;\n    const ex = flat[ids[j]].x - flat[ids[i]].x;\n    const ey = flat[ids[j]].y - flat[ids[i]].y;\n    const sr = (ex * dx + ey * dy) / den;\n    const si = (ey * dx - ex * dy) / den;\n    const norm = Math.hypot(sr, si);\n    if (norm < 1e-12)\n        return null;\n    const cr = sr / norm;\n    const ci = si / norm;\n    const vx = xs[k].x - xs[i].x;\n    const vy = xs[k].y - xs[i].y;\n    return {\n        x: flat[ids[i]].x + cr * vx - ci * vy,\n        y: flat[ids[i]].y + ci * vx + cr * vy,\n    };\n}\nexport function canonicalise(flat) {\n    if (flat.length === 0)\n        return flat;\n    let cx = 0;\n    let cy = 0;\n    for (const p of flat) {\n        cx += p.x;\n        cy += p.y;\n    }\n    cx /= flat.length;\n    cy /= flat.length;\n    let sxx = 0;\n    let sxy = 0;\n    let syy = 0;\n    for (const p of flat) {\n        const dx = p.x - cx;\n        const dy = p.y - cy;\n        sxx += dx * dx;\n        sxy += dx * dy;\n        syy += dy * dy;\n    }\n    const ang = 0.5 * Math.atan2(2 * sxy, sxx - syy);\n    const co = Math.cos(-ang);\n    const si = Math.sin(-ang);\n    let minX = Infinity;\n    let minY = Infinity;\n    const out = flat.map((p) => {\n        const dx = p.x - cx;\n        const dy = p.y - cy;\n        const q = { x: co * dx - si * dy, y: si * dx + co * dy };\n        if (q.x < minX)\n            minX = q.x;\n        if (q.y < minY)\n            minY = q.y;\n        return q;\n    });\n    for (const q of out) {\n        q.x -= minX;\n        q.y -= minY;\n    }\n    return out;\n}\nexport function boundaryEdges(mesh) {\n    const used = new Map();\n    const key = (a, b) => (a < b ? `${a}-${b}` : `${b}-${a}`);\n    for (const [a, b, c] of mesh.tris) {\n        for (const [u, v] of [\n            [a, b],\n            [b, c],\n            [c, a],\n        ]) {\n            used.set(key(u, v), (used.get(key(u, v)) ?? 0) + 1);\n        }\n    }\n    const out = [];\n    for (const [a, b, c] of mesh.tris) {\n        for (const [u, v] of [\n            [a, b],\n            [b, c],\n            [c, a],\n        ]) {\n            if (used.get(key(u, v)) === 1)\n                out.push([u, v]);\n        }\n    }\n    return out;\n}\nexport function chainEdges(edges) {\n    const next = new Map();\n    const hasPredecessor = new Set();\n    for (const [u, v] of edges) {\n        next.set(u, v);\n        hasPredecessor.add(v);\n    }\n    const chains = [];\n    const visited = new Set();\n    const walk = (seed) => {\n        const chain = [seed];\n        visited.add(seed);\n        let cur = seed;\n        while (true) {\n            const to = next.get(cur);\n            if (to === undefined || to === seed)\n                break;\n            if (visited.has(to))\n                break;\n            chain.push(to);\n            visited.add(to);\n            cur = to;\n        }\n        if (next.get(cur) === seed && chain.length > 2)\n            chain.push(seed);\n        if (chain.length > 1)\n            chains.push(chain);\n    };\n    for (const [u] of next)\n        if (!hasPredecessor.has(u))\n            walk(u);\n    for (const [u] of next)\n        if (!visited.has(u))\n            walk(u);\n    return chains;\n}\nexport function boundaryLoop(mesh) {\n    const chains = chainEdges(boundaryEdges(mesh));\n    if (chains.length === 0)\n        return [];\n    return chains.map((loop) => {\n        return loop[0] === loop[loop.length - 1] ? loop.slice(0, -1) : loop;\n    });\n}\nexport function measureDistortion(mesh, flat) {\n    let squared = 0;\n    let count = 0;\n    let edgeMax = 0;\n    let area3d = 0;\n    let area2d = 0;\n    const triStrains = [];\n    for (const [a, b, c] of mesh.tris) {\n        const local = flattenTriangle(mesh, a, b, c);\n        for (const [u, v] of [\n            [a, b],\n            [b, c],\n            [c, a],\n        ]) {\n            const length3d = distance3d(mesh, u, v);\n            if (length3d === 0)\n                continue;\n            const p = flat.positions[u];\n            const q = flat.positions[v];\n            const ratio = Math.hypot(q.x - p.x, q.y - p.y) / length3d - 1;\n            squared += ratio * ratio;\n            count += 1;\n            edgeMax = Math.max(edgeMax, Math.abs(ratio));\n        }\n        area3d += local.area;\n        area2d += Math.abs(signedArea(flat.positions[a], flat.positions[b], flat.positions[c]));\n        triStrains.push(principalStrains(local, flat, a, b, c));\n    }\n    return {\n        edgeRms: count > 0 ? Math.sqrt(squared / count) : 0,\n        edgeMax,\n        areaRatio: area3d > 0 ? area2d / area3d : 1,\n        triStrains,\n    };\n}\nfunction flattenTriangle(mesh, a, b, c) {\n    const A = mesh.positions[a];\n    const B = mesh.positions[b];\n    const C = mesh.positions[c];\n    const ex = B.x - A.x, ey = B.y - A.y, ez = B.z - A.z;\n    const fx = C.x - A.x, fy = C.y - A.y, fz = C.z - A.z;\n    const ux = Math.hypot(ex, ey, ez);\n    const dot = ux > 0 ? (ex * fx + ey * fy + ez * fz) / ux : 0;\n    const nx = ey * fz - ez * fy;\n    const ny = ez * fx - ex * fz;\n    const nz = ex * fy - ey * fx;\n    const cross = Math.hypot(nx, ny, nz);\n    return { ux, vx: dot, vy: ux > 0 ? cross / ux : 0, area: cross / 2 };\n}\nfunction principalStrains(local, flat, a, b, c) {\n    const det = local.ux * local.vy;\n    if (det === 0)\n        return { e1: 0, e2: 0 };\n    const A = flat.positions[a];\n    const B = flat.positions[b];\n    const C = flat.positions[c];\n    const qx1 = B.x - A.x, qy1 = B.y - A.y;\n    const qx2 = C.x - A.x, qy2 = C.y - A.y;\n    const i00 = local.vy / det;\n    const i01 = -local.vx / det;\n    const i11 = local.ux / det;\n    const j00 = qx1 * i00;\n    const j01 = qx1 * i01 + qx2 * i11;\n    const j10 = qy1 * i00;\n    const j11 = qy1 * i01 + qy2 * i11;\n    const e = (j00 + j11) / 2;\n    const f = (j00 - j11) / 2;\n    const g = (j10 + j01) / 2;\n    const h = (j10 - j01) / 2;\n    const q = Math.hypot(e, h);\n    const r = Math.hypot(f, g);\n    const sigma1 = q + r;\n    const sigma2 = Math.abs(q - r);\n    // sigma1 >= sigma2, so e1 (tension) is always >= e2 (compression)\n    return { e1: sigma1 - 1, e2: sigma2 - 1 };\n}\nfunction signedArea(a, b, c) {\n    return ((b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x)) / 2;\n}\nfunction distance3d(mesh, u, v) {\n    const p = mesh.positions[u];\n    const q = mesh.positions[v];\n    return Math.hypot(q.x - p.x, q.y - p.y, q.z - p.z);\n}\n"
        },
        {
          "atomType": "Rectangle",
          "x": 0.1,
          "y": 0.5,
          "uniqueID": "id-136929",
          "ioValues": [
            {
              "name": "x length",
              "ioValue": 10
            },
            {
              "name": "y length",
              "ioValue": 10
            }
          ]
        },
        {
          "atomType": "Rotate",
          "x": 0.3,
          "y": 0.3,
          "uniqueID": "id-136933",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "x-axis degrees",
              "ioValue": 0
            },
            {
              "name": "y-axis degrees",
              "ioValue": 0
            },
            {
              "name": "z-axis degrees",
              "ioValue": 45
            }
          ]
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.5,
          "y": 0.3,
          "uniqueID": "id-136945",
          "name": "crv.VisGaussianCurvature",
          "ioValues": [
            {
              "name": "faces",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "curvature_scale",
              "ioValue": 100
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-136941",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Code",
              "x": 0.5,
              "y": 0.5,
              "uniqueID": "id-136942",
              "ioValues": [
                {
                  "name": "surface",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "curvatureScale",
                  "ioValue": 100
                }
              ],
              "interpreterVersion": 1,
              "code": "/**\n * Colours selected faces of the input assembly by discrete Gaussian\n * curvature: red for concave (negative curvature), blue for convex\n * (positive curvature), white for flat/neutral, darker as curvature grows\n * more severe.\n *\n * Uses whichever faces are selected on `surface` (via a faceselect input);\n * if none are selected, every face of the assembly is coloured.\n *\n *   curvatureScale — absolute curvature magnitude (1/length², in the\n *                    model's own units) at which a colour fully saturates.\n *                    Tune per model scale: small parts with tight fillets\n *                    need a larger value, large gently-curved parts need a\n *                    smaller one.\n */\n// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(surface: Assembly, curvatureScale: number = 0.01): Assembly[] {\n  if (!surface) {\n    throw new Error(\"Gaussian curvature: missing required geometry input.\");\n  }\n  if (!Number.isFinite(curvatureScale) || curvatureScale <= 0) {\n    throw new Error(\n      \"Gaussian curvature: curvatureScale must be a positive number.\",\n    );\n  }\n\n  const noneSelected = surface.getSelectedFaces().length === 0;\n  const facesToProcess: replicad.Face[] = [];\n  // eslint-disable-next-line @typescript-eslint/no-explicit-any\n  (surface as any).onLeafs((leaf: any) => {\n    if (leaf.geometry instanceof replicad.Drawing) {\n      console.warn(\"Gaussian curvature: drawings have no faces, skipping\");\n      return leaf;\n    }\n    if (noneSelected) {\n      facesToProcess.push(\n        ...(leaf.geometry instanceof replicad.Face\n          ? [leaf.geometry]\n          : leaf.geometry.faces),\n      );\n    } else if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n      facesToProcess.push(\n        ...leaf.selection.faces.map((idx: number) => leaf.geometry.faces[idx]),\n      );\n    }\n    return leaf;\n  });\n\n  if (facesToProcess.length === 0) {\n    throw new Error(\"Gaussian curvature: input has no faces to visualize.\");\n  }\n\n  return facesToProcess.map(\n    (face) =>\n      new Assembly({\n        geometry: face,\n        color: \"#5B9BD5\",\n        tags: [\"heatmap:gaussian-curvature\"],\n        metadata: {\n          meshOverride: buildCurvatureMeshOverride(face, curvatureScale),\n        },\n      }),\n  );\n}\n\n// ─── meshOverride baking ──────────────────────────────────────────────────\n//\n// Tessellates the face at a fixed, coarse tolerance, then computes discrete\n// Gaussian curvature at every mesh vertex via the angle-defect method: for\n// each vertex, sum the interior angles of its adjacent triangles and compare\n// against 2π (or π on the mesh boundary); the deficit divided by the local\n// (mixed/Voronoi) area is the curvature estimate.\n//\n// Colour is baked flat per triangle (averaging its 3 corner curvatures) and\n// applied to a de-duplicated \"triangle soup\" copy of the mesh — each\n// triangle gets its own unshared vertices — so the renderer's per-vertex\n// colour interpolation can't blend two different triangles' colours across\n// a shared corner and produce a gradient within a single triangle.\n\nfunction buildCurvatureMeshOverride(\n  // eslint-disable-next-line @typescript-eslint/no-explicit-any\n  face: any,\n  curvatureScale: number,\n) {\n  const faces = face.mesh({ tolerance: 0.1, angularTolerance: 0.3 });\n  const edges = face.meshEdges({ tolerance: 0.1, angularTolerance: 0.3 });\n\n  const { vertices, triangles } = faces;\n  const numVerts = vertices.length / 3;\n  const angleSum = new Float64Array(numVerts);\n  const areaSum = new Float64Array(numVerts);\n  const edgeTriCount = new Map<string, number>();\n  const neighbors: Set<number>[] = Array.from(\n    { length: numVerts },\n    () => new Set(),\n  );\n\n  const px = (i: number) => vertices[i * 3];\n  const py = (i: number) => vertices[i * 3 + 1];\n  const pz = (i: number) => vertices[i * 3 + 2];\n\n  const angleAt = (a: number, b: number, c: number): number => {\n    const abx = px(b) - px(a),\n      aby = py(b) - py(a),\n      abz = pz(b) - pz(a);\n    const acx = px(c) - px(a),\n      acy = py(c) - py(a),\n      acz = pz(c) - pz(a);\n    const abLen = Math.hypot(abx, aby, abz);\n    const acLen = Math.hypot(acx, acy, acz);\n    if (abLen === 0 || acLen === 0) return 0;\n    const cos = (abx * acx + aby * acy + abz * acz) / (abLen * acLen);\n    return Math.acos(Math.min(1, Math.max(-1, cos)));\n  };\n\n  const edgeKey = (i: number, j: number) => (i < j ? `${i}_${j}` : `${j}_${i}`);\n\n  const numTriangles = triangles.length / 3;\n  for (let t = 0; t < numTriangles; t++) {\n    const i0 = triangles[t * 3];\n    const i1 = triangles[t * 3 + 1];\n    const i2 = triangles[t * 3 + 2];\n\n    const abx = px(i1) - px(i0),\n      aby = py(i1) - py(i0),\n      abz = pz(i1) - pz(i0);\n    const acx = px(i2) - px(i0),\n      acy = py(i2) - py(i0),\n      acz = pz(i2) - pz(i0);\n    const cx = aby * acz - abz * acy;\n    const cy = abz * acx - abx * acz;\n    const cz = abx * acy - aby * acx;\n    const area = 0.5 * Math.hypot(cx, cy, cz);\n\n    angleSum[i0] += angleAt(i0, i1, i2);\n    angleSum[i1] += angleAt(i1, i2, i0);\n    angleSum[i2] += angleAt(i2, i0, i1);\n    areaSum[i0] += area / 3;\n    areaSum[i1] += area / 3;\n    areaSum[i2] += area / 3;\n\n    for (const [a, b] of [\n      [i0, i1],\n      [i1, i2],\n      [i2, i0],\n    ] as const) {\n      const key = edgeKey(a, b);\n      edgeTriCount.set(key, (edgeTriCount.get(key) ?? 0) + 1);\n      neighbors[a].add(b);\n      neighbors[b].add(a);\n    }\n  }\n\n  // Vertices touched by an edge that borders only one triangle sit on the\n  // face's trimmed boundary. The angle-defect formula only measures\n  // curvature correctly for vertices fully surrounded by triangles (a full\n  // 2π turn); at a boundary vertex the angle sum is just the mesh's local\n  // corner angle, which has nothing to do with surface curvature (e.g. a\n  // flat rectangular face's corners would read as sharply convex). Instead,\n  // boundary vertices inherit curvature from their interior neighbors, and\n  // default to 0 (flat) when the face has no interior vertices at all — as\n  // happens for a coarsely-tessellated flat face like a cube side.\n  const isBoundary = new Uint8Array(numVerts);\n  for (const [key, count] of edgeTriCount) {\n    if (count === 1) {\n      const [a, b] = key.split(\"_\").map(Number);\n      isBoundary[a] = 1;\n      isBoundary[b] = 1;\n    }\n  }\n\n  const k = new Float64Array(numVerts);\n  for (let v = 0; v < numVerts; v++) {\n    if (isBoundary[v]) continue;\n    const area = areaSum[v];\n    k[v] = area > 0 ? (2 * Math.PI - angleSum[v]) / area : 0;\n  }\n  for (let v = 0; v < numVerts; v++) {\n    if (!isBoundary[v]) continue;\n    let sum = 0;\n    let count = 0;\n    for (const n of neighbors[v]) {\n      if (!isBoundary[n]) {\n        sum += k[n];\n        count++;\n      }\n    }\n    k[v] = count > 0 ? sum / count : 0;\n  }\n\n  const [r0, g0, b0] = hexToRgb(\"#ffffff\");\n  const [rConcave, gConcave, bConcave] = hexToRgb(\"#ff0000\");\n  const [rConvex, gConvex, bConvex] = hexToRgb(\"#0000ff\");\n\n  const curvatureToColor = (curvature: number): [number, number, number] => {\n    let t = curvature / curvatureScale;\n    if (t < -1) t = -1;\n    else if (t > 1) t = 1;\n    const mag = Math.abs(t);\n    const [rBase, gBase, bBase] =\n      t < 0 ? [rConcave, gConcave, bConcave] : [rConvex, gConvex, bConvex];\n    const darken = 1 - 0.4 * mag;\n    return [\n      (r0 + (rBase - r0) * mag) * darken,\n      (g0 + (gBase - g0) * mag) * darken,\n      (b0 + (bBase - b0) * mag) * darken,\n    ];\n  };\n\n  const { normals } = faces;\n  const hasNormals =\n    Array.isArray(normals) && normals.length === vertices.length;\n  const flatVertices: number[] = new Array(numTriangles * 9);\n  const flatNormals: number[] | undefined = hasNormals\n    ? new Array(numTriangles * 9)\n    : undefined;\n  const flatTriangles: number[] = new Array(numTriangles * 3);\n  const vertexColors: number[] = new Array(numTriangles * 9);\n\n  for (let t = 0; t < numTriangles; t++) {\n    const corners = [\n      triangles[t * 3],\n      triangles[t * 3 + 1],\n      triangles[t * 3 + 2],\n    ];\n    const [r, g, b] = curvatureToColor(\n      (k[corners[0]] + k[corners[1]] + k[corners[2]]) / 3,\n    );\n    for (let c = 0; c < 3; c++) {\n      const src = corners[c];\n      const dst = t * 3 + c;\n      flatVertices[dst * 3] = vertices[src * 3];\n      flatVertices[dst * 3 + 1] = vertices[src * 3 + 1];\n      flatVertices[dst * 3 + 2] = vertices[src * 3 + 2];\n      if (flatNormals) {\n        flatNormals[dst * 3] = normals[src * 3];\n        flatNormals[dst * 3 + 1] = normals[src * 3 + 1];\n        flatNormals[dst * 3 + 2] = normals[src * 3 + 2];\n      }\n      flatTriangles[dst] = dst;\n      vertexColors[dst * 3] = r;\n      vertexColors[dst * 3 + 1] = g;\n      vertexColors[dst * 3 + 2] = b;\n    }\n  }\n\n  return {\n    faces: {\n      ...faces,\n      vertices: flatVertices,\n      triangles: flatTriangles,\n      ...(flatNormals ? { normals: flatNormals } : {}),\n    },\n    edges,\n    // Plain array (not Float32Array) so metadata.meshOverride survives the\n    // JSON round-trip in cacheAssemblyStructure/getAssembly. Renderer wraps\n    // back into a typed array when constructing the BufferAttribute.\n    vertexColors,\n  };\n}\n\nfunction hexToRgb(hex: string): [number, number, number] {\n  const h = hex.replace(\"#\", \"\");\n  return [\n    parseInt(h.slice(0, 2), 16) / 255,\n    parseInt(h.slice(2, 4), 16) / 255,\n    parseInt(h.slice(4, 6), 16) / 255,\n  ];\n}\n",
              "compiledCode": "\"use strict\";\n/**\n * Colours selected faces of the input assembly by discrete Gaussian\n * curvature: red for concave (negative curvature), blue for convex\n * (positive curvature), white for flat/neutral, darker as curvature grows\n * more severe.\n *\n * Uses whichever faces are selected on `surface` (via a faceselect input);\n * if none are selected, every face of the assembly is coloured.\n *\n *   curvatureScale — absolute curvature magnitude (1/length², in the\n *                    model's own units) at which a colour fully saturates.\n *                    Tune per model scale: small parts with tight fillets\n *                    need a larger value, large gently-curved parts need a\n *                    smaller one.\n */\n// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(surface, curvatureScale = 0.01) {\n    if (!surface) {\n        throw new Error(\"Gaussian curvature: missing required geometry input.\");\n    }\n    if (!Number.isFinite(curvatureScale) || curvatureScale <= 0) {\n        throw new Error(\"Gaussian curvature: curvatureScale must be a positive number.\");\n    }\n    const noneSelected = surface.getSelectedFaces().length === 0;\n    const facesToProcess = [];\n    // eslint-disable-next-line @typescript-eslint/no-explicit-any\n    surface.onLeafs((leaf) => {\n        if (leaf.geometry instanceof replicad.Drawing) {\n            console.warn(\"Gaussian curvature: drawings have no faces, skipping\");\n            return leaf;\n        }\n        if (noneSelected) {\n            facesToProcess.push(...(leaf.geometry instanceof replicad.Face\n                ? [leaf.geometry]\n                : leaf.geometry.faces));\n        }\n        else if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n            facesToProcess.push(...leaf.selection.faces.map((idx) => leaf.geometry.faces[idx]));\n        }\n        return leaf;\n    });\n    if (facesToProcess.length === 0) {\n        throw new Error(\"Gaussian curvature: input has no faces to visualize.\");\n    }\n    return facesToProcess.map((face) => new Assembly({\n        geometry: face,\n        color: \"#5B9BD5\",\n        tags: [\"heatmap:gaussian-curvature\"],\n        metadata: {\n            meshOverride: buildCurvatureMeshOverride(face, curvatureScale),\n        },\n    }));\n}\n// ─── meshOverride baking ──────────────────────────────────────────────────\n//\n// Tessellates the face at a fixed, coarse tolerance, then computes discrete\n// Gaussian curvature at every mesh vertex via the angle-defect method: for\n// each vertex, sum the interior angles of its adjacent triangles and compare\n// against 2π (or π on the mesh boundary); the deficit divided by the local\n// (mixed/Voronoi) area is the curvature estimate.\n//\n// Colour is baked flat per triangle (averaging its 3 corner curvatures) and\n// applied to a de-duplicated \"triangle soup\" copy of the mesh — each\n// triangle gets its own unshared vertices — so the renderer's per-vertex\n// colour interpolation can't blend two different triangles' colours across\n// a shared corner and produce a gradient within a single triangle.\nfunction buildCurvatureMeshOverride(\n// eslint-disable-next-line @typescript-eslint/no-explicit-any\nface, curvatureScale) {\n    const faces = face.mesh({ tolerance: 0.1, angularTolerance: 0.3 });\n    const edges = face.meshEdges({ tolerance: 0.1, angularTolerance: 0.3 });\n    const { vertices, triangles } = faces;\n    const numVerts = vertices.length / 3;\n    const angleSum = new Float64Array(numVerts);\n    const areaSum = new Float64Array(numVerts);\n    const edgeTriCount = new Map();\n    const neighbors = Array.from({ length: numVerts }, () => new Set());\n    const px = (i) => vertices[i * 3];\n    const py = (i) => vertices[i * 3 + 1];\n    const pz = (i) => vertices[i * 3 + 2];\n    const angleAt = (a, b, c) => {\n        const abx = px(b) - px(a), aby = py(b) - py(a), abz = pz(b) - pz(a);\n        const acx = px(c) - px(a), acy = py(c) - py(a), acz = pz(c) - pz(a);\n        const abLen = Math.hypot(abx, aby, abz);\n        const acLen = Math.hypot(acx, acy, acz);\n        if (abLen === 0 || acLen === 0)\n            return 0;\n        const cos = (abx * acx + aby * acy + abz * acz) / (abLen * acLen);\n        return Math.acos(Math.min(1, Math.max(-1, cos)));\n    };\n    const edgeKey = (i, j) => (i < j ? `${i}_${j}` : `${j}_${i}`);\n    const numTriangles = triangles.length / 3;\n    for (let t = 0; t < numTriangles; t++) {\n        const i0 = triangles[t * 3];\n        const i1 = triangles[t * 3 + 1];\n        const i2 = triangles[t * 3 + 2];\n        const abx = px(i1) - px(i0), aby = py(i1) - py(i0), abz = pz(i1) - pz(i0);\n        const acx = px(i2) - px(i0), acy = py(i2) - py(i0), acz = pz(i2) - pz(i0);\n        const cx = aby * acz - abz * acy;\n        const cy = abz * acx - abx * acz;\n        const cz = abx * acy - aby * acx;\n        const area = 0.5 * Math.hypot(cx, cy, cz);\n        angleSum[i0] += angleAt(i0, i1, i2);\n        angleSum[i1] += angleAt(i1, i2, i0);\n        angleSum[i2] += angleAt(i2, i0, i1);\n        areaSum[i0] += area / 3;\n        areaSum[i1] += area / 3;\n        areaSum[i2] += area / 3;\n        for (const [a, b] of [\n            [i0, i1],\n            [i1, i2],\n            [i2, i0],\n        ]) {\n            const key = edgeKey(a, b);\n            edgeTriCount.set(key, (edgeTriCount.get(key) ?? 0) + 1);\n            neighbors[a].add(b);\n            neighbors[b].add(a);\n        }\n    }\n    // Vertices touched by an edge that borders only one triangle sit on the\n    // face's trimmed boundary. The angle-defect formula only measures\n    // curvature correctly for vertices fully surrounded by triangles (a full\n    // 2π turn); at a boundary vertex the angle sum is just the mesh's local\n    // corner angle, which has nothing to do with surface curvature (e.g. a\n    // flat rectangular face's corners would read as sharply convex). Instead,\n    // boundary vertices inherit curvature from their interior neighbors, and\n    // default to 0 (flat) when the face has no interior vertices at all — as\n    // happens for a coarsely-tessellated flat face like a cube side.\n    const isBoundary = new Uint8Array(numVerts);\n    for (const [key, count] of edgeTriCount) {\n        if (count === 1) {\n            const [a, b] = key.split(\"_\").map(Number);\n            isBoundary[a] = 1;\n            isBoundary[b] = 1;\n        }\n    }\n    const k = new Float64Array(numVerts);\n    for (let v = 0; v < numVerts; v++) {\n        if (isBoundary[v])\n            continue;\n        const area = areaSum[v];\n        k[v] = area > 0 ? (2 * Math.PI - angleSum[v]) / area : 0;\n    }\n    for (let v = 0; v < numVerts; v++) {\n        if (!isBoundary[v])\n            continue;\n        let sum = 0;\n        let count = 0;\n        for (const n of neighbors[v]) {\n            if (!isBoundary[n]) {\n                sum += k[n];\n                count++;\n            }\n        }\n        k[v] = count > 0 ? sum / count : 0;\n    }\n    const [r0, g0, b0] = hexToRgb(\"#ffffff\");\n    const [rConcave, gConcave, bConcave] = hexToRgb(\"#ff0000\");\n    const [rConvex, gConvex, bConvex] = hexToRgb(\"#0000ff\");\n    const curvatureToColor = (curvature) => {\n        let t = curvature / curvatureScale;\n        if (t < -1)\n            t = -1;\n        else if (t > 1)\n            t = 1;\n        const mag = Math.abs(t);\n        const [rBase, gBase, bBase] = t < 0 ? [rConcave, gConcave, bConcave] : [rConvex, gConvex, bConvex];\n        const darken = 1 - 0.4 * mag;\n        return [\n            (r0 + (rBase - r0) * mag) * darken,\n            (g0 + (gBase - g0) * mag) * darken,\n            (b0 + (bBase - b0) * mag) * darken,\n        ];\n    };\n    const { normals } = faces;\n    const hasNormals = Array.isArray(normals) && normals.length === vertices.length;\n    const flatVertices = new Array(numTriangles * 9);\n    const flatNormals = hasNormals\n        ? new Array(numTriangles * 9)\n        : undefined;\n    const flatTriangles = new Array(numTriangles * 3);\n    const vertexColors = new Array(numTriangles * 9);\n    for (let t = 0; t < numTriangles; t++) {\n        const corners = [\n            triangles[t * 3],\n            triangles[t * 3 + 1],\n            triangles[t * 3 + 2],\n        ];\n        const [r, g, b] = curvatureToColor((k[corners[0]] + k[corners[1]] + k[corners[2]]) / 3);\n        for (let c = 0; c < 3; c++) {\n            const src = corners[c];\n            const dst = t * 3 + c;\n            flatVertices[dst * 3] = vertices[src * 3];\n            flatVertices[dst * 3 + 1] = vertices[src * 3 + 1];\n            flatVertices[dst * 3 + 2] = vertices[src * 3 + 2];\n            if (flatNormals) {\n                flatNormals[dst * 3] = normals[src * 3];\n                flatNormals[dst * 3 + 1] = normals[src * 3 + 1];\n                flatNormals[dst * 3 + 2] = normals[src * 3 + 2];\n            }\n            flatTriangles[dst] = dst;\n            vertexColors[dst * 3] = r;\n            vertexColors[dst * 3 + 1] = g;\n            vertexColors[dst * 3 + 2] = b;\n        }\n    }\n    return {\n        faces: {\n            ...faces,\n            vertices: flatVertices,\n            triangles: flatTriangles,\n            ...(flatNormals ? { normals: flatNormals } : {}),\n        },\n        edges,\n        // Plain array (not Float32Array) so metadata.meshOverride survives the\n        // JSON round-trip in cacheAssemblyStructure/getAssembly. Renderer wraps\n        // back into a typed array when constructing the BufferAttribute.\n        vertexColors,\n    };\n}\nfunction hexToRgb(hex) {\n    const h = hex.replace(\"#\", \"\");\n    return [\n        parseInt(h.slice(0, 2), 16) / 255,\n        parseInt(h.slice(2, 4), 16) / 255,\n        parseInt(h.slice(4, 6), 16) / 255,\n    ];\n}\n"
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136943",
              "name": "faces",
              "type": "faceselect",
              "options": [],
              "selectedFaceData": {
                "0": [
                  0
                ]
              }
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136944",
              "name": "curvature_scale",
              "options": []
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-136942",
              "ap2ID": "id-136941"
            },
            {
              "ap2Name": "surface",
              "ap1ID": "id-136943",
              "ap2ID": "id-136942"
            },
            {
              "ap2Name": "curvatureScale",
              "ap1ID": "id-136944",
              "ap2ID": "id-136942"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "crv.VisGaussianCurvature",
            "dateCreated": "2026-08-12T00:02:56Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/tristan-huber/crv.VisGaussianCurvature/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/tristan-huber/crv.VisGaussianCurvature/master/README.md?sanitize=true",
            "yyyy": 2026,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/tristan-huber/crv.VisGaussianCurvature",
            "topics": [
              "abundance-tool"
            ],
            "pullRequests": [],
            "forks": 0,
            "dateModified": "2026-08-12T00:04:58Z",
            "ranking": 1,
            "searchField": "crv.visgaussiancurvature tristan-huber visualize gaussian curvature for selected faces (or default all faces)> red indicating concave, blue hyperbolic, white zero gaussian curvature.  abundance-tool",
            "owner": "tristan-huber",
            "description": "visualize gaussian curvature for selected faces (or default all faces)> Red indicating concave, blue hyperbolic, white zero gaussian curvature. ",
            "contentURL": "https://raw.githubusercontent.com/tristan-huber/crv.VisGaussianCurvature/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/tristan-huber/crv.VisGaussianCurvature/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "Unitless",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790368508690
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.1,
          "y": 0.7,
          "uniqueID": "id-136962",
          "name": "Sphere",
          "ioValues": [
            {
              "name": "Radius",
              "ioValue": 10
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-136958",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Code",
              "x": 0.5,
              "y": 0.4,
              "uniqueID": "id-136959",
              "ioValues": [
                {
                  "name": "radius",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ],
              "interpreterVersion": 0,
              "code": "//Inputs:[radius];\n\n//let shape = replicad.makeSphere(radius)\n\nconst newPlane = new replicad.Plane().pivot(0, 'Y');\n\n return { geometry: [replicad.makeSphere(radius)], tags: [],\n      color: '#A3CE5B',\n      bom:[],\n      plane: newPlane}\n\n\n    /**\n    To Use the Code Atom, enter your inputs to the input list a.e Inputs:[shape, height]\n    If your input is connected to another atom with a replicad geometry you can access its geometry by looking up its ID in your library. a.e library[Input1].geometry[0] \n    Use any replicad available methods to modify your geometry. Learn more about all of the available methods at \n    https://replicad.xyz/docs/introapp/UserGuide.html \n    Return a replicad object that includes geometry, color, tags and plane. \n\n\n    Example Code Atom:\n\n      Inputs:[shape, x];\n\n      let finalShape = library[shape].geometry[0].clone.translate[x,0,0]\n\n      return {geometry: finalShape, color: library[shape].color, plane: library[shape].plane, tags: library[shape].tags }\n\n      - See more examples at _______ \n\n\n    */\n"
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136960",
              "name": "Radius",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Constant",
              "x": 0.2,
              "y": 0.9,
              "uniqueID": "id-136961",
              "value": 10
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-136959",
              "ap2ID": "id-136958"
            },
            {
              "ap2Name": "radius",
              "ap1ID": "id-136960",
              "ap2ID": "id-136959"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "Sphere",
            "dateCreated": "2025-06-20T21:36:53Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/README.md?sanitize=true",
            "yyyy": 2025,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/BarbourSmith/Sphere",
            "topics": [],
            "pullRequests": [],
            "parentRepo": "alzatin/Sphere",
            "forks": 0,
            "dateModified": "2025-10-24T17:13:15Z",
            "ranking": 1,
            "searchField": "sphere barboursmith creates a sphere ",
            "owner": "BarbourSmith",
            "description": "Creates a sphere",
            "contentURL": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/BarbourSmith/Sphere/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "Inches",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790368569347
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.2,
          "y": 0.7,
          "uniqueID": "id-136984",
          "name": "Cross-Section",
          "ioValues": [
            {
              "name": "Offset",
              "ioValue": 0
            },
            {
              "name": "Angle X",
              "ioValue": 90
            },
            {
              "name": "Angle Z",
              "ioValue": 0
            },
            {
              "name": "Angle Y",
              "ioValue": 0
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-136973",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136974",
              "name": "Geometry",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136975",
              "name": "Offset",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.3,
              "uniqueID": "id-136976",
              "name": "Angle X",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.4,
              "uniqueID": "id-136977",
              "name": "Angle Z",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.5,
              "uniqueID": "id-136978",
              "name": "Angle Y",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Rectangle",
              "x": 0.2,
              "y": 0.4,
              "uniqueID": "id-136979",
              "ioValues": [
                {
                  "name": "x length",
                  "ioValue": 40000
                },
                {
                  "name": "y length",
                  "ioValue": 40000
                }
              ]
            },
            {
              "atomType": "Extrude",
              "x": 0.3,
              "y": 0.5,
              "uniqueID": "id-136980",
              "ioValues": [
                {
                  "name": "geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "height",
                  "ioValue": 4000
                }
              ]
            },
            {
              "atomType": "Move",
              "x": 0.3,
              "y": 0.7,
              "uniqueID": "id-136981",
              "ioValues": [
                {
                  "name": "geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "xDist",
                  "ioValue": 0
                },
                {
                  "name": "yDist",
                  "ioValue": 0
                },
                {
                  "name": "zDist",
                  "ioValue": 0
                }
              ]
            },
            {
              "atomType": "Rotate",
              "x": 0.4,
              "y": 0.8,
              "uniqueID": "id-136982",
              "ioValues": [
                {
                  "name": "geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "x-axis degrees",
                  "ioValue": 90
                },
                {
                  "name": "y-axis degrees",
                  "ioValue": 0
                },
                {
                  "name": "z-axis degrees",
                  "ioValue": 0
                }
              ]
            },
            {
              "atomType": "Difference",
              "x": 0.5,
              "y": 0.2,
              "uniqueID": "id-136983",
              "ioValues": [
                {
                  "name": "retain",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "remove",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "retain",
              "ap1ID": "id-136974",
              "ap2ID": "id-136983"
            },
            {
              "ap2Name": "zDist",
              "ap1ID": "id-136975",
              "ap2ID": "id-136981"
            },
            {
              "ap2Name": "x-axis degrees",
              "ap1ID": "id-136976",
              "ap2ID": "id-136982"
            },
            {
              "ap2Name": "z-axis degrees",
              "ap1ID": "id-136977",
              "ap2ID": "id-136982"
            },
            {
              "ap2Name": "y-axis degrees",
              "ap1ID": "id-136978",
              "ap2ID": "id-136982"
            },
            {
              "ap2Name": "geometry",
              "ap1ID": "id-136979",
              "ap2ID": "id-136980"
            },
            {
              "ap2Name": "geometry",
              "ap1ID": "id-136980",
              "ap2ID": "id-136981"
            },
            {
              "ap2Name": "geometry",
              "ap1ID": "id-136981",
              "ap2ID": "id-136982"
            },
            {
              "ap2Name": "remove",
              "ap1ID": "id-136982",
              "ap2ID": "id-136983"
            },
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-136983",
              "ap2ID": "id-136973"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "Cross-Section",
            "dateCreated": "2025-02-04T20:50:02Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/README.md?sanitize=true",
            "yyyy": 2025,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/BarbourSmith/Cross-Section",
            "topics": [],
            "pullRequests": [],
            "forks": 0,
            "dateModified": "2026-01-23T18:20:41Z",
            "ranking": 3,
            "searchField": "cross-section barboursmith takes a cross section of the input ",
            "owner": "BarbourSmith",
            "description": "Takes a cross section of the input",
            "contentURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "MM",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790368587170
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.4,
          "y": 0.7,
          "uniqueID": "id-137018",
          "name": "Cross-Section",
          "ioValues": [
            {
              "name": "Offset",
              "ioValue": 3
            },
            {
              "name": "Angle X",
              "ioValue": 270
            },
            {
              "name": "Angle Z",
              "ioValue": 0
            },
            {
              "name": "Angle Y",
              "ioValue": 0
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-137019",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-137020",
              "name": "Geometry",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-137021",
              "name": "Offset",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.3,
              "uniqueID": "id-137022",
              "name": "Angle X",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.4,
              "uniqueID": "id-137023",
              "name": "Angle Z",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.5,
              "uniqueID": "id-137024",
              "name": "Angle Y",
              "type": "number",
              "options": []
            },
            {
              "atomType": "Rectangle",
              "x": 0.2,
              "y": 0.4,
              "uniqueID": "id-137025",
              "ioValues": [
                {
                  "name": "x length",
                  "ioValue": 40000
                },
                {
                  "name": "y length",
                  "ioValue": 40000
                }
              ]
            },
            {
              "atomType": "Extrude",
              "x": 0.3,
              "y": 0.5,
              "uniqueID": "id-137026",
              "ioValues": [
                {
                  "name": "geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "height",
                  "ioValue": 4000
                }
              ]
            },
            {
              "atomType": "Move",
              "x": 0.3,
              "y": 0.7,
              "uniqueID": "id-137027",
              "ioValues": [
                {
                  "name": "geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "xDist",
                  "ioValue": 0
                },
                {
                  "name": "yDist",
                  "ioValue": 0
                },
                {
                  "name": "zDist",
                  "ioValue": 3
                }
              ]
            },
            {
              "atomType": "Rotate",
              "x": 0.4,
              "y": 0.8,
              "uniqueID": "id-137028",
              "ioValues": [
                {
                  "name": "geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "x-axis degrees",
                  "ioValue": 270
                },
                {
                  "name": "y-axis degrees",
                  "ioValue": 0
                },
                {
                  "name": "z-axis degrees",
                  "ioValue": 0
                }
              ]
            },
            {
              "atomType": "Difference",
              "x": 0.5,
              "y": 0.2,
              "uniqueID": "id-137029",
              "ioValues": [
                {
                  "name": "retain",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "remove",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "retain",
              "ap1ID": "id-137020",
              "ap2ID": "id-137029"
            },
            {
              "ap2Name": "zDist",
              "ap1ID": "id-137021",
              "ap2ID": "id-137027"
            },
            {
              "ap2Name": "x-axis degrees",
              "ap1ID": "id-137022",
              "ap2ID": "id-137028"
            },
            {
              "ap2Name": "z-axis degrees",
              "ap1ID": "id-137023",
              "ap2ID": "id-137028"
            },
            {
              "ap2Name": "y-axis degrees",
              "ap1ID": "id-137024",
              "ap2ID": "id-137028"
            },
            {
              "ap2Name": "geometry",
              "ap1ID": "id-137025",
              "ap2ID": "id-137026"
            },
            {
              "ap2Name": "geometry",
              "ap1ID": "id-137026",
              "ap2ID": "id-137027"
            },
            {
              "ap2Name": "geometry",
              "ap1ID": "id-137027",
              "ap2ID": "id-137028"
            },
            {
              "ap2Name": "remove",
              "ap1ID": "id-137028",
              "ap2ID": "id-137029"
            },
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-137029",
              "ap2ID": "id-137019"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "Cross-Section",
            "dateCreated": "2025-02-04T20:50:02Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/README.md?sanitize=true",
            "yyyy": 2025,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/BarbourSmith/Cross-Section",
            "topics": [],
            "pullRequests": [],
            "forks": 0,
            "dateModified": "2026-01-23T18:20:41Z",
            "ranking": 3,
            "searchField": "cross-section barboursmith takes a cross section of the input ",
            "owner": "BarbourSmith",
            "description": "Takes a cross section of the input",
            "contentURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/BarbourSmith/Cross-Section/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "MM",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790368587170
        },
        {
          "atomType": "Code",
          "x": 0.5,
          "y": 0.7,
          "uniqueID": "id-137064",
          "ioValues": [
            {
              "name": "surface",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "curvatureScale",
              "ioValue": 0.015
            }
          ],
          "interpreterVersion": 1,
          "code": "/**\n * Colours selected faces of the input assembly by discrete Gaussian\n * curvature: red for concave (negative curvature), blue for convex\n * (positive curvature), white for flat/neutral, darker as curvature grows\n * more severe.\n *\n * Uses whichever faces are selected on `surface` (via a faceselect input);\n * if none are selected, every face of the assembly is coloured.\n *\n *   curvatureScale — absolute curvature magnitude (1/length², in the\n *                    model's own units) at which a colour fully saturates.\n *                    Tune per model scale: small parts with tight fillets\n *                    need a larger value, large gently-curved parts need a\n *                    smaller one.\n */\n// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(surface: Assembly, curvatureScale: number = 0.01): Assembly[] {\n  if (!surface) {\n    throw new Error(\"Gaussian curvature: missing required geometry input.\");\n  }\n  if (!Number.isFinite(curvatureScale) || curvatureScale <= 0) {\n    throw new Error(\n      \"Gaussian curvature: curvatureScale must be a positive number.\",\n    );\n  }\n\n  const noneSelected = surface.getSelectedFaces().length === 0;\n  const facesToProcess: replicad.Face[] = [];\n  // eslint-disable-next-line @typescript-eslint/no-explicit-any\n  (surface as any).onLeafs((leaf: any) => {\n    if (leaf.geometry instanceof replicad.Drawing) {\n      console.warn(\"Gaussian curvature: drawings have no faces, skipping\");\n      return leaf;\n    }\n    if (noneSelected) {\n      facesToProcess.push(\n        ...(leaf.geometry instanceof replicad.Face\n          ? [leaf.geometry]\n          : leaf.geometry.faces),\n      );\n    } else if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n      facesToProcess.push(\n        ...leaf.selection.faces.map((idx: number) => leaf.geometry.faces[idx]),\n      );\n    }\n    return leaf;\n  });\n\n  if (facesToProcess.length === 0) {\n    throw new Error(\"Gaussian curvature: input has no faces to visualize.\");\n  }\n\n  return facesToProcess.map(\n    (face) =>\n      new Assembly({\n        geometry: face,\n        color: \"#5B9BD5\",\n        tags: [\"heatmap:gaussian-curvature\"],\n        metadata: {\n          meshOverride: buildCurvatureMeshOverride(face, curvatureScale),\n        },\n      }),\n  );\n}\n\n// ─── meshOverride baking ──────────────────────────────────────────────────\n//\n// Tessellates the face at a fixed, coarse tolerance, then computes discrete\n// Gaussian curvature at every mesh vertex via the angle-defect method: for\n// each vertex, sum the interior angles of its adjacent triangles and compare\n// against 2π (or π on the mesh boundary); the deficit divided by the local\n// (mixed/Voronoi) area is the curvature estimate.\n//\n// Colour is baked flat per triangle (averaging its 3 corner curvatures) and\n// applied to a de-duplicated \"triangle soup\" copy of the mesh — each\n// triangle gets its own unshared vertices — so the renderer's per-vertex\n// colour interpolation can't blend two different triangles' colours across\n// a shared corner and produce a gradient within a single triangle.\n\nfunction buildCurvatureMeshOverride(\n  // eslint-disable-next-line @typescript-eslint/no-explicit-any\n  face: any,\n  curvatureScale: number,\n) {\n  const faces = face.mesh({ tolerance: 0.1, angularTolerance: 0.3 });\n  const edges = face.meshEdges({ tolerance: 0.1, angularTolerance: 0.3 });\n\n  const { vertices, triangles } = faces;\n  const numVerts = vertices.length / 3;\n  const angleSum = new Float64Array(numVerts);\n  const areaSum = new Float64Array(numVerts);\n  const edgeTriCount = new Map<string, number>();\n  const neighbors: Set<number>[] = Array.from(\n    { length: numVerts },\n    () => new Set(),\n  );\n\n  const px = (i: number) => vertices[i * 3];\n  const py = (i: number) => vertices[i * 3 + 1];\n  const pz = (i: number) => vertices[i * 3 + 2];\n\n  const angleAt = (a: number, b: number, c: number): number => {\n    const abx = px(b) - px(a),\n      aby = py(b) - py(a),\n      abz = pz(b) - pz(a);\n    const acx = px(c) - px(a),\n      acy = py(c) - py(a),\n      acz = pz(c) - pz(a);\n    const abLen = Math.hypot(abx, aby, abz);\n    const acLen = Math.hypot(acx, acy, acz);\n    if (abLen === 0 || acLen === 0) return 0;\n    const cos = (abx * acx + aby * acy + abz * acz) / (abLen * acLen);\n    return Math.acos(Math.min(1, Math.max(-1, cos)));\n  };\n\n  const edgeKey = (i: number, j: number) => (i < j ? `${i}_${j}` : `${j}_${i}`);\n\n  const numTriangles = triangles.length / 3;\n  for (let t = 0; t < numTriangles; t++) {\n    const i0 = triangles[t * 3];\n    const i1 = triangles[t * 3 + 1];\n    const i2 = triangles[t * 3 + 2];\n\n    const abx = px(i1) - px(i0),\n      aby = py(i1) - py(i0),\n      abz = pz(i1) - pz(i0);\n    const acx = px(i2) - px(i0),\n      acy = py(i2) - py(i0),\n      acz = pz(i2) - pz(i0);\n    const cx = aby * acz - abz * acy;\n    const cy = abz * acx - abx * acz;\n    const cz = abx * acy - aby * acx;\n    const area = 0.5 * Math.hypot(cx, cy, cz);\n\n    angleSum[i0] += angleAt(i0, i1, i2);\n    angleSum[i1] += angleAt(i1, i2, i0);\n    angleSum[i2] += angleAt(i2, i0, i1);\n    areaSum[i0] += area / 3;\n    areaSum[i1] += area / 3;\n    areaSum[i2] += area / 3;\n\n    for (const [a, b] of [\n      [i0, i1],\n      [i1, i2],\n      [i2, i0],\n    ] as const) {\n      const key = edgeKey(a, b);\n      edgeTriCount.set(key, (edgeTriCount.get(key) ?? 0) + 1);\n      neighbors[a].add(b);\n      neighbors[b].add(a);\n    }\n  }\n\n  // Vertices touched by an edge that borders only one triangle sit on the\n  // face's trimmed boundary. The angle-defect formula only measures\n  // curvature correctly for vertices fully surrounded by triangles (a full\n  // 2π turn); at a boundary vertex the angle sum is just the mesh's local\n  // corner angle, which has nothing to do with surface curvature (e.g. a\n  // flat rectangular face's corners would read as sharply convex). Instead,\n  // boundary vertices inherit curvature from their interior neighbors, and\n  // default to 0 (flat) when the face has no interior vertices at all — as\n  // happens for a coarsely-tessellated flat face like a cube side.\n  const isBoundary = new Uint8Array(numVerts);\n  for (const [key, count] of edgeTriCount) {\n    if (count === 1) {\n      const [a, b] = key.split(\"_\").map(Number);\n      isBoundary[a] = 1;\n      isBoundary[b] = 1;\n    }\n  }\n\n  const k = new Float64Array(numVerts);\n  for (let v = 0; v < numVerts; v++) {\n    if (isBoundary[v]) continue;\n    const area = areaSum[v];\n    k[v] = area > 0 ? (2 * Math.PI - angleSum[v]) / area : 0;\n  }\n  for (let v = 0; v < numVerts; v++) {\n    if (!isBoundary[v]) continue;\n    let sum = 0;\n    let count = 0;\n    for (const n of neighbors[v]) {\n      if (!isBoundary[n]) {\n        sum += k[n];\n        count++;\n      }\n    }\n    k[v] = count > 0 ? sum / count : 0;\n  }\n\n  const [r0, g0, b0] = hexToRgb(\"#ffffff\");\n  const [rConcave, gConcave, bConcave] = hexToRgb(\"#ff0000\");\n  const [rConvex, gConvex, bConvex] = hexToRgb(\"#0000ff\");\n\n  const curvatureToColor = (curvature: number): [number, number, number] => {\n    let t = curvature / curvatureScale;\n    if (t < -1) t = -1;\n    else if (t > 1) t = 1;\n    const mag = Math.abs(t);\n    const [rBase, gBase, bBase] =\n      t < 0 ? [rConcave, gConcave, bConcave] : [rConvex, gConvex, bConvex];\n    const darken = 1 - 0.4 * mag;\n    return [\n      (r0 + (rBase - r0) * mag) * darken,\n      (g0 + (gBase - g0) * mag) * darken,\n      (b0 + (bBase - b0) * mag) * darken,\n    ];\n  };\n\n  const { normals } = faces;\n  const hasNormals =\n    Array.isArray(normals) && normals.length === vertices.length;\n  const flatVertices: number[] = new Array(numTriangles * 9);\n  const flatNormals: number[] | undefined = hasNormals\n    ? new Array(numTriangles * 9)\n    : undefined;\n  const flatTriangles: number[] = new Array(numTriangles * 3);\n  const vertexColors: number[] = new Array(numTriangles * 9);\n\n  for (let t = 0; t < numTriangles; t++) {\n    const corners = [\n      triangles[t * 3],\n      triangles[t * 3 + 1],\n      triangles[t * 3 + 2],\n    ];\n    const [r, g, b] = curvatureToColor(\n      (k[corners[0]] + k[corners[1]] + k[corners[2]]) / 3,\n    );\n    for (let c = 0; c < 3; c++) {\n      const src = corners[c];\n      const dst = t * 3 + c;\n      flatVertices[dst * 3] = vertices[src * 3];\n      flatVertices[dst * 3 + 1] = vertices[src * 3 + 1];\n      flatVertices[dst * 3 + 2] = vertices[src * 3 + 2];\n      if (flatNormals) {\n        flatNormals[dst * 3] = normals[src * 3];\n        flatNormals[dst * 3 + 1] = normals[src * 3 + 1];\n        flatNormals[dst * 3 + 2] = normals[src * 3 + 2];\n      }\n      flatTriangles[dst] = dst;\n      vertexColors[dst * 3] = r;\n      vertexColors[dst * 3 + 1] = g;\n      vertexColors[dst * 3 + 2] = b;\n    }\n  }\n\n  return {\n    faces: {\n      ...faces,\n      vertices: flatVertices,\n      triangles: flatTriangles,\n      ...(flatNormals ? { normals: flatNormals } : {}),\n    },\n    edges,\n    // Plain array (not Float32Array) so metadata.meshOverride survives the\n    // JSON round-trip in cacheAssemblyStructure/getAssembly. Renderer wraps\n    // back into a typed array when constructing the BufferAttribute.\n    vertexColors,\n  };\n}\n\nfunction hexToRgb(hex: string): [number, number, number] {\n  const h = hex.replace(\"#\", \"\");\n  return [\n    parseInt(h.slice(0, 2), 16) / 255,\n    parseInt(h.slice(2, 4), 16) / 255,\n    parseInt(h.slice(4, 6), 16) / 255,\n  ];\n}\n",
          "compiledCode": "\"use strict\";\n/**\n * Colours selected faces of the input assembly by discrete Gaussian\n * curvature: red for concave (negative curvature), blue for convex\n * (positive curvature), white for flat/neutral, darker as curvature grows\n * more severe.\n *\n * Uses whichever faces are selected on `surface` (via a faceselect input);\n * if none are selected, every face of the assembly is coloured.\n *\n *   curvatureScale — absolute curvature magnitude (1/length², in the\n *                    model's own units) at which a colour fully saturates.\n *                    Tune per model scale: small parts with tight fillets\n *                    need a larger value, large gently-curved parts need a\n *                    smaller one.\n */\n// eslint-disable-next-line @typescript-eslint/no-unused-vars\nfunction run(surface, curvatureScale = 0.01) {\n    if (!surface) {\n        throw new Error(\"Gaussian curvature: missing required geometry input.\");\n    }\n    if (!Number.isFinite(curvatureScale) || curvatureScale <= 0) {\n        throw new Error(\"Gaussian curvature: curvatureScale must be a positive number.\");\n    }\n    const noneSelected = surface.getSelectedFaces().length === 0;\n    const facesToProcess = [];\n    // eslint-disable-next-line @typescript-eslint/no-explicit-any\n    surface.onLeafs((leaf) => {\n        if (leaf.geometry instanceof replicad.Drawing) {\n            console.warn(\"Gaussian curvature: drawings have no faces, skipping\");\n            return leaf;\n        }\n        if (noneSelected) {\n            facesToProcess.push(...(leaf.geometry instanceof replicad.Face\n                ? [leaf.geometry]\n                : leaf.geometry.faces));\n        }\n        else if (leaf.selection?.faces && leaf.selection.faces.length > 0) {\n            facesToProcess.push(...leaf.selection.faces.map((idx) => leaf.geometry.faces[idx]));\n        }\n        return leaf;\n    });\n    if (facesToProcess.length === 0) {\n        throw new Error(\"Gaussian curvature: input has no faces to visualize.\");\n    }\n    return facesToProcess.map((face) => new Assembly({\n        geometry: face,\n        color: \"#5B9BD5\",\n        tags: [\"heatmap:gaussian-curvature\"],\n        metadata: {\n            meshOverride: buildCurvatureMeshOverride(face, curvatureScale),\n        },\n    }));\n}\n// ─── meshOverride baking ──────────────────────────────────────────────────\n//\n// Tessellates the face at a fixed, coarse tolerance, then computes discrete\n// Gaussian curvature at every mesh vertex via the angle-defect method: for\n// each vertex, sum the interior angles of its adjacent triangles and compare\n// against 2π (or π on the mesh boundary); the deficit divided by the local\n// (mixed/Voronoi) area is the curvature estimate.\n//\n// Colour is baked flat per triangle (averaging its 3 corner curvatures) and\n// applied to a de-duplicated \"triangle soup\" copy of the mesh — each\n// triangle gets its own unshared vertices — so the renderer's per-vertex\n// colour interpolation can't blend two different triangles' colours across\n// a shared corner and produce a gradient within a single triangle.\nfunction buildCurvatureMeshOverride(\n// eslint-disable-next-line @typescript-eslint/no-explicit-any\nface, curvatureScale) {\n    const faces = face.mesh({ tolerance: 0.1, angularTolerance: 0.3 });\n    const edges = face.meshEdges({ tolerance: 0.1, angularTolerance: 0.3 });\n    const { vertices, triangles } = faces;\n    const numVerts = vertices.length / 3;\n    const angleSum = new Float64Array(numVerts);\n    const areaSum = new Float64Array(numVerts);\n    const edgeTriCount = new Map();\n    const neighbors = Array.from({ length: numVerts }, () => new Set());\n    const px = (i) => vertices[i * 3];\n    const py = (i) => vertices[i * 3 + 1];\n    const pz = (i) => vertices[i * 3 + 2];\n    const angleAt = (a, b, c) => {\n        const abx = px(b) - px(a), aby = py(b) - py(a), abz = pz(b) - pz(a);\n        const acx = px(c) - px(a), acy = py(c) - py(a), acz = pz(c) - pz(a);\n        const abLen = Math.hypot(abx, aby, abz);\n        const acLen = Math.hypot(acx, acy, acz);\n        if (abLen === 0 || acLen === 0)\n            return 0;\n        const cos = (abx * acx + aby * acy + abz * acz) / (abLen * acLen);\n        return Math.acos(Math.min(1, Math.max(-1, cos)));\n    };\n    const edgeKey = (i, j) => (i < j ? `${i}_${j}` : `${j}_${i}`);\n    const numTriangles = triangles.length / 3;\n    for (let t = 0; t < numTriangles; t++) {\n        const i0 = triangles[t * 3];\n        const i1 = triangles[t * 3 + 1];\n        const i2 = triangles[t * 3 + 2];\n        const abx = px(i1) - px(i0), aby = py(i1) - py(i0), abz = pz(i1) - pz(i0);\n        const acx = px(i2) - px(i0), acy = py(i2) - py(i0), acz = pz(i2) - pz(i0);\n        const cx = aby * acz - abz * acy;\n        const cy = abz * acx - abx * acz;\n        const cz = abx * acy - aby * acx;\n        const area = 0.5 * Math.hypot(cx, cy, cz);\n        angleSum[i0] += angleAt(i0, i1, i2);\n        angleSum[i1] += angleAt(i1, i2, i0);\n        angleSum[i2] += angleAt(i2, i0, i1);\n        areaSum[i0] += area / 3;\n        areaSum[i1] += area / 3;\n        areaSum[i2] += area / 3;\n        for (const [a, b] of [\n            [i0, i1],\n            [i1, i2],\n            [i2, i0],\n        ]) {\n            const key = edgeKey(a, b);\n            edgeTriCount.set(key, (edgeTriCount.get(key) ?? 0) + 1);\n            neighbors[a].add(b);\n            neighbors[b].add(a);\n        }\n    }\n    // Vertices touched by an edge that borders only one triangle sit on the\n    // face's trimmed boundary. The angle-defect formula only measures\n    // curvature correctly for vertices fully surrounded by triangles (a full\n    // 2π turn); at a boundary vertex the angle sum is just the mesh's local\n    // corner angle, which has nothing to do with surface curvature (e.g. a\n    // flat rectangular face's corners would read as sharply convex). Instead,\n    // boundary vertices inherit curvature from their interior neighbors, and\n    // default to 0 (flat) when the face has no interior vertices at all — as\n    // happens for a coarsely-tessellated flat face like a cube side.\n    const isBoundary = new Uint8Array(numVerts);\n    for (const [key, count] of edgeTriCount) {\n        if (count === 1) {\n            const [a, b] = key.split(\"_\").map(Number);\n            isBoundary[a] = 1;\n            isBoundary[b] = 1;\n        }\n    }\n    const k = new Float64Array(numVerts);\n    for (let v = 0; v < numVerts; v++) {\n        if (isBoundary[v])\n            continue;\n        const area = areaSum[v];\n        k[v] = area > 0 ? (2 * Math.PI - angleSum[v]) / area : 0;\n    }\n    for (let v = 0; v < numVerts; v++) {\n        if (!isBoundary[v])\n            continue;\n        let sum = 0;\n        let count = 0;\n        for (const n of neighbors[v]) {\n            if (!isBoundary[n]) {\n                sum += k[n];\n                count++;\n            }\n        }\n        k[v] = count > 0 ? sum / count : 0;\n    }\n    const [r0, g0, b0] = hexToRgb(\"#ffffff\");\n    const [rConcave, gConcave, bConcave] = hexToRgb(\"#ff0000\");\n    const [rConvex, gConvex, bConvex] = hexToRgb(\"#0000ff\");\n    const curvatureToColor = (curvature) => {\n        let t = curvature / curvatureScale;\n        if (t < -1)\n            t = -1;\n        else if (t > 1)\n            t = 1;\n        const mag = Math.abs(t);\n        const [rBase, gBase, bBase] = t < 0 ? [rConcave, gConcave, bConcave] : [rConvex, gConvex, bConvex];\n        const darken = 1 - 0.4 * mag;\n        return [\n            (r0 + (rBase - r0) * mag) * darken,\n            (g0 + (gBase - g0) * mag) * darken,\n            (b0 + (bBase - b0) * mag) * darken,\n        ];\n    };\n    const { normals } = faces;\n    const hasNormals = Array.isArray(normals) && normals.length === vertices.length;\n    const flatVertices = new Array(numTriangles * 9);\n    const flatNormals = hasNormals\n        ? new Array(numTriangles * 9)\n        : undefined;\n    const flatTriangles = new Array(numTriangles * 3);\n    const vertexColors = new Array(numTriangles * 9);\n    for (let t = 0; t < numTriangles; t++) {\n        const corners = [\n            triangles[t * 3],\n            triangles[t * 3 + 1],\n            triangles[t * 3 + 2],\n        ];\n        const [r, g, b] = curvatureToColor((k[corners[0]] + k[corners[1]] + k[corners[2]]) / 3);\n        for (let c = 0; c < 3; c++) {\n            const src = corners[c];\n            const dst = t * 3 + c;\n            flatVertices[dst * 3] = vertices[src * 3];\n            flatVertices[dst * 3 + 1] = vertices[src * 3 + 1];\n            flatVertices[dst * 3 + 2] = vertices[src * 3 + 2];\n            if (flatNormals) {\n                flatNormals[dst * 3] = normals[src * 3];\n                flatNormals[dst * 3 + 1] = normals[src * 3 + 1];\n                flatNormals[dst * 3 + 2] = normals[src * 3 + 2];\n            }\n            flatTriangles[dst] = dst;\n            vertexColors[dst * 3] = r;\n            vertexColors[dst * 3 + 1] = g;\n            vertexColors[dst * 3 + 2] = b;\n        }\n    }\n    return {\n        faces: {\n            ...faces,\n            vertices: flatVertices,\n            triangles: flatTriangles,\n            ...(flatNormals ? { normals: flatNormals } : {}),\n        },\n        edges,\n        // Plain array (not Float32Array) so metadata.meshOverride survives the\n        // JSON round-trip in cacheAssemblyStructure/getAssembly. Renderer wraps\n        // back into a typed array when constructing the BufferAttribute.\n        vertexColors,\n    };\n}\nfunction hexToRgb(hex) {\n    const h = hex.replace(\"#\", \"\");\n    return [\n        parseInt(h.slice(0, 2), 16) / 255,\n        parseInt(h.slice(2, 4), 16) / 255,\n        parseInt(h.slice(4, 6), 16) / 255,\n    ];\n}\n"
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "geometry",
          "ap1ID": "id-135982",
          "ap2ID": "id-136236"
        },
        {
          "ap2Name": "retain",
          "ap1ID": "id-135982",
          "ap2ID": "id-136242"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-135985",
          "ap2ID": "id-136933"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-135996",
          "ap2ID": "id-136006"
        },
        {
          "ap2Name": "Assembly",
          "ap1ID": "id-136000",
          "ap2ID": "id-136246"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-136006",
          "ap2ID": "id-136000"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-136012",
          "ap2ID": "id-136016"
        },
        {
          "ap2Name": "shape",
          "ap1ID": "id-136012",
          "ap2ID": "id-136226"
        },
        {
          "ap2Name": "remove",
          "ap1ID": "id-136236",
          "ap2ID": "id-136242"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-136242",
          "ap2ID": "id-135996"
        },
        {
          "ap2Name": "Shape 2",
          "ap1ID": "id-136929",
          "ap2ID": "id-135991"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-136929",
          "ap2ID": "id-135985"
        },
        {
          "ap2Name": "Shape 1",
          "ap1ID": "id-136933",
          "ap2ID": "id-135991"
        },
        {
          "ap2Name": "surface",
          "ap1ID": "id-136213",
          "ap2ID": "id-136924"
        },
        {
          "ap2Name": "surface",
          "ap1ID": "id-136246",
          "ap2ID": "id-136199"
        },
        {
          "ap2Name": "retain",
          "ap1ID": "id-136962",
          "ap2ID": "id-136012"
        },
        {
          "ap2Name": "Geometry",
          "ap1ID": "id-136962",
          "ap2ID": "id-136984"
        },
        {
          "ap2Name": "Geometry",
          "ap1ID": "id-136984",
          "ap2ID": "id-137018"
        },
        {
          "ap2Name": "faces",
          "ap1ID": "id-136984",
          "ap2ID": "id-136945"
        },
        {
          "ap2Name": "surface",
          "ap1ID": "id-136984",
          "ap2ID": "id-137064"
        },
        {
          "ap2Name": "Assembly",
          "ap1ID": "id-136984",
          "ap2ID": "id-136213"
        }
      ],
      "fileTypeVersion": 1
    },
    {
      "atomType": "Code",
      "x": 0.5,
      "y": 0.6,
      "uniqueID": "id-136466",
      "ioValues": [
        {
          "name": "x",
          "ioValue": 5
        },
        {
          "name": "extra",
          "ioValue": "__GEOMETRY_INPUT__"
        }
      ],
      "interpreterVersion": 1,
      "code": "\n/**\n * =========================================================================\n *  ABUNDANCE CODE ATOM — Quick Reference\n * =========================================================================\n *\n *  HOW INPUTS WORK\n *  ---------------\n *  Every parameter of run() becomes an input port on this atom.\n *  Four types are supported:\n *\n *    number   -> numeric input / slider\n *    string   -> text input  (great for hex color codes, labels, etc.)\n *    boolean  -> true/false toggle\n *    Assembly -> geometry -- wire this to the output of another atom\n *\n *  Give a parameter a default value ( = x ) to make it optional.\n *  Mark it with ? (name?: Assembly) to make it optional with no default.\n *  Parameters without a default must be connected before the atom runs.\n *\n *  RETURN VALUE\n *  ------------\n *  Return an Assembly, an Assembly[], or a primitive (number/string/boolean).\n *  Returning an Assembly[] lets downstream atoms (BOM, cut layout...)\n *  see each piece independently.\n *\n *  GEOMETRY API  (replicad is available as a global)\n *  -------------------------------------------------\n *  Create 2-D sketches\n *    replicad.drawCircle(radius)\n *    replicad.drawRectangle(width, height)\n *    replicad.drawRoundedRectangle(width, height, cornerRadius)\n *\n *  Turn a 2-D sketch into a 3-D solid\n *    sketch.sketchOnPlane().extrude(distance)\n *    sketch.sketchOnPlane().revolve()          // rotate 360 deg around Z\n *\n *  Create 3-D primitives directly\n *    replicad.makeBaseBox(width, depth, height)\n *    replicad.makeSphere(radius)\n *    replicad.makeCylinder(radius, height)\n *\n *  Boolean operations\n *    solidA.fuse(solidB)                       // union\n *    solidA.cut(solidB)                        // difference (cut a hole)\n *    solidA.intersect(solidB)                  // intersection\n *\n *  Post-processing\n *    solid.fillet(radius)                      // round ALL edges\n *    solid.fillet(r, e => e.inPlane(\"XY\", z))  // round only top-face edges\n *    solid.chamfer(distance)                   // bevel ALL edges\n *    solid.translate(x, y, z)                  // move the solid\n *    solid.rotate(angle, [0, 0, 1])            // rotate around Z axis\n *\n *  ASSEMBLY CLASS\n *  --------------\n *  Wrap any replicad solid in an Assembly to attach display metadata:\n *    new Assembly({ geometry, color, tags, bom })\n *      geometry -> replicad solid / drawing returned by the API above\n *      color    -> hex string shown in the 3-D viewport  (\"#A3CE5B\")\n *      tags     -> string[] used for filtering in cut layout / BOM\n *      bom      -> string[] lines added to the Bill of Materials panel\n *\n *  AI PROMPT TIP\n *  -------------\n *  Copy the code in this editor into ChatGPT / Claude and say something like:\n *    \"Modify this Abundance Code Atom to create a gear with 12 teeth\"\n *    \"Change this to make a hexagonal nut instead of a round plate\"\n *  The comments above give the AI all the context it needs to produce\n *  valid, working Abundance TypeScript code.\n * =========================================================================\n */\n\n/**\n * A rectangular mounting plate with a circular hole in the centre.\n * All parameters have defaults so the atom produces geometry immediately\n * after creation — no connections required to get started.\n */\nfunction run(\n  x: number = 5,\n  extra?: Assembly           \n) {\n  console.log(`foo ${context.units}`)\n  return new Assembly({geometry: replicad.drawCircle(x)})\n}\n",
      "compiledCode": "\"use strict\";\n/**\n * =========================================================================\n *  ABUNDANCE CODE ATOM — Quick Reference\n * =========================================================================\n *\n *  HOW INPUTS WORK\n *  ---------------\n *  Every parameter of run() becomes an input port on this atom.\n *  Four types are supported:\n *\n *    number   -> numeric input / slider\n *    string   -> text input  (great for hex color codes, labels, etc.)\n *    boolean  -> true/false toggle\n *    Assembly -> geometry -- wire this to the output of another atom\n *\n *  Give a parameter a default value ( = x ) to make it optional.\n *  Mark it with ? (name?: Assembly) to make it optional with no default.\n *  Parameters without a default must be connected before the atom runs.\n *\n *  RETURN VALUE\n *  ------------\n *  Return an Assembly, an Assembly[], or a primitive (number/string/boolean).\n *  Returning an Assembly[] lets downstream atoms (BOM, cut layout...)\n *  see each piece independently.\n *\n *  GEOMETRY API  (replicad is available as a global)\n *  -------------------------------------------------\n *  Create 2-D sketches\n *    replicad.drawCircle(radius)\n *    replicad.drawRectangle(width, height)\n *    replicad.drawRoundedRectangle(width, height, cornerRadius)\n *\n *  Turn a 2-D sketch into a 3-D solid\n *    sketch.sketchOnPlane().extrude(distance)\n *    sketch.sketchOnPlane().revolve()          // rotate 360 deg around Z\n *\n *  Create 3-D primitives directly\n *    replicad.makeBaseBox(width, depth, height)\n *    replicad.makeSphere(radius)\n *    replicad.makeCylinder(radius, height)\n *\n *  Boolean operations\n *    solidA.fuse(solidB)                       // union\n *    solidA.cut(solidB)                        // difference (cut a hole)\n *    solidA.intersect(solidB)                  // intersection\n *\n *  Post-processing\n *    solid.fillet(radius)                      // round ALL edges\n *    solid.fillet(r, e => e.inPlane(\"XY\", z))  // round only top-face edges\n *    solid.chamfer(distance)                   // bevel ALL edges\n *    solid.translate(x, y, z)                  // move the solid\n *    solid.rotate(angle, [0, 0, 1])            // rotate around Z axis\n *\n *  ASSEMBLY CLASS\n *  --------------\n *  Wrap any replicad solid in an Assembly to attach display metadata:\n *    new Assembly({ geometry, color, tags, bom })\n *      geometry -> replicad solid / drawing returned by the API above\n *      color    -> hex string shown in the 3-D viewport  (\"#A3CE5B\")\n *      tags     -> string[] used for filtering in cut layout / BOM\n *      bom      -> string[] lines added to the Bill of Materials panel\n *\n *  AI PROMPT TIP\n *  -------------\n *  Copy the code in this editor into ChatGPT / Claude and say something like:\n *    \"Modify this Abundance Code Atom to create a gear with 12 teeth\"\n *    \"Change this to make a hexagonal nut instead of a round plate\"\n *  The comments above give the AI all the context it needs to produce\n *  valid, working Abundance TypeScript code.\n * =========================================================================\n */\n/**\n * A rectangular mounting plate with a circular hole in the centre.\n * All parameters have defaults so the atom produces geometry immediately\n * after creation — no connections required to get started.\n */\nfunction run(x = 5, extra) {\n    console.log(`foo ${context.units}`);\n    return new Assembly({ geometry: replicad.drawCircle(x) });\n}\n"
    },
    {
      "atomType": "GitHubMolecule",
      "x": 0.4,
      "y": 0.7,
      "uniqueID": "id-136689",
      "name": "CodeTest",
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 1,
          "y": 0.5,
          "uniqueID": "id-136681",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "GitHubMolecule",
          "x": 0.5,
          "y": 0.6,
          "uniqueID": "id-136682",
          "name": "selected.Fillet",
          "ioValues": [
            {
              "name": "radius",
              "ioValue": 3
            },
            {
              "name": "shape",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "topLevel": false,
          "allAtoms": [
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136683",
              "name": "radius",
              "options": []
            },
            {
              "atomType": "Output",
              "x": 1,
              "y": 0.5,
              "uniqueID": "id-136684",
              "ioValues": [
                {
                  "name": "number or geometry",
                  "ioValue": "__GEOMETRY_INPUT__"
                }
              ]
            },
            {
              "atomType": "Code",
              "x": 0.4,
              "y": 0.7,
              "uniqueID": "id-136685",
              "ioValues": [
                {
                  "name": "toFillet",
                  "ioValue": "__GEOMETRY_INPUT__"
                },
                {
                  "name": "radius",
                  "ioValue": 3
                }
              ],
              "interpreterVersion": 1,
              "code": "function run(toFillet: Assembly, radius: number) {\n  return toFillet.onLeafs((leaf) => {\n    if (leaf.geometry instanceof replicad.Drawing || !replicad.isShape3D(leaf.geometry)) {\n      return leaf;\n    }\n    if (leaf.selection?.part) {\n      leaf.geometry = leaf.geometry.fillet(radius)\n      return leaf;\n    }\n    // else apply only to filtered faces and edges\n    let edgesToFillet: replicad.Edge[] = []\n    if (leaf.selection?.faces) {\n      leaf.selection.faces.forEach((faceIndex) => {\n        edgesToFillet.push(...leaf.geometry.faces[faceIndex].edges)\n      });\n    }\n    console.log(edgesToFillet)\n    console.log(\"intermediate\")\n    console.log(leaf.selection)\n    if (leaf.selection?.edges) {\n      edgesToFillet.push(...leaf.selection.edges.map((index) => leaf.geometry.edges[index]))\n    }\n    if (edgesToFillet.length == 0) {\n      return leaf;\n    }\n    edgesToFillet = edgesToFillet.filter((e) => e != null)\n    if (edgesToFillet.length == 0) {\n      return leaf.geometry\n    }\n    console.log(edgesToFillet)\n    leaf.geometry = leaf.geometry.fillet(radius, (e) => { return e.inList(edgesToFillet.map((e) => e.clone())) })\n    return leaf;\n  })\n}",
              "compiledCode": "\"use strict\";\nfunction run(toFillet, radius) {\n    return toFillet.onLeafs((leaf) => {\n        if (leaf.geometry instanceof replicad.Drawing || !replicad.isShape3D(leaf.geometry)) {\n            return leaf;\n        }\n        if (leaf.selection?.part) {\n            leaf.geometry = leaf.geometry.fillet(radius);\n            return leaf;\n        }\n        // else apply only to filtered faces and edges\n        let edgesToFillet = [];\n        if (leaf.selection?.faces) {\n            leaf.selection.faces.forEach((faceIndex) => {\n                edgesToFillet.push(...leaf.geometry.faces[faceIndex].edges);\n            });\n        }\n        console.log(edgesToFillet);\n        console.log(\"intermediate\");\n        console.log(leaf.selection);\n        if (leaf.selection?.edges) {\n            edgesToFillet.push(...leaf.selection.edges.map((index) => leaf.geometry.edges[index]));\n        }\n        if (edgesToFillet.length == 0) {\n            return leaf;\n        }\n        edgesToFillet = edgesToFillet.filter((e) => e != null);\n        if (edgesToFillet.length == 0) {\n            return leaf.geometry;\n        }\n        console.log(edgesToFillet);\n        leaf.geometry = leaf.geometry.fillet(radius, (e) => { return e.inList(edgesToFillet.map((e) => e.clone())); });\n        return leaf;\n    });\n}\n"
            },
            {
              "atomType": "Input",
              "x": 0,
              "y": 0.2,
              "uniqueID": "id-136686",
              "name": "shape",
              "type": "edgeselect",
              "options": [],
              "selectedEdgeData": {
                "0": [
                  4
                ]
              }
            }
          ],
          "allConnectors": [
            {
              "ap2Name": "radius",
              "ap1ID": "id-136683",
              "ap2ID": "id-136685"
            },
            {
              "ap2Name": "number or geometry",
              "ap1ID": "id-136685",
              "ap2ID": "id-136684"
            },
            {
              "ap2Name": "toFillet",
              "ap1ID": "id-136686",
              "ap2ID": "id-136685"
            }
          ],
          "parentRepo": {
            "lastFoundGit": "2026-09-20T02:23:39.099Z",
            "privateRepo": false,
            "repoName": "selected.Fillet",
            "dateCreated": "2026-07-21T21:29:20Z",
            "likes": 0,
            "pngURL": "https://raw.githubusercontent.com/tristan-huber/selected.Fillet/master/project.png?sanitize=true",
            "readme": "https://raw.githubusercontent.com/tristan-huber/selected.Fillet/master/README.md?sanitize=true",
            "yyyy": 2026,
            "topMoleculeID": "",
            "userRanking": 0,
            "userSetAsThumbnail": false,
            "githubMoleculesUsed": [],
            "html_url": "https://github.com/tristan-huber/selected.Fillet",
            "topics": [
              "abundance-tool"
            ],
            "pullRequests": [],
            "forks": 0,
            "dateModified": "2026-07-21T21:34:22Z",
            "ranking": 3,
            "searchField": "selected.fillet tristan-huber fillet the only the selected edges of an assembly. you can also input face or sub-assembly selections, edges will be filleted if they meet any of the selection criteria, eg on a selected subassembly, on a selected face. default fillets all edges. abundance-tool",
            "owner": "tristan-huber",
            "description": "Fillet the only the selected edges of an assembly. You can also input face or sub-assembly selections, edges will be filleted if they meet any of the selection criteria, eg on a selected subassembly, on a selected face. Default fillets all edges.",
            "contentURL": "https://raw.githubusercontent.com/tristan-huber/selected.Fillet/master/project.abundance?sanitize=true",
            "svgURL": "https://raw.githubusercontent.com/tristan-huber/selected.Fillet/master/project.svg?sanitize=true",
            "isLocal": false
          },
          "unitsKey": "Unitless",
          "fileTypeVersion": 1,
          "lastReloadedFromGithubAt": 1790356787159
        },
        {
          "atomType": "Extrude",
          "x": 0.3,
          "y": 0.4,
          "uniqueID": "id-136687",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "height",
              "ioValue": 30
            }
          ]
        },
        {
          "atomType": "Rectangle",
          "x": 0.1,
          "y": 0.2,
          "uniqueID": "id-136688",
          "ioValues": [
            {
              "name": "x length",
              "ioValue": 10
            },
            {
              "name": "y length",
              "ioValue": 20
            }
          ]
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "shape",
          "ap1ID": "id-136687",
          "ap2ID": "id-136682"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-136688",
          "ap2ID": "id-136687"
        },
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-136682",
          "ap2ID": "id-136681"
        }
      ],
      "parentRepo": {
        "lastFoundGit": "2026-09-20T02:23:39.099Z",
        "privateRepo": false,
        "repoName": "CodeTest",
        "dateCreated": "2025-06-20T17:34:36Z",
        "likes": 0,
        "pngURL": "https://raw.githubusercontent.com/tristan-huber/CodeTest/main/project.png",
        "readme": "https://raw.githubusercontent.com/tristan-huber/CodeTest/master/README.md?sanitize=true",
        "yyyy": 2025,
        "topMoleculeID": "",
        "userRanking": 0,
        "userSetAsThumbnail": false,
        "githubMoleculesUsed": [
          {
            "owner": "tristan-huber",
            "repoName": "selected.Fillet"
          }
        ],
        "html_url": "https://github.com/tristan-huber/CodeTest",
        "topics": [],
        "pullRequests": [],
        "forks": 0,
        "dateModified": "739884",
        "ranking": 1,
        "searchField": "codetest tristan-huber  ",
        "owner": "tristan-huber",
        "description": "",
        "contentURL": "https://raw.githubusercontent.com/tristan-huber/CodeTest/master/project.abundance?sanitize=true",
        "svgURL": "https://raw.githubusercontent.com/tristan-huber/CodeTest/master/project.svg?sanitize=true",
        "isLocal": false
      },
      "unitsKey": "MM",
      "fileTypeVersion": 1,
      "lastReloadedFromGithubAt": 1790359263757
    }
  ],
  "allConnectors": [
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135505",
      "ap2ID": "id-135522"
    },
    {
      "ap2Name": "assembly",
      "ap1ID": "id-135509",
      "ap2ID": "id-135531"
    },
    {
      "ap2Name": "Shape 2",
      "ap1ID": "id-135522",
      "ap2ID": "id-135612"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135543",
      "ap2ID": "id-135552"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135543",
      "ap2ID": "id-135558"
    },
    {
      "ap2Name": "x length",
      "ap1ID": "id-135546",
      "ap2ID": "id-135505"
    },
    {
      "ap2Name": "y length",
      "ap1ID": "id-135548",
      "ap2ID": "id-135505"
    },
    {
      "ap2Name": "y length",
      "ap1ID": "id-135548",
      "ap2ID": "id-135600"
    },
    {
      "ap2Name": "Shape 2",
      "ap1ID": "id-135552",
      "ap2ID": "id-135564"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-135558",
      "ap2ID": "id-135564"
    },
    {
      "ap2Name": "assembly",
      "ap1ID": "id-135564",
      "ap2ID": "id-135573"
    },
    {
      "ap2Name": "Shape 2",
      "ap1ID": "id-135564",
      "ap2ID": "id-135582"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135582",
      "ap2ID": "id-135587"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135587",
      "ap2ID": "id-135593"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135593",
      "ap2ID": "id-135597"
    },
    {
      "ap2Name": "Shape 2",
      "ap1ID": "id-135597",
      "ap2ID": "id-135608"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135600",
      "ap2ID": "id-135604"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-135604",
      "ap2ID": "id-135612"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135608",
      "ap2ID": "id-135509"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-135612",
      "ap2ID": "id-135608"
    },
    {
      "ap2Name": "number or geometry",
      "ap1ID": "id-135531",
      "ap2ID": "id-135503"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-135573",
      "ap2ID": "id-135582"
    },
    {
      "ap2Name": "Geometry",
      "ap1ID": "id-135788",
      "ap2ID": "id-135935"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-135935",
      "ap2ID": "id-135798"
    }
  ],
  "unitsKey": "Inches",
  "fileTypeVersion": 1,
  "filetypeVersion": 1
}